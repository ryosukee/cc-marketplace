schema_version: "1.1"
name: neg-03-github-issue
description: GitHub に issue を立てさせるだけの依頼で、どちらの skill も発動しないこと
tags: [description, negative, boundary]
context:
  scaffold_script: scaffold.sh
execution:
  prompt: この不具合、GitHubにissueを立てておいて。
  max_turns: 8
  timeout_seconds: 300
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Write, Edit]
runs: 3
graders:
  - name: kanban-skill-not-fired
    type: tool_used
    tool: Skill
    input_match: '"skill"\s*:\s*"(?:[\w-]+:)?plane-kanban"'
    min: 0
    max: 0
  - name: setup-skill-not-fired
    type: tool_used
    tool: Skill
    input_match: '"skill"\s*:\s*"(?:[\w-]+:)?plane-kanban-setup"'
    min: 0
    max: 0
