---
name: start
description: >-
  セッション開始。前回の引き継ぎ資料を読み込み、コンテキストを復元し、
  今日やることを提案する。
  "続きから"、"今日の作業"、"session start"、"セッション開始" 等で発動。
---

# start

前セッションの引き継ぎ資料を読み込み、コンテキストを復元し、方向を提案する。

## ワークフロー

### ステップ 1: .handover/ ディレクトリの確認

`${CLAUDE_PLUGIN_ROOT}/references/handover-init.md` を Read し、記載された手順に従って `.handover/` の存在確認と初期化を行う。
特定された `.handover/` の絶対パスを以降のステップすべてで使う。

初回作成の場合はステップ 4 に進む。

### ステップ 2: todo の読み込み

`.handover/todo/` 内の全ファイルを Read する (ステップ 1 で特定したパス基準)。

ファイルがない場合はステップ 4 に進む。

### ステップ 3: ユーザーへの提示と判断委譲

読み込んだ全 handover を統合して、次の 3 点だけを提示する。

1. 何をしている途中か (1〜2 文)
2. どこまで終わったか (成果物の識別子。PR 番号・コミット ID)
3. 続きの起点 (最初に読むファイル、最初に実行すること)

これに復元タスクの一覧を添える。登録の可否を判断する対象なので一覧で出す。

handover の節を順に要約しない。上の 3 点と復元タスク以外は提示しない。
詳細が要るときはユーザーが handover を読む。

提示後、読み込んだ todo/ の handover ファイルを archive/ へ移動する（確認不要。
archive は全件保持の移動であり、見返す必要が出たら archive/ を読めばよい）。
読むだけの参照台帳など handover でないファイルが todo/ にある場合は移動せず、
本来の置き場への移設をユーザーに提案する。

そのうえでユーザーに判断を委ねる:

- タスクの復元: 復元タスクとして記載されたタスクを復元するか
- 方向: 前回の続きをするか、別のことをするか

ユーザーの指示に従って、承認されたタスクを TaskCreate で登録する。

実行した処理は必ず報告する。archive へ移動したファイル名と、登録したタスクの一覧を、
ステップ 5 までの報告に含める。黙って移動・登録しない。

### ステップ 4: draft の作成

`.handover/draft/` に新しい draft ファイルを作成する。

- ファイル名: セッション内容を表す slug を仮で付ける (例: `continue-auth-refactor.md`)
- 内容: handover テンプレートの骨格 (セクション見出しのみ)
- ステップ 3 で読み込んだ情報があれば「背景」「ゴール・原則」に転記する

既に `.handover/draft/` にファイルが存在する場合 (前回 end せず終了):

- 対象 draft は上記 handover-init.md の「draft の特定」に従う
- 既存 draft の内容をユーザーに提示する
- 再利用するか新規作成するか確認する

### ステップ 5: 方向提案

復元タスクの筆頭を起点として、次に着手するものを 1 つ提案する。
候補を並べない。復元タスクは既にステップ 3 で一覧にしてあり、
そこから選ぶのはユーザーの判断になる。

テキスト出力のみ。ユーザーが自由に返答できるようにする。

## エラーハンドリング

| エラー | 解決方法 |
|--------|---------|
| .handover/ が存在しない | ステップ 1 で作成を提案 |
| todo/ が空 | ステップ 4 に進む (初回または前回 handover なし) |
| draft/ に既存ファイル | ユーザーに再利用/新規作成を確認 |
