# {{project_name}}

## 技術スタック

| レイヤー | 技術 |
|---------|------|
{{tech_stack_table}}

## ビルド・検証コマンド

```bash
# ビルド
{{build_command}}

# lint
{{lint_command}}

# テスト
{{test_command}}
```

## ディレクトリ構成

```
{{directory_structure}}
```

## ドメインモデル

(プロジェクトの成長に合わせて記述する。ER 関係、主要エンティティ等)

## 主要フロー

(プロジェクトの成長に合わせて記述する。ユーザーフロー、データフロー等)

## ワークフロー

チーム開発ワークフローの概要:

- `/impl-plan {機能概要}` で実装プランを作成
- `/team-implement plans/{名前}.md` でパイプライン実行
- 詳細は `.claude/agents/` と `.claude/skills/` を参照

アーキテクチャルール:
- 詳細は `.claude/rules/` を参照
