# 未コミットの codify 2 件の実文（2026-08-25 保存）

他セッションの retrospective が symlink（`~/.claude/rules/cc-marketplace`）経由で `rules/` へ書き、
main の作業ツリーに未コミットのまま残っていた変更 2 件の実文。ccm-f044 設問 6 の回答
（ブランチ退避ではなく、保持すべき内容を保存して取り組むときに復元する）に従って保存した。
出典は [norm-refit 台帳](../norm-refit.md) の 2026-08-25 エントリ。

復元するときは、下の diff をそのまま `git apply` するか、節を手で写す。
`decision-record.md` の節は ccm-f044 設問 1 の決着（「エントリの中へ」を「repo 内に実文を持つ」へ緩める）に
合わせて書き換えてから入れる。

## rules/decision-record.md（+17 行）

```diff
diff --git a/rules/decision-record.md b/rules/decision-record.md
index a4e0c8a..863bbd2 100644
--- a/rules/decision-record.md
+++ b/rules/decision-record.md
@@ -21,6 +21,23 @@
 - 何を決めなかったか（保留にした範囲・対象外にした範囲）
 - 決め手（採用側が満たした条件）
 - 出典（決定した回・フォーム・issue へのリンク）
+- 出典の実文（下記）
+
+## 出典は実文ごと台帳に入れる
+
+出典は場所を指すだけなので、指した先の実文を必ずエントリの中へ転記する。
+対象は形式を問わない。フォーム・issue・PR のコメント・コミット・会話のすべて。
+
+- 転記するのは、その確定を生んだ問いと、選ばれた答え。設問文・選択肢・回答者の補足を、
+  要約せずそのまま引く
+- 原本が失われている、または原本にあたる資料が無いときは、実文の代わりに
+  「原本なし」と書き、何が失われたかを 1 行で残す
+- 転記が長くなっても削らない。読みやすさのための省略は要約資料の仕事で、台帳の仕事ではない
+
+判定基準: そのエントリだけを読んだ人が、何を問われて何を選んだかを再構成できるか。
+
+why: 出典がリンクだけだと、指した先が消えた時点で確定の根拠が復元できなくなる。
+指した先が最初から存在しなくても、誰も気づけない。
 
 ## 台帳の数値は決定と観測を分けて扱う
 
```

## rules/primary-sources-first.md（+14 行）

```diff
diff --git a/rules/primary-sources-first.md b/rules/primary-sources-first.md
index 138c226..5c1bba3 100644
--- a/rules/primary-sources-first.md
+++ b/rules/primary-sources-first.md
@@ -33,6 +33,20 @@ git 履歴も一次情報に含める。過去にどう判断してなぜ変え
 - 自分の文書に規定・仕様・記録への参照を書くときは、参照先の実在を確かめてから書く。
   「そういう規定があるはずだ」で書かない
 
+## 測ったものと主張したいことが同じかを確かめる
+
+「実測」「確認済み」と書くときは、測定が返した値の主語と、主張の主語が一致しているかを確かめる。
+
+- 測定が返したのは何についての値かを、主張とは別に 1 文で書き出す。
+  書き出した文と主張が同じ対象を指していなければ、その測定は根拠にならない
+- 結果が主張と同じ方向を向いていることは、根拠になっている証拠ではない。
+  隣接する対象の測定は、しばしば主張と同じ方向を向く
+- 噛み合っていないと分かったら、主張の側を「未確認」に落とすか、
+  主張の対象を直接測り直す
+
+判定基準: その測定で、主張の反対が真だった場合に別の値が出るか。
+出ないなら、その測定は主張を確かめていない。
+
 ## why
 
 実例（このルールの起点になった見落とし 2 件、2026-07 の設計セッション）:
```
