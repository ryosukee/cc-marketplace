# レビューの構造レベル指摘 事例集

レビューで受けた構造の観点の指摘を集める。対象は、何をどこに置くか、何を書いて何を落とすか、
表と地の文の役割分担、参照の張り方。規範の内容の是非は含めない。

文レベル（語順・格の位置・係り受け・助詞・曖昧な構造・表記）は
[norm-refit の PR レビューの文レベル指摘 事例集](./sentence-level-review-cases.md) が扱い、こちらでは扱わない。
1 つの指摘が両方に当たるときは、当たる部分をそれぞれの事例集に分けて書き、互いの事例番号で参照する。

記法: 「before」は指摘された記述、「after」は直した記述。各事例の実文は逐語で、要約・言い換えをしない。
引用の中のリンク記法は、指す先がこの repo に無く markdownlint の link-fragments に掛かるため、インラインコードで囲んで文字だけを残す。
出所: 各事例に「出所: 人間のレビュー（repo 名）」「出所: review agent」のいずれかを付ける。信頼度が違うので混ぜて数えない。
収集元の repo: cc-marketplace 以外から来た事例は、節の見出しの先頭に `[repo 名]` を付け、出典にその repo の PR とレビューの場所を書く。
型の名前は指摘を束ねるためのもので、規則として読まない。指摘の実文が述べていない一般則を型の名前に書かない。
型: 1 つの事例が複数の型に当たることがある。節の見出しはその事例を置いた型を示し、他にも当たる型があるときは事例の末尾に「型: 1, 3」の形で当たる型をすべて列挙する（節の型も含める）。件数は延べ（型ごとの合計）と実数（事例の数）を分けて出す。

## 型 1: 表から読めることを地の文で繰り返す

### 1-1 [efso-document] 冒頭の要約が表の内容をなぞる

efso-document の PR #2912（Auth0 の代替 IdP 比較）の diffo レビュー。h1 直下の要約が、直後の順位表と同じことを述べていた。

指摘の実文（diffo スレッド e2cde7b7、2026-09-09）:

> ここの要約は不要。テーブルを見ればわかる。

before:

> 推奨は Keycloak セルフホスト、次点は Zitadel セルフホスト。Amazon Cognito (Essentials) は残る確認が通れば 3 位に入り、Ory Network・Ory セルフホスト (Enterprise License)・Zitadel Cloud・FusionAuth は見積り次第で候補に残す。Entra External ID は候補外、自前実装は最後の手段で、GCP Identity Platform と Clerk は脱落した。

after（`6f2c3346a`）: 段落ごと削除。h1 直下は「何の比較か」の 1 文だけになった

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド e2cde7b7 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:5`（`75027636c` 時点）。修正は `6f2c3346a`

### 1-2 [efso-document] 表の値を説明する注記が要らない

efso-document の PR #2912 の diffo レビュー。表に「提供形態」列を足したとき、値の意味を説明する ※ を併せて置いた。

指摘の実文（diffo スレッド 2ee46b67、2026-09-09）:

> この説明は不要

before:

> ※ 提供形態の「SaaS・セルフホスト」は FusionAuth がどちらでも動かせること、「セルフホスト（自社構築）」は既製の IdP 製品を運用するセルフホストとは別物で部品から組むこと（`[3.9 自前実装（部品を選んで組む）](#39-自前実装部品を選んで組む)`）を指す

after（`b0272ad3b`）: ※ ごと削除。同じ指摘で値そのものを短くし（「セルフホスト（自社構築）」→「自社構築」）、注記なしで読める形にした

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 2ee46b67 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:27`（`0edf54247` 時点）。修正は `b0272ad3b`

### 1-3 [efso-document] 列挙した値の全部を説明しない

efso-document の PR #2912 の diffo レビュー。判定の 7 値を列挙し、全部に説明を付けていた。
指摘は 2 点で、置き場を ※ にすることと、読み手が迷う値だけに絞ること。

指摘の実文（diffo スレッド 4e8ace6e、2026-09-09）:

> まず表に対する説明として※で書いた方がよい。それから 7 値の説明は不要。確認次第や、見積もり次第に対する説明だけ残して

同じスレッドで、1.2 の冒頭に残っていた同じ列挙にも指摘があった。

> 消す

before:

> 判定は 推奨 / 次点 / 確認次第 / 見積り次第 / 候補外 / 最後の手段 / 脱落 の 7 値。推奨と次点は採用を勧める 1 位と 2 位、確認次第は残る未判定が通れば順位に入るもの、見積り次第は定価が当社規模・当社構成に適用されず見積りを取るまで序列に載らないものを指す。

after（`b0272ad3b` と `6f2c3346a`）:

> ※ 判定の「確認次第」は残る未判定が通れば順位に入るもの、「見積り次第」は定価が当社規模・当社構成に適用されず見積りを取るまで序列に載らないもの、「候補外」は要件が「一部」のまま価格でも劣後するもの、「最後の手段」は代替 IdP を採用できないと分かった段階で検討するもの、「脱落」は Shopify の外部 IdP 要件を満たさないもの

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 4e8ace6e / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:30`・`:53`（`0edf54247` 時点）。修正は `b0272ad3b` と `6f2c3346a`

型: 1, 2

## 型 2: 表の説明は地の文ではなく ※ に置く

### 2-1 [efso-document] 同じ観点の説明は 1 行にまとめる

efso-document の PR #2912 の diffo レビュー。判定の説明を 2 段落に分けて地の文で書いていた。

指摘の実文（diffo スレッド 97a3875a、2026-09-09）:

> これも表の説明なので※にするべきだし、判定の話なので上記と同じ行で書いて

before:

> 候補外は要件が「一部」のまま価格でも劣後するもの、最後の手段は代替 IdP を採用できないと分かった段階で検討するもの、脱落は Shopify の外部 IdP 要件を満たさないものを指す。

after（`b0272ad3b`）: 1-3 の ※ の後半に統合し、判定の説明を 1 行にまとめた

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 97a3875a / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:32`（`0edf54247` 時点）。修正は `b0272ad3b`

### 2-2 [efso-document] 表の但し書きも ※ に置く

efso-document の PR #2912 の diffo レビュー。表の値がいつ確定するかの但し書きを地の文で書いていた。

指摘の実文（diffo スレッド 94010374、2026-09-09）:

> これも※

before:

> 順位は現時点の暫定。3 位は Amazon Cognito の未判定の確認、4〜7 位は見積り（Ory セルフホストはライセンスの可否も）が出るまで確定しない。

after（`b0272ad3b`）:

> ※ 順位は現時点の暫定。3 位は Amazon Cognito の未判定の確認、4〜7 位は見積り（Ory (Enterprise License) はライセンスの可否も）が出るまで確定しない<br>

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 94010374 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:34`（`0edf54247` 時点）。修正は `b0272ad3b`

### 2-3 [efso-document] 表に何を載せていないかを、表の下の ※ に置いた

efso-document の PR #2912 の diffo レビュー。表 1 が何を載せていないかの断りを、表の下の ※ に置いていた。

指摘の実文（diffo スレッド d519dc8e、2026-09-09）:

> 表の注釈というより、表の上に書く方がいいかも

before（表 1 の下の ※）:

> ※ 表 1 は 1 つの基盤で全系統を受ける前提で並べているため、Customer と Staff / Vendor を別の基盤に分ける分割構成は行に無い。分割構成も選択肢としては残っており、検討は `[4. 分割構成の検討](#4-分割構成の検討)` にある

after（`4d4aeceea`）: 表の上の地の文へ移した

> 表 1 は 1 つの基盤で全系統を受ける前提で並べている。Customer と Staff / Vendor を別の基盤に分ける分割構成は行に無く、選択肢としては残っている（検討は `[4. 分割構成の検討](#4-分割構成の検討)`）。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド d519dc8e / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:28`（`5c364dc2e` 時点）。修正は `4d4aeceea`

補記: 2-1 と 2-2 は表の値と順位の説明を地の文から ※ へ移した事例で、こちらは逆に ※ から表の上へ移した。表を読んだあとで足りる説明は ※ に、表を読む前に要る前提は表の上に置く。移したあとの文言への指摘は [文レベルの事例集](./sentence-level-review-cases.md) の 16-1 にある。

## 型 3: 説明を消せるように、情報を表のセルへ持たせる

### 3-1 [efso-document] 値の性質はセルの先頭に置く

efso-document の PR #2912 の diffo レビュー。金額の確度を表の外の注記で説明し、セルには括弧書きで添えていた。

指摘の実文（diffo スレッド d84663ea、2026-09-09）:

> 年額のカラムでカッコ書きで推定など書いているが、先頭に推定か外挿か定価から計算かわかるように書いて、そうすればここの説明は不要になる

同じスレッドで、ラベルの見せ方にも続けて指摘があった。

> 先頭に書いたラベルの直後に改行して、ラベルだけの行になるようにして

before（セルと、表の外の注記）:

> 約 1,400 万〜3,300 万円（推定）

> ※ 金額は Keycloak が推定、Ory Network と Zitadel Cloud が定価からの外挿、Cognito と Entra が定価の計算値、FusionAuth が非公開で、確度が揃っていない。各候補の根拠は `[3. 候補の比較と評価](#3-候補の比較と評価)` にある

after（`b0272ad3b` と `75027636c`）: セルを `推定<br>約 1,400 万〜3,300 万円` の 2 行にし、確度を説明していた ※ を削除した。ラベルは 推定 / 定価から計算 / 定価から外挿 / 未試算 / 公開価格なし の 5 通り

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド d84663ea / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:28`（`0edf54247` 時点）。修正は `b0272ad3b` と `75027636c`

型: 1, 3

## 型 4: 参照の置き場と、指す先の見出しの有無

### 4-1 [efso-document] 案内文が章に入る前の導入部にあった

efso-document の PR #2912 の diffo レビュー。順位が変わる条件の在り処を、章に入る前の導入部で案内していた。

指摘の実文（diffo スレッド 60956655、2026-09-09）:

> 1.1 section 直下のテーブルの直前に書くべき。
> 「推奨が覆る条件は」ではなく、「hoge に書いた条件によっては順位や判定が覆る」と書く方が読みやすい

before（h1 直下の導入部）:

> 推奨が覆る条件は `[1.1 推奨候補と理由](#11-推奨候補と理由)` に書いた。未確認事項と採用に向けた次の作業は `[5. 未確認事項と次の作業](#5-未確認事項と次の作業)` にある。

after（`6f2c3346a`）: 1.1 の見出しの直後・表 1 の直前へ移した

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 60956655 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:7`（`75027636c` 時点）。修正は `6f2c3346a`

補記: 同じ指摘の後半（「推奨が覆る条件は」の言い回し）は文レベルなので、この事例集では扱わない。

### 4-2 [efso-document] 参照先に見出しが無い

efso-document の PR #2912 の diffo レビュー。4-1 で移した案内が、見出しの無い段落群を「表の下」と位置で指していた。

指摘の実文（diffo スレッド 60956655、2026-09-09）:

> 見出しをつけて

before:

> 表の下に挙げた条件によっては、順位や判定が覆る。

after（`70d592675`）: 段落群に `#### 1.1.1` の見出しを付け、案内をアンカーリンクにした

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 60956655 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:9`（`6f2c3346a` 時点）。修正は `70d592675`

補記: 見出しの文言そのものへの再指摘は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 13-1 と 4-12 にある。

### 4-3 [efso-document] 参照先を呼び名で指した

efso-document の PR #2912 の diffo レビュー。別の md ファイルを「要件ファイル」という呼び名で本文の 4 箇所から指していた。

指摘の実文（diffo スレッド 2cb0230b、2026-09-09）:

> 要件ファイルという言及の仕方をやめよう。他の markdown なら markdown link にするべきだし、セクションが特定できるならアンカーリンクにするべき

before:

> 食い違いの中身と引用は要件ファイルにある。食い違いは解消されておらず、候補を外す根拠にできない。

after（`cc7486b91`）:

> 食い違いの中身と引用は `[Shopify が外部 IdP に求める要件](./shopify-external-idp-requirements.md)` にある。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 2cb0230b / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:66`・`:84`・`:98`・`:105`（`1f6efdbee` 時点）。修正は `cc7486b91`

補記: 4 箇所のうち、節が特定できるものはアンカーリンクにした（`[Organization SSO (SAML)](./shopify-external-idp-requirements.md#2-organization-sso-saml)`）。残りは段落ごと落ちて言及自体が消えた。

## 型 5: 不要な案内

### 5-1 [efso-document] 冒頭に別の章への案内を置いた

efso-document の PR #2912 の diffo レビュー。1.1 の冒頭に、別の章への案内を並べていた。

指摘の実文（diffo スレッド fb35a3ab、2026-09-09）:

> 未確認事項、、以降の文はここでは不要では？無駄に案内しようとしないでほしい。

before:

> `[1.1.1 場合によって順位・推奨候補は変わりうる](#111-場合によって順位推奨候補は変わりうる)` に、変わる条件を挙げた。未確認事項と採用に向けた次の作業は `[5. 未確認事項と次の作業](#5-未確認事項と次の作業)` にある。

after（`a3d2ab8a2`）:

> `[1.1.1 場合によって順位・推奨候補は変わりうる](#111-場合によって順位推奨候補は変わりうる)` に、変わる条件を挙げた。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド fb35a3ab / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:9`（`a45edec9d` 時点）。修正は `a3d2ab8a2`

補記: 同じスレッドで、案内を落とす判断の背景と、残した 1 文が残っている理由が示された（2026-09-09）。

> そもそも上から順に読めるように章立てをしてるんだから、自分の文書に対する自己参照はほとんどのケースで不要なはず。今回の参照は、表に決定が書いてあるがそれはあくまで確定ではないということを注釈しておきたかったから注釈の意味も込めて同時に参照をしているに過ぎない

### 5-2 [efso-document] 次の見出しがそれなのに、次の節を案内した

efso-document の PR #2912 の diffo レビュー。節の末尾に、直後の節へ何を分けたかを説明する 1 文を置いていた。

指摘の実文（diffo スレッド 16ad0e02、2026-09-09）:

> この１文いる？

before:

> 性能は候補ごとに公開されている材料が揃わないため、材料そのものを `[3.2 性能の材料](#32-性能の材料)` に分けた。

after（`afad991d5`）: 1 文ごと削除。この文の 2 行下が `### 3.2 性能の材料` の見出しで、案内しなくても続けて読める

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 16ad0e02 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:99`（`5b0f1f79a` 時点）。修正は `afad991d5`

## 型 6: 見出し直下の数文の見せ方

### 6-1 [efso-document] 大きい見出しの直下で 2 文を続けて書いた

efso-document の PR #2912（Auth0 の代替 IdP 比較）の diffo レビュー。h1 の直下に 2 文を 1 段落で置いていた。
原文を句点で折り返しただけではレンダリング後に 1 行へ戻るため、表示上は変わらない。

指摘の実文（diffo スレッド fe4b53dd、2026-09-09）:

> 1 文目の末尾で一回改行を入れて

同じスレッドで、原文の折り返しでは足りないことと、その理由が示された。

> 表示上も分けたい。見やすさのため。特に大セクション直下の数文は１文ずつ改行するか箇条書きとかの方が読みやすい。今回は２文なので改行でよい。表示上も改行。

before:

> Auth0 を使わない決断のもとで、実際に採用する代替 IdP を選ぶための比較。候補ごとに Shopify の外部 IdP 要件の充足と MAU 360 万での年額を出し、順位を付けた。

after（`15eb227b8`）:

> Auth0 を使わない決断のもとで、実際に採用する代替 IdP を選ぶための比較。`<br>`<br>
> 候補ごとに Shopify の外部 IdP 要件の充足と MAU 360 万での年額を出し、順位を付けた。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド fe4b53dd / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:3`（`d32538d14` 時点）。修正は `a6d56047c`（原文の折り返し）と `15eb227b8`（表示上の改行）

### 6-2 [efso-document] 主題が続く 2 文のあいだで改行した

efso-document の PR #2912 の diffo レビュー。章の冒頭の 3 文を、すべて `<br>` で区切って並べていた。
1 文目と 2 文目は同じ主題（Auth0 を使わない決断と、この文書の扱い）で、改行すると別の話に見える。

指摘の実文（diffo スレッド 6d80b63f、2026-09-09）:

> この２文は同じ主題の流れなので改行しないで

before:

> Auth0 は使わない決断が下っている。`<br>`<br>
> このドキュメントは代替を選ぶためのもので、Auth0 を使わないと決めた理由は扱わない。`<br>`<br>
> 候補ごとの仕様と料金は、各社の公式ドキュメントと価格表で調べた。調査時点は 2026 年 9 月。

after（`dfa4208b9`）:

> Auth0 は使わない決断が下っている。このドキュメントは代替を選ぶためのもので、Auth0 を使わないと決めた理由は扱わない。`<br>`<br>
> 候補ごとの仕様と料金は、各社の公式ドキュメントと価格表で調べた。調査時点は 2026 年 9 月。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 6d80b63f / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:47-49`（`320f87126` 時点）。修正は `dfa4208b9`

補記: 6-1 は逆に、大きい見出しの直下の 2 文を表示上も分けるよう指摘された事例。行の切れ目を主題の切れ目に合わせる点は同じ。

### 6-3 [efso-document] 主題が同じ 1 文目と 3 文目のあいだに、別の主題の文を挟んだ

efso-document の PR #2912 の diffo レビュー。h1 直下の 3 文で、1 文目と 3 文目がこの比較の範囲を述べており、そのあいだに何を出したかの文が入っていた。

指摘の実文（diffo スレッド d2f7603f、2026-09-09）:

> こっちを最初の文に改行せずに繋げて２文で１行にして

before:

> Auth0 を使わない決断のもとで、実際に採用する代替 IdP を選ぶための比較。`<br>`<br>
> 候補ごとに Shopify の外部 IdP 要件への対応と MAU 360 万での年額を出し、順位を付けた。`<br>`<br>
> Auth0 を使わないと決めた理由は扱わない。

after（`4d4aeceea`）:

> Auth0 を使わない決断のもとで、実際に採用する代替 IdP を選ぶための比較。Auth0 を使わないと決めた理由は扱わない。`<br>`<br>
> 候補ごとに Shopify の外部 IdP 要件への対応と MAU 360 万での年額を出し、順位を付けた。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド d2f7603f / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:3-5`（`5c364dc2e` 時点）。修正は `4d4aeceea`

## 型 7: 作成過程の議論を読者向けの記述に混ぜる

### 7-1 [efso-document] 要件の由来を「前提」に書いた

efso-document の PR #2912（Auth0 の代替 IdP 比較）の diffo レビュー。前提の箇条書き。
評価項目をどう引いたかは書き手が決めた過程で、読み手が先に知る必要は無い。項目を扱う節で触れれば足りる。

指摘の実文（diffo スレッド ef7e7137、2026-09-09）:

> これはこのドキュメントを作るにあたって議論した経緯であり、このドキュメントを読む人に必要な前提ではない。要件について扱っている比較などのセクションで根本の要求から要件を整理しているということが補足されていれば十分

before:

> - 要件の由来は、現行設計が IdP に求めている根本の業務要求（何が満たされないとどの経路・業務が成立しないか）にある。Auth0 で使っていた機能名は、その要求を満たす手段の 1 つとして扱い、機能名の一致で判定しない

after（`1456b3bf4`）: 行ごと削除。評価項目の節の冒頭に「項目は、2 つの場所で IdP に求めている根本の業務要求から引いた」があり、そちらで足りる

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド ef7e7137 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:43`（`812c61382` 時点）。修正は `1456b3bf4`

### 7-2 [efso-document] 探したが見つからなかったことを章の冒頭に書いた

efso-document の PR #2912 の diffo レビュー。3 章の冒頭に、どの候補にも事例報告が無いことと、どこを探したかを 2 段落で置いていた。

指摘の実文（diffo スレッド 0b9b3ce8、2026-09-09）:

> この情報をこのセクションの冒頭に書く必要ある？全く書く必要ないと思う

before:

> Shopify の新カスタマーアカウントに外部 IdP として繋いだ成功・失敗の報告は、どの候補にも無い（2026-09-03 時点。community.shopify.dev と community.shopify.com、GitHub、Hacker News、Web 検索で候補名と Shopify の組み合わせを探した）。

> あるのは自前の OIDC サーバーを繋いだ報告と、Keycloak を繋ぐ最中の質問 1 件だけで、それらは該当する候補のドキュメントに載せた。

after（`cc7486b91`）: 2 段落とも削除。章の見出しの直後に 3.1 が続く形になった

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 0b9b3ce8 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:138`・`:140`（`1f6efdbee` 時点）。修正は `cc7486b91`

## 型 8: 章の冒頭の前提が増えたら、最初のサブセクションへ出す

### 8-1 [efso-document] 前提を箇条書きの入れ子で章の冒頭に積んだ

efso-document の PR #2912 の diffo レビュー。2 章の冒頭に、スコープ・規模と金額・調査時点の 3 群を箇条書きの入れ子で置いていた。

指摘の実文（diffo スレッド 40e90e4a、2026-09-09）:

> 各社の金額ってこれ SaaS のみだよね。「SaaS の候補の金額は」の方が確実では？
> あとはセルフホストの推定方法に関してもここに書いておくべきでは？
> ここの規模と金額の前提の内容が増えてくるならそもそも 2.1 として最初のサブセクションとして立ててしまった方が読みやすい。
>
> そうすれば section2 冒頭は 2,3 文になり、箇条書きすら必要なくなる。１文ずつ改行して書くだけで読みやすくなる。

before:

> ## 2. 前提
>
> - このドキュメントのスコープ
>     - Auth0 は使わない決断が下った。このドキュメントは代替を選ぶためのもので、Auth0 を使わないと決めた理由は扱わない
> - 規模と金額の前提
>     - MAU は `[Auth0 商談 議事録（2026-07-22 料金・Custom Token Exchange）](../../../../open-issues/dev/to-be/cross-domain/auth0/hearings/2026-07-pricing-cte/minutes.md)` 記載の 360 万（現行想定）、1,000 万（将来想定）を主シナリオとし、100 万を下限の参考として併記する
>     - 各社の金額は公開価格表からの計算値（定価）。この規模はどの候補も個別見積りの対象で、ディスカウント後の実額は未取得
>     - 為替は換算用に ¥155/$ を仮置きする。¥170/$ に振れるとドル建て候補の円換算値は約 10% 上振れする
>     - 年額は月額 × 12 の単純換算
> - 調査時点
>     - 2026 年 9 月

after（`cc7486b91`）: 規模と金額の群を `### 2.1 規模と金額の前提` として独立させ、章の冒頭は箇条書きをやめて 3 文にし、1 文ずつ改行した

> ## 2. 前提
>
> Auth0 は使わない決断が下っている。`<br>`<br>
> このドキュメントは代替を選ぶためのもので、Auth0 を使わないと決めた理由は扱わない。`<br>`<br>
> 調査時点は 2026 年 9 月。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 40e90e4a / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:45-55`（`1f6efdbee` 時点）。修正は `cc7486b91`

型: 6, 8

補記: 冒頭の「各社の金額」が指す範囲は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 5-13 にある。セルフホストの推定方法を足したのは、この文書に何を書くかの判断で、事例としては扱わない。

## 型 9: 参照先を読まないと意味が取れない細部を書く

### 9-1 [efso-document] 内訳に要件番号と別文書の分類を持ち込んだ

efso-document の PR #2912 の diffo レビュー。要件の件数の内訳に、参照先で採番した番号と、参照先の分類の内訳を添えていた。

指摘の実文（diffo スレッド 0a6ceb0b、2026-09-09）:

> 以下の書き方にして。細かい情報や番号は不要。詳細の要件一覧ドキュメントを見ないと意味がわからない。このドキュメントはそこを読まなくてもわかるようにするべき。
>
> - 顧客ログインに関わる Customer Accounts の外部 IdP 接続 (OIDC) が 28 件
> - スタッフログインに関わる Organization SSO（スタッフ向け SAML）が 15 件

before:

> 43 件の内訳は 2 つ。
>
> - Customer Accounts の外部 IdP 接続 (OIDC) が 28 件（CA-01〜CA-28）。Shopify 外部 IdP 要件 20 件、Shopify 依存 8 件
> - Organization SSO（スタッフ向け SAML）が 15 件（SSO-01〜SSO-15）。Shopify 外部 IdP 要件 10 件、Shopify 依存 5 件

after（`cc7486b91`）:

> 必須要件として使うのは、顧客ログインに関わる `[Customer Accounts の外部 IdP 接続 (OIDC)](./shopify-external-idp-requirements.md#1-customer-accounts-の外部-idp-接続-oidc)` の 28 件。スタッフログインに関わる `[Organization SSO (SAML)](./shopify-external-idp-requirements.md#2-organization-sso-saml)` の 15 件は、優劣項目「SAML 2.0 IdP」の判定条件に使う。28 件のうち判定の対象から除くものと条件付きで判定するものも、要件一覧の側に書いた。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 0a6ceb0b / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:61-64`（`1f6efdbee` 時点）。修正は `cc7486b91`

補記: この after は地の文に畳んだため、同じスレッドで箇条書きに戻す指摘を受けた。そちらは 10-1 にある。

### 9-2 [efso-document] 別文書の分類をこの文書に持ち出した

efso-document の PR #2912 の diffo レビュー。参照先が要件を 2 分類していることを、この文書でも説明していた。

指摘の実文（diffo スレッド 7d708587、2026-09-09）:

> 分類は要件ファイルと同じ
> これは意味がわからない。分類ってなんのこと？要件ファイルってどれ？どっちもこのドキュメントで初出のくせに説明がない。そもそもその分類をこっちのドキュメントに持ち出す必要性がなさそう。必要あるの？こっちでは「Shopify の外部 IdP 接続の要件」でひとまとめにしてよくない？

before:

> 分類は要件ファイルと同じ。Shopify 外部 IdP 要件は、help.shopify.com または shopify.dev が外部 IdP 接続の要件・設定手順として明記しているもの。

> Shopify 依存の要件は、公式の要件ページには無いが、Shopify の仕様や制約（staff の回答、provider guide の注記、ベンダーガイド、API リファレンス）から外部 IdP の側が満たさざるを得ないもの。

after（`cc7486b91`）: 2 段落とも削除。分類は参照先にだけ置き、この文書では 43 件をひとまとめに扱う形にした

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 7d708587 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:66`・`:68`（`1f6efdbee` 時点）。修正は `cc7486b91`

補記: 「分類」「要件ファイル」が初出で説明の無い語であることへの指摘は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 5-14 にある。呼び名で参照したことへの別スレッドの指摘は 4-3。

### 9-3 [efso-document] 判定から除く要件の内訳を番号付きで書いた

efso-document の PR #2912 の diffo レビュー。判定の対象から除く要件と条件付きで判定する要件を、参照先の番号で名指しして並べていた。

指摘の実文（diffo スレッド f7749d14、2026-09-09）:

> 足切りに使うのは→必須要件として Auth0 代替候補の精査に使うのは
> 細かい対象から除く内容もこっちのドキュメントで書く必要がない。要件一覧のドキュメント側に書くべき。要件に割り振った番号をそもそもこっちのドキュメントに露出させたくない。各候補に対してこの要件を満たしていないというような言及をする時のみ

before:

> 足切りに使うのは Customer Accounts の 28 件。28 件のうち、CA-01（Shopify Plus プラン限定）はストア側の条件、CA-14（Entra ID を IdP にする場合の provider type）は特定製品向けの設定なので、判定の対象から除く。

> CA-18（Shop Pay のボタン）はストアが Shop Pay を提供する場合の条件、CA-20 (claim import) は任意機能なので、条件付きで判定する。Organization SSO の 15 件は、比較項目「SAML 2.0 IdP」の判定条件に使う。

after（`cc7486b91`）: 除外と条件付き判定の内訳を要件一覧（`to-be/idp/docs/investigation/shopify-external-idp-requirements.md`）へ移し、この文書には所在だけを残した

> 28 件のうち判定の対象から除くものと条件付きで判定するものも、要件一覧の側に書いた。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド f7749d14 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:70`・`:72`（`1f6efdbee` 時点）。修正は `cc7486b91`

### 9-4 [efso-document] 要件の採番と母集団の引き算が、別の箇所に戻っていた

efso-document の PR #2912 の diffo レビュー。一度落とした要件番号（`CA-nn`）が、別の指摘への対応の中で他の箇所へ戻っていた。
番号の定義はこの文書に無く、要件 1 件ごとの判定も別のドキュメントが持つ。

指摘の実文（diffo スレッド 3ca266df、2026-09-09）:

> 謎の採番がこっちのドキュメントにはみ出てる。これはなんの番号？書く必要があるなら辿れるようにするべきだし。書く必要がないなら消せ

同じ回に別スレッド（27365ae1）で出た、同じ性質の指摘:

> それを書いても全く意味が取れない上に、こっちのドキュメントに出すべきでない情報が出てきてる。最悪

before:

> - Clerk: 開発者向けのマネージド認証 SaaS。OAuth application に Logout URL の欄が無く (CA-16)、authorize エンドポイントが `login_hint` を受けない (CA-19)。判定の根拠は `[Clerk の評価](./auth0-alternative-clerk.md)`

> | Amazon Cognito の未判定 5 件 | Shopify が `/logout` に送るパラメータ (CA-06)、token / discovery / userinfo の応答時間 (CA-09 / CA-21)、Sign in with Shop のボタン配置 (CA-18)、ログイン画面の言語 (CA-24)。実機で確かめる |

after（`0aef19a92`）:

> - Clerk: 開発者向けのマネージド認証 SaaS。OAuth application に Logout URL の欄が無く、authorize エンドポイントが `login_hint` を受けない。判定の根拠は `[Clerk の評価](./auth0-alternative-clerk.md)`

> | Amazon Cognito の Shopify 要件の未判定 | Shopify が `/logout` に送るパラメータ、token / discovery / userinfo の応答時間、Sign in with Shop のボタン配置、ログイン画面の言語。実機で確かめる。要件ごとの判定は `[Amazon Cognito (user pools) の評価](./auth0-alternative-cognito.md)` にある |

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 3ca266df / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:117-118`、スレッド 27365ae1 / 同 `:73`（どちらも `a7ea955aa` 時点）。修正は `0aef19a92`

補記: 文書全体の `CA-` 6 箇所を 1 件ずつ判定した。消したのは 4 箇所で、いずれも欠陥や論点の中身が文に書いてあり番号が無くても成立する。残したのは 2 箇所で、番号を定義しているドキュメントへのリンクと同じ文にある箇所（アンカーを節に付けて「の行」と明示）と、参照一覧でそのドキュメントが `CA-01` から `CA-28` を持つと説明している行。番号を最初に落とした判断は 9-3 にある。要件の件数（28 件・26 件・5 件・0 件）も同じ回に落とした。件数は要件 1 件ごとの判定を数え上げた結果で、内訳がこの文書に無い以上、数だけ書いても読み手が確かめられないため。

## 型 10: 並列する項目を地の文に流す

### 10-1 [efso-document] 2 つの内訳を地の文で並べた

efso-document の PR #2912 の diffo レビュー。9-1 の修正で内訳を地の文に畳んだところ、同じスレッドで箇条書きに戻すよう指摘を受けた。

指摘の実文（diffo スレッド 0a6ceb0b、2026-09-09。9-1 と同じスレッドの追加の指摘）:

> 箇条書きにしろ

before（`320f87126` 時点。9-1 の after）:

> 必須要件として使うのは、顧客ログインに関わる `[Customer Accounts の外部 IdP 接続 (OIDC)](./shopify-external-idp-requirements.md#1-customer-accounts-の外部-idp-接続-oidc)` の 28 件。スタッフログインに関わる `[Organization SSO (SAML)](./shopify-external-idp-requirements.md#2-organization-sso-saml)` の 15 件は、推奨要件「SAML 2.0 IdP」の判定条件に使う。28 件のうち判定の対象から除くものと条件付きで判定するものも、要件一覧の側に書いた。

after（`9422782a4`）:

> 43 件は 2 つに分かれる。
>
> - 顧客ログインに関わる `[Customer Accounts の外部 IdP 接続 (OIDC)](./shopify-external-idp-requirements.md#1-customer-accounts-の外部-idp-接続-oidc)` が 28 件
> - スタッフログインに関わる `[Organization SSO (SAML)](./shopify-external-idp-requirements.md#2-organization-sso-saml)` が 15 件
>
> 必須要件として使うのは 28 件のほう。15 件は推奨要件「SAML 2.0 IdP」の判定条件に使う。28 件のうち判定の対象から除くものと条件付きで判定するものも、要件一覧の側に書いた。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 0a6ceb0b / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:73`（`320f87126` 時点）。修正は `9422782a4`

### 10-2 [efso-document] 提供形態ごとの説明を、段落と箇条書きに分けて流した

efso-document の PR #2912 の diffo レビュー。金額の作り方を提供形態ごとに述べていたが、SaaS は段落だけ、セルフホストは段落 + 箇条書きという別々の形になっていた。

指摘の実文（diffo スレッド 2ea3f4d8、2026-09-09）:

> 形態ごとに構造化して読みやすくしてほしい

before（2 段落。2 段落目の下にライセンス費・インフラ稼働費・運用工数の 3 項目の箇条書きが続く）:

> 金額の作り方は提供形態で分かれる。SaaS の候補の金額は公開価格表からの計算値（定価）。この規模はどの候補も個別見積りの対象で、実額は未取得。定価より下がるとは限らず、Enterprise を MAU 非連動の最低契約額で出す候補もある。当社規模の定価が公開されていない Zitadel Cloud と Ory Network だけは、下位プランの定価を当社規模まで当てた外挿値を載せている。

> セルフホストと自前実装の候補には公開価格表が無いので、ライセンス費・インフラ稼働費・運用工数の 3 つを積んだ推定値を載せている。初期構築と移行の工数は一度きりなので年額に入れない。

after（`f723a9545`。3 項目の箇条書きは 2 つ目の項目の入れ子へ下げた）:

> 金額の作り方は提供形態で分かれる。
>
> - SaaS の候補は、公開価格表からの計算値（定価）
>     - この規模はどの候補も個別見積りの対象で、実額は未取得。定価より下がるとは限らず、Enterprise を MAU 非連動の最低契約額で出す候補もある
>     - 当社規模の定価が公開されていない Zitadel Cloud と Ory Network は、下位プランの定価を当社規模まで当てた外挿値を載せている
> - セルフホストと自前実装の候補は、ライセンス費・インフラ稼働費・運用工数の 3 つを積んだ推定値。公開価格表が無いため。初期構築と移行の工数は一度きりなので年額に入れない

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 2ea3f4d8 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:58`・`:60`（`6c7849d56` 時点）。修正は `f723a9545`

### 10-3 [efso-document] どの 2 つかを地の文に流した

efso-document の PR #2912 の diffo レビュー。要件をどこから引いたかを 1 文にまとめたところ、2 つの由来が読み分けられなくなった。

指摘の実文（diffo スレッド a3d752a5、2026-09-09）:

> いや、読みづらくて読み間違えた。２つがどの２つなのかが分かりづらかった。以下の二つって書いて箇条書きにして。あと項目って用語が残ってる

before:

> 要件は Shopify 接続のほかに、IdP が発行する User JWT を BFF が Core へ転送する 4 経路（EC Front・Native App・Realm 管理アプリ・Vendor 管理アプリ）の業務要求からも引いた。

after（`38cb3c047`）:

> 要件は以下の 2 つから引いた。
>
> - Shopify 接続。顧客が Shopify Customer Accounts から IdP にログインする部分と、スタッフが Shopify Admin に SAML SSO でログインする部分
> - IdP が発行する User JWT を BFF が Core へ転送する 4 経路。EC Front・Native App・Realm 管理アプリ・Vendor 管理アプリ

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド a3d752a5 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:87`（`628ac4321` 時点）。修正は `38cb3c047`

補記: この箇条書きは同じスレッドの次の指摘で前置きごと削除された。16-2 にある。「項目って用語が残ってる」は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 5-19 にある。

### 10-4 [efso-document] 2 つの想定を 1 項目に詰め、関連する項目から離して置いた

efso-document の PR #2912 の diffo レビュー。前提の箇条書きで、req/s の仮置きを末尾に置き、MAU 2 通りの数字を 1 項目の中に並べていた。

指摘の実文（diffo スレッド c683dd82、2026-09-09）:

> MAU の箇条書きの下においた方が読みやすい
> と置く、ではなく「とする。」
> そのあとは箇条書きをネストして、それぞれの想定ごとに箇条書きを分けて

before（MAU の項目は箇条書きの先頭、この項目は末尾）:

> - 認証リクエストは MAU × 150/月、ピークは平均の 3 倍と置く。MAU 360 万で平均 約 210 req/s・ピーク 約 630 req/s、MAU 1,000 万で平均 約 580 req/s・ピーク 約 1,700 req/s（`[Auth0 を自前実装で置き換える場合のコスト概算](./auth0-selfbuild-cost.md#3-インフラ費)` のワークロード前提）

after（`73165d966`。MAU の項目の直下へ移した）:

> - 認証リクエストは MAU × 150/月、ピークは平均の 3 倍とする（`[Auth0 を自前実装で置き換える場合のコスト概算](./auth0-selfbuild-cost.md#3-インフラ費)` のワークロード前提）
>     - MAU 360 万: 平均 約 210 req/s、ピーク 約 630 req/s
>     - MAU 1,000 万: 平均 約 580 req/s、ピーク 約 1,700 req/s

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド c683dd82 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:55`（`7677a3373` 時点）。修正は `73165d966`

補記: 同じ指摘に含まれる「と置く」→「とする」は文レベルの語の言い換えで、1 語のため文レベルの事例集には立てていない。

### 10-5 [efso-document] 分割構成の節が地の文の段落だけで並んでいた

efso-document の PR #2912 の diffo レビュー。分割構成を検討する節。
「利点は 2 つ」と数を宣言しながら、その 2 つを段落で続けて書いていた。

指摘の実文（diffo スレッド c3c315f9、2026-09-09）:

> このセクションの中身が全部地の文と段落のみでのっぺりして読みづらい。
> ２つあるとか上げたときは箇条書きにした方がよい。
> セクションの最初の文と、利点は２つ考えられると書き出す部分は改行して見た目を分けた方がよい

before:

> 系統ごとに IdP を分ける構成を検討した。想定する分割は、Customer（Shopify 接続、MAU の大半）と Staff / Vendor の 2 系統（MAU の小さい側）を別の基盤にする形。分割に期待できる利点は 2 つ考えられるが、いずれも想定規模では効果が薄い。
>
> 利点 1 は MAU 課金の圧縮。Customer 側をセルフホスト（Keycloak・Zitadel）に置き、Staff / Vendor 側だけを MAU 課金の SaaS にする形なら、SaaS の課金は小さい系統の分だけになる。
>
> ただし Staff / Vendor 側は Organizations と RBAC を使う設計で、SaaS 側にもその機能が要る。Ory Network は Organizations の要件から Enterprise 契約が前提で、小さい系統のためだけに Enterprise を契約する意味は薄い。
>
> 利点 2 は系統ごとに最適な基盤を選べる運用の柔軟性。ただし認証基盤が 2 系統になることで、セッション設計・ID マッピング・運用監視・障害対応が二重になる。柔軟性より二重化のコストが上回る。

after（`e95169664`）:

> 分割に期待できる利点は 2 つある。どちらも想定規模では効果が薄い。
>
> - MAU 課金の圧縮: Customer 側をセルフホスト（Keycloak・Zitadel）に置き、Staff / Vendor 側だけを MAU 課金の SaaS にすれば、SaaS の課金は小さい系統の分だけになる。ただし Staff / Vendor 側は Organizations と RBAC を使う設計で、SaaS 側にもその機能が要る。Ory Network は Organizations の要件から Enterprise 契約が前提で、小さい系統のためだけに Enterprise を契約する意味は薄い
> - 系統ごとに最適な基盤を選べる運用の柔軟性: 認証基盤が 2 系統になると、セッション設計・ID マッピング・運用監視・障害対応が二重になる。柔軟性より二重化のコストが上回る

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド c3c315f9 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:294`（`d342eca18` 時点）。修正は `e95169664`

補記: 同じ節の後半（Customer 側を SaaS に置く形とセルフホストへ寄せる形）も、地の文 2 段落から箇条書きに変えた。書き出しの 1 文と「利点は 2 つある」の行は段落として分けた。この節の書き出しの名詞句は別スレッドで指摘され、[norm-refit の PR レビューの文レベル指摘 事例集](./sentence-level-review-cases.md) の 3-4 にある。

## 型 11: 節の冒頭に、これから何を説明するかを書かない

### 11-1 [efso-document] 節の冒頭でいきなり内訳から書き出した

efso-document の PR #2912 の diffo レビュー。節の冒頭に前置きを置かず、重要度が 3 段階であることから書き始めていた。

指摘の実文（diffo スレッド 6efc1c10、2026-09-09）:

> 2.1 もだったけど、地の文→箇条書き→地の文→箇条書きだけでかなりの文量あると読むのがしんどい
> そもそも、セクションのはじめ、サブセクションのはじめにはこれから何を説明するよっていうのを軽く説明すべき。セクションタイトルと同じ情報を書くだけでは意味はないよ。要約を長々と書くのも意味がないよ。
> 例えば少なくともこのセクションには以下のような序文が必要。
> section 2 で挙げた要件それぞれに重要度を割り当て、各 Auth0 代替候補がそれらの要件のどれを満たしどれを満たさないのかによって各候補を評価する。
> 各重要度については以下の通り。
>
> いきなり「項目の重みは 3 段階」とか言われても本当に意味がわからない。これは日本語執筆規範の主題の欠如に近い。文レベルではなくセクションなどの構造レベルでこれをやってる。項目とかも汎用語だし。

before:

> ### 2.2 評価項目
>
> 項目の重みは 3 段階。足切り（満たさなければ候補から外す）、比較（候補間の差になる）、参考（序列に効かないが記録する）。

after（`cc7486b91` と `7651a740c`）: 節の冒頭に、何を調べてどこにまとめ、そこから何を抜き出して評価したかの序文を置いてから、重要度の 3 段階を並べる形にした

> ### 2.2 要件と評価のしかた
>
> Shopify が外部 IdP に求める要件を調べ、全 43 件を `[Shopify が外部 IdP に求める要件](./shopify-external-idp-requirements.md)` にまとめた。要件の中身と出典（Shopify の公式ページと staff 回答）はその文書が持つ。候補の評価は、そのうち必須要件を抜き出し、候補がそれを満たすかどうかで行った。

> 評価項目の重要度は 3 段階になる。
>
> - 必須要件: 満たさなければ候補から外す
> - 推奨要件: 候補の順位に効く
> - 任意要件: 順位に効かないが記録する

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 6efc1c10 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:88-90`（`1f6efdbee` 時点）。修正は `cc7486b91` と `7651a740c`

補記: 末尾の「項目とかも汎用語だし」は文レベルで、同じ語への別スレッドの指摘が [文レベルの事例集](./sentence-level-review-cases.md) の 14-1 にある。

### 11-2 [efso-document] 定義から離れた節で、ラベルだけを出した

efso-document の PR #2912 の diffo レビュー。重要度のラベルを定義したのは 2 章で、それを使う 3.3 の冒頭にはラベル名しか出していなかった。

指摘の実文（diffo スレッド a76d5ae9、2026-09-09）:

> ここ以降での言及の仕方はどうする？結局「推奨要件」とだけ言われてもパッとこの定義を思い浮かべられない

同じスレッドの追加の指摘:

> 定義までは「要件」という一般的な用語で丸めてない？それでいいと思う。それよりも 3.3 が読みづらい。ただ 3.3 は 3.3 の中で説明しなおせばいいかなと思った

before:

> 推奨要件 7 つの中身と、推奨要件に置いた根拠は `[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md#3-推奨要件)` にある。ここに書くのは、候補の順位に効く 6 つ（SAML 2.0 IdP、ユーザー管理 API、トークンのクレーム拡張、運用形態、Access Token の audience 指定、攻撃検知）と、OSS のライセンスの違い。候補ごとに、満たさない要件と、条件が付く要件を並べる。Social (Google / Apple / LINE) は、ビルトインで持つか汎用 OIDC で繋ぐかが候補ごとに違うだけで順位に効かないため書かない。

after（`19378d4b3`）:

> 推奨要件は、満たさなくても候補から外さないが、満たさない候補は別の手段で埋めるか制約を受け入れることになる要件。7 つあり、中身とその重要度に置いた根拠は `[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md#3-推奨要件)` にある。ここに書くのは、埋め合わせの負担が候補ごとに違う 6 つ（SAML 2.0 IdP、ユーザー管理 API、トークンのクレーム拡張、運用形態、Access Token の audience 指定、攻撃検知）と、OSS のライセンスの違い。候補ごとに、満たさない要件と、条件が付く要件を並べる。Social (Google / Apple / LINE) は、ビルトインで持つか汎用 OIDC で繋ぐかが候補ごとに違うだけで順位に効かないため書かない。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド a76d5ae9 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:208`（`f928c48ce` 時点）。修正は `19378d4b3`

補記: ラベルの定義そのものを書き直した同じスレッドの前半は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 5-23 にある。

### 11-3 [efso-document] 節の冒頭を、製品側の制約から書き出した

efso-document の PR #2912 の diffo レビュー。現行設計の所属先と Customer の表し方を扱う節。
冒頭に節の範囲を書かず、代替候補 2 つの制約から書き出していた。

指摘の実文（diffo スレッド 7ee1de36、2026-09-10）:

> 途中まで地の文で途中から箇条書き。地の文のところの説明の内容や順序が悪くて頭に入ってこない。いきなり zitadel と ory の話をするのは意味不明

before（`#### 3.1.2 EFSO Tenant と Realm 内共通 Customer の表現` の冒頭段落）:

> Zitadel と Ory の「1 ユーザーは 1 Organization」という制約で差が出るのは Customer の側。現行設計の admin は所属先を 1 つしか持たないので、この制約には当たらない。

after（`75ff23e7c`）: 冒頭を範囲の宣言に置き換え、製品側の制約は候補別の箇条書きの直前へ移した

> この節で見るのは、現行設計の所属先と Customer の表し方を代替候補にそのまま移せるかどうか。

移した先（箇条書きの直前）:

> 代替候補のうち Zitadel と Ory には、1 ユーザーが 1 つの Organization にしか属せない制約がある。admin はどの種別も所属先が 1 つなので、この制約には当たらない。判定が分かれるのは Customer の側で、代替候補ごとの状況は次のとおり。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 7ee1de36 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:231`（`dda592e3d` 時点）。修正は `75ff23e7c`

補記: 冒頭に置いた範囲の宣言は、節の末尾にあった「ここで見たのは、現行設計の所属と Customer の表し方をそのまま移せるかどうかだけ。」を先頭へ移して書き直したもの。末尾には、別途精査すると書いた 1 文だけが残った。

### 11-4 [efso-document] 節の範囲の宣言が、参照の案内の後ろにあった

efso-document の PR #2912 の diffo レビュー。代替候補ごとの評価を並べる節の冒頭。
参照先の案内を先に書き、この節に何を書くかの宣言をその後ろに置いていた。
案内は候補ごとのドキュメントだけを指し、同じ文書の 3 章にも各候補の評価があることに触れていなかった。

指摘の実文（diffo スレッド cc1e71f4、2026-09-10）:

> 実際にはこのドキュメントの section3 でも各候補の詳細はある程度書かれている。先にそっちを案内しつつ。完全な詳細は別ドキュメントというような案内にしたい。
> あと、「詳細（.../.../.../）」のカッコ書きは不要。長すぎる。
> 先に、ここは hoge だけを書く。って宣言してから、section3 に各候補に対する評価のもう少し詳しい要約、他のドキュメントに完全な詳細っていう案内にして。

before:

> 代替候補ごとの詳細（概要 / 要件充足 / 良い点 / ダメな点 / 料金 / 未確認 / 実績・参考）は別のドキュメントに分けた。ここには判定・年額・理由だけを置く。

after（`45540c3a5`）:

> ここに置くのは判定・年額・理由だけ。各代替候補のもう少し詳しい評価は `[3. Auth0 代替候補の比較](#3-auth0-代替候補の比較)` にあり、完全な詳細は代替候補ごとのドキュメントにある。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド cc1e71f4 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:49`（`9d972ce88` 時点）。修正は `45540c3a5`

型: 11, 16, 18

補記: 括弧書きに並べた 7 つは候補ごとのドキュメントが持つ節の名前で、参照先の側にある（型 16）。案内を 2 段にしたのは、同じ候補を 1.2・3 章・候補ごとのドキュメントの 3 箇所で扱いながら、どこが何を持つかを案内していなかったため（型 18）。この after の「置くのは」にはさらに指摘があり、[文レベルの事例集](./sentence-level-review-cases.md) の 5-30 にある。

## 型 12: 扱いを変えない区別を立てる

### 12-1 [efso-document] 必須の中から「特に効く」5 件を抜き出して並べた

efso-document の PR #2912 の diffo レビュー。必須の要件のうち特に効くものを 5 件挙げていたが、その 5 件と残りとで以後の扱いは変わらない。

指摘の実文（diffo スレッド b93344aa、2026-09-09）:

> →必須要件の中でも特に重要な要件は
>
> で、特に重要なものをあげてるけど、特に重要なものとそうでない残りの要件とでは、比較時の扱いが異なるの？どれも必須要件なんじゃないの？どう比較したの？

同じスレッドの追加の指摘:

> であればここでは並べる必要すらないのでは？ 2.2 とセクションマージしてしまっていいレベルな気がする。
> shopify の必須要件を調べました、それは別のドキュメントにまとめました。そのうち必須の要件を抜きだして、それぞれの候補がその要件を満たしているかで要件の評価をしました。というような流れの説明だけでいいはず。詳細や要件一覧のドキュメント側に押し込めるべき

before:

> 足切りの条件として特に効く要件は 5 つ。
>
> - OIDC RP-Initiated Logout 1.0 への対応。Back-Channel / Front-Channel は不可 (CA-06)
> - ID トークンの必須クレーム `sub` / `nonce` / `email` / `email_verified` (true) / `iss` / `aud` (CA-11) と、署名アルゴリズム（CA-12。HS256 不可）
> - リフレッシュトークンの発行 (CA-13)
> - token / discovery / userinfo の 1 秒応答 (CA-09) と、外部 IdP へのリクエストのタイムアウト 1,000 ms (CA-21)
> - 初回ログイン時の (provider, sub) への永続的な紐づけ（CA-23。同じ人に同じ sub を出す）

after（`cc7486b91`）: 5 件の列挙ごと削除。旧 2.1 と旧 2.2 を `### 2.2 要件と評価のしかた` に統合し、どこに要件をまとめ、そこから何を抜き出して評価したかの流れだけを残した

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド b93344aa / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:74-80`（`1f6efdbee` 時点）。修正は `cc7486b91`

型: 9, 12

補記: 落ちた 5 件のうち 2 件は、書き方そのものへの指摘も別スレッドで受けている。[文レベルの事例集](./sentence-level-review-cases.md) の 4-14（「初回ログイン時の (provider, sub) への永続的な紐づけ」）と 5-15（「Back-Channel / Front-Channel は不可」）。

### 12-2 [efso-document] 扱いが同じ 2 つの判定値を並べた

efso-document の PR #2912 の diffo レビュー。性能の判定に「未検証」と「未判定」の 2 値を置いていたが、どちらも当社の負荷試験を待つ点で以後の扱いは変わらない。

指摘の実文（diffo スレッド 5ea5658a、2026-09-09）:

> 未検証と未判定がぱっと見同じすぎる。結局参考としても比較できるものではないってことでいいの？要検証に揃えていいのでは？

before（判定列の 2 値と、その違いを説明する ※）:

> ※ 「未検証」は公開値はあるが当社の構成と条件で測っていないもの、「未判定」は公開値そのものが無いもの<br>

after（`d9642a2f5`）: 2 値を「要検証」に統一し、違いを説明する ※ を削除。値の違いで表していたことは「判定できない理由」の列へ移した

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 5ea5658a / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:120`（`c64b24fdc` 時点）。修正は `d9642a2f5`

補記: 同じ表の列そのものへの、同じスレッドの指摘は 19-1 にある。

## 型 13: 全件を並べた表より、結論に効く差分だけを残す

### 13-1 [efso-document] 必須項目と候補の全組み合わせを表にした

efso-document の PR #2912 の diffo レビュー。必須 4 項目 × 候補 9 列の表で、満たす候補も満たさない候補も同じ密度で並べていた。

指摘の実文（diffo スレッド e99a2121、2026-09-09）:

> この表めちゃくちゃ見づらいからなくていいかも。どの候補がどの要件を満たしていなくて順位が下がるのかなどが分かればよい。満たすことは全体だから満たすことの情報も含めて複雑に見える

before（見出しと表の 1 行目。表は 4 行 × 候補 9 列）:

```text
### 3.1 足切り項目の充足

| 足切り項目 | Keycloak | Zitadel | Zitadel Cloud | Ory Network | Ory (Enterprise License) | Amazon Cognito (Essentials) | Entra External ID | FusionAuth | 自前実装（部品を選んで組む） |
|---|---|---|---|---|---|---|---|---|---|
| Shopify 外部 IdP 要件 | 満たす | 満たす | 満たす | 満たす | 満たす (Hydra) | 未判定 5 件（満たさない 0） | 一部 | 満たす 20 / 条件付き 3 / 未判定 3 | 満たす（自前で立てる認可サーバー側の実装が条件） |
```

after（`cc7486b91`）: 表を廃止し、満たさないことが確定して順位に効いた候補と要件だけを箇条書きにした

> ### 3.1 必須要件への対応
>
> 必須 4 項目のうち、満たさないことが確定して順位に効いたものを挙げる。判定が付いていない項目は `[6. 未確認事項と次の作業](#6-未確認事項と次の作業)` に集めた。
>
> - Entra External ID は性能を満たさない。上限がテナントあたり 200 req/s で、当社のピーク想定 約 630 req/s を下回る。定価が最も高いことと合わせて圏外に置いた
> - Amazon Cognito は性能の既定の上限が当社ピークを下回る。引き上げは申請制で、通れば上回る。判定が「確認次第」に留まるのはこのため

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド e99a2121 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:142-152`（`1f6efdbee` 時点）。修正は `cc7486b91`

### 13-2 [efso-document] 判定の導出過程まで表に載せた

efso-document の PR #2912 の diffo レビュー。性能の表を 5 列（候補・公開されている材料・構成/条件・当社ピークとの関係・判定）で組み、根拠をすべてセルに入れていた。

指摘の実文（diffo スレッド 7eb1674f、2026-09-09）:

> このテーブルももっと簡略化したい。テーブルの中身の情報ってどこかのドキュメントに記載済みのものなの？
> 各候補と判定だけ分かればよく、理由や導出方法や詳細はトレーサビリティがあればよい

before（見出し行と 1 行目。表は 9 行 × 5 列）:

```text
| 候補 | 公開されている材料 | 構成・条件 | 当社ピークとの関係 | 判定 |
|---|---|---|---|---|
| Keycloak セルフホスト | 公式の到達実績 1,000 logins/s + 20,000 refresh/s。公式ベンチマークは 3 Pod で 2,000 logins/s + 10,000 refresh/s、12,000 req/s まで線形 | AWS 1 リージョン 3 AZ、6 Pod（各 40 vCPU / 8 GB）、Aurora PostgreSQL。リージョン跨ぎは非推奨。認証エンドポイントのレート制限は本体に無くインフラ依存 | 上回る | 公式実測が当社ピークを上回る |
```

after（`9be3d3922`）: 候補と判定の 2 列にし、根拠は候補ごとのドキュメントへの参照に任せた

```text
| 候補 | 判定 |
|---|---|
| Keycloak セルフホスト | 公式実測が当社ピークを上回る |
```

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 7eb1674f / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:107-117`（`862b95e46` 時点）。修正は `9be3d3922`

補記: 判定だけに削った結果、候補どうしの違いが読めなくなり、別のスレッドで列を戻すことになった。19-1 にある。

### 13-3 [efso-document] 満たす候補も満たさない候補も同じ密度で表に並べた

efso-document の PR #2912 の diffo レビュー。推奨要件 7 つと判断に効く補助 3 つ × 候補 7 列の表に、候補ごとの実装名を全部書いていた。

指摘の実文（diffo スレッド 8fed050c、2026-09-09）:

> この表も長いのでいらない。満たすものは正直 comparison ドキュメントで見る必要がない。特筆するべきものだけピックアップして構造化して記述して。

before（見出し行と 1 行目。表は 10 行 × 8 列）:

```text
| 要件 | Keycloak | Zitadel | Ory | Amazon Cognito | Entra External ID | FusionAuth | Authlete（自前実装の OIDC コア） |
|---|---|---|---|---|---|---|---|
| SAML 2.0 IdP（スタッフの一元管理） | 対応（SAML クライアントの登録） | 対応（SAML アプリケーションの登録。SP-initiated のみ） | 単体では非対応。Ory Polis の SAML Federation（Enterprise ライセンス）で対応できる見込み | 非対応（SAML では SP 側にしかなれない） | 対応（外部テナントの Enterprise applications から non-gallery アプリとして登録） | 条件付き（Persistent 要求では NameID が UUID になり、lambda で email に書き換える） | 非対応（spec sheet に記載なし） |
```

after（`d2381b1b2`）: 表を廃止し、判定が割れる要件とプラン・言語の制約だけを要件ごとの箇条書きにした。落としたのは Social（満たさない候補が無い）と、直後の段落と重なる Organization に属さないユーザー・ランタイムの 2 つ

> - SAML 2.0 IdP（スタッフの一元管理）: Keycloak は SAML クライアントの登録で対応する。非対応は Amazon Cognito（SAML では SP 側にしかなれない）と Authlete（spec sheet に記載なし）。Ory も単体では非対応で、Ory Polis の SAML Federation（Enterprise ライセンス）を足せば対応できる見込み。FusionAuth は Persistent 要求で NameID が UUID になるため、lambda で email に書き換える。Zitadel は SP-initiated のみ。SAML 2.0 IdP の要否はスタッフ認証の設計判断として未決定で（`[6. 未確認事項と次の作業](#6-未確認事項と次の作業)`）、要ると決まれば Amazon Cognito と Ory の順位に効く

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 8fed050c / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:126-137`（`9be3d3922` 時点）。修正は `d2381b1b2`

補記: この削除で他の節に無い情報まで落ちていたことが、同じスレッドで指摘された。17-1 にある。

## 型 14: 根拠に挙げた実績が、何を確かめていないかを書く

### 14-1 [efso-document] 接続実績を挙げるとき、その実績が確かめていない範囲を書かなかった

efso-document の PR #2912 の diffo レビュー。資料どうしが食い違う項目について、既存システムでの接続実績を根拠として足すことになった場面。

指摘の実文（diffo スレッド 343f3358、2026-09-09）:

> あと参考として auth0 の時はどうやって認証していたのかは参考になるのでは？少なくともそれは stub 検証で認証できることが実機検証で確認できてるよね？

同じスレッドの追加の指摘:

> なるほど。であればその実績があることと、実際には Post だったのか Basic だったのかの確認ができているわけではないという補足を加えるといいかもね

before: 該当なし（実績への言及そのものが無かった）

after（`cc7486b91`）:

> 参考として、Auth0 は client_secret_post の設定で Shopify との接続実績がある。ただし Shopify が Basic と Post のどちらで token エンドポイントを叩いたかは確認できていない。食い違いは解消されておらず、未確認事項として `[6. 未確認事項と次の作業](#6-未確認事項と次の作業)` にも載せている。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 343f3358 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:82-86`（`1f6efdbee` 時点）。修正は `cc7486b91`

補記: 前半の「参考になるのでは？」は、この文書に何を書くかの判断で、事例としては扱わない。事例に取るのは後半の、実績を挙げるならその実績が確かめていない範囲も併記する、という部分。

### 14-2 [efso-document] 扱わないと書いた論点の未試算分を、未確認事項に載せなかった

efso-document の PR #2912 の diffo レビュー。分割構成を扱わないと本文に書いたが、判定が定性のままであることは未確認事項の一覧に無かった。

指摘の実文（diffo スレッド 7b1842ee、2026-09-09）:

> 5章にも足して

before: 該当なし（未確認事項の表に行が無かった）

after（`8f26a307f`）:

> | 分割構成の二重化のコスト | 未試算。認証基盤が 2 系統になるとセッション設計・ID マッピング・運用監視・障害対応が二重になるが、その工数と稼働費を出していない。`[4. 分割構成の検討](#4-分割構成の検討)` の判定は定性のままで、Enterprise 見積りが出ても金額で比べられない |

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 7b1842ee / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:328`（`bb6c226bb` 時点）。修正は `8f26a307f`

補記: 同じスレッドの、スコープを述べる文の主語への指摘は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 16-1 にある。

### 14-3 [efso-document] 出典の無い数値を「推定」とだけ書いた

efso-document の PR #2912 の diffo レビュー。金額と性能の両方の前提になるワークロードの 1 行。
値の出どころを問われ、実測でも出典付きでもないことが分かった。

指摘の実文（diffo スレッド b26411e1、2026-09-09）:

> リクエストがこの計算式である根拠はあるの？これは議事録にはないの？

根拠が無いと回答したあとの指示:

> このドキュメントないでも仮置きだとわかるように書いて

before:

> - 認証リクエストは MAU × 150/月、ピークは平均の 3 倍とする（`[Auth0 を自前実装で置き換える場合のコスト概算](./auth0-selfbuild-cost.md#3-インフラ費)` のワークロード前提）

after（`a7ea955aa`）:

> - 認証リクエストは MAU × 150/月、ピークは平均の 3 倍を仮置きする。実測値ではなく出典も無く、`[Auth0 を自前実装で置き換える場合のコスト概算](./auth0-selfbuild-cost.md#3-インフラ費)` が試算のために置いた推定ワークロードをそのまま使っている。下に並べたピークの req/s は `[3.2 性能の判定](#32-性能の判定)` でも EFSO のピーク想定として使うため、実測が出れば性能要件の充足判定も変わる

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド b26411e1 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:137`（`d342eca18` 時点）。修正は `a7ea955aa`

補記: 出どころを探した結果、`auth0-selfbuild-cost.md` が「試算に使った推定ワークロード」として置いた値で、出典も算出過程も書かれていなかった。Auth0 商談の議事録にも無く、リポジトリに実測も無い。同じ commit で `auth0-selfbuild-cost.md` の未確認事項にも「認証リクエスト数」「ピーク倍率」を、既にあった人月単価・為替と同じ形式で足した。同じ文の助詞の欠落は別スレッドで指摘され、[norm-refit の PR レビューの文レベル指摘 事例集](./sentence-level-review-cases.md) の 2-3 にある。

## 型 15: 1 点で足りるところに、当たり前のことや言い換えを足す

### 15-1 [efso-document] 節の冒頭に、続く箇条書きから読めることを書いた

efso-document の PR #2912 の diffo レビュー。前提の節の冒頭に、値を全候補で揃える理由の 1 文を置いていた。
続く箇条書きが MAU・為替・年額の 3 点を並べており、1 文を読まなくても分かる。

指摘の実文（diffo スレッド 5164ad87、2026-09-09）:

> この文の意味は読めるようになったけど、セクション冒頭に書くことではないね。当たり前すぎる。
> この３つを最初に箇条書きで書くだけで良さそう
> MAU は議事録記載のこれ
> 為替はこれを定数として使う
> 年額は月額 * 12 の単純計算
>
> その上でその下にサブセクションを立てれば？
> 各候補の金額の算出方法

before:

> ### 2.1 MAU と金額の算出方法
>
> 候補ごとの年額を比べられるように、MAU と為替レートは全候補で同じ値を使う。
>
> - MAU は `[Auth0 商談 議事録（2026-07-22 料金・Custom Token Exchange）](../../../../open-issues/dev/to-be/cross-domain/auth0/hearings/2026-07-pricing-cte/minutes.md)` 記載の 360 万（現行想定）、1,000 万（将来想定）を主シナリオとし、100 万を下限の参考として併記する
> - 為替は換算用に ¥155/$ を仮置きする。¥170/$ に振れるとドル建て候補の円換算値は約 10% 上振れする
> - 年額は月額 × 12 の単純換算

after（`65a9c50b9`）: 冒頭の 1 文を削除して箇条書きから始め、提供形態ごとの金額の作り方は `#### 各候補の金額の算出方法` のサブセクションへ分けた

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 5164ad87 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:52`（`c79735c39` 時点）。修正は `65a9c50b9`

補記: 削除したこの 1 文と節の見出しは、同じスレッドの前段で語を直したもの。[文レベルの事例集](./sentence-level-review-cases.md) の 5-17 にある。箇条書きの項目から補足を落とした続きは 16-3。

### 15-2 [efso-document] 1 文でよい断りを 4 文で書いた

efso-document の PR #2912 の diffo レビュー。ほとんどの候補が判定できないことを、性能の節の冒頭に書き足す場面。

指摘の実文（diffo スレッド 83e61053、2026-09-09）:

> 構成が違いすぎてほとんど判定不能ならここにその旨を書きたい

同じスレッドの追加の指摘:

> 長すぎる。ほとんど判定不能だったという程度の１文だけを追記してほしかった

before: 該当なし（この断り自体が無かった）

after（`d9642a2f5`。この 4 文に再指摘を受けた）:

> 各社が公開している性能値は、測ったときの構成も測り方も揃っておらず、単位もログイン/秒とリクエスト/秒が混ざる。当社の構成と負荷条件で測った値はどの候補にも無いため、公開値を並べても候補どうしの比較にはならない。下の表で判定できるのは、公開値だけで当社のピーク想定を上回るか下回るかが決まる候補に限られ、残りは当社の負荷試験で測るまで要検証とする。性能での序列は決めない。

after（`4d4aeceea`）:

> 各社が公開している性能値は測ったときの構成も測り方も揃っておらず、ほとんどの候補は当社の負荷試験で測るまで判定できない。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 83e61053 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:103`（`c64b24fdc` 時点）・`:189`（`5c364dc2e` 時点）。修正は `d9642a2f5` と `4d4aeceea`

補記: この 1 文はさらに、判定できない理由の中身に別の指摘を受けて書き直された。[文レベルの事例集](./sentence-level-review-cases.md) の 5-22 にある。

## 型 16: 他の場所が持っている内容を、ここにも書く

### 16-1 [efso-document] 後段で書くことを、前もって 1 行にまとめた

efso-document の PR #2912 の diffo レビュー。43 件の使い分けを述べた 1 行が、後段の必須要件・推奨要件の記述と重なっていた。

指摘の実文（diffo スレッド f6b86096、2026-09-09。前者・後者への書き換えを受けた後の追加の指摘）:

> というかこの１行いらなくない？

before:

> 前者は必須要件「Shopify 外部 IdP 要件」の、後者は推奨要件「SAML 2.0 IdP」の判定条件に使う。前者のうち判定の対象から除くものと条件付きで判定するものは、要件一覧の側に書いた。

after（`38cb3c047`）: 1 行ごと削除

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド f6b86096 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:79`（`628ac4321` 時点）。修正は `38cb3c047`

補記: 削除前の書き換え（件数で受け直す、後で定義する語と衝突する）は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 5-18 にある。

### 16-2 [efso-document] 各項目が持っている前提を、前置きにも書いた

efso-document の PR #2912 の diffo レビュー。要件をどこから引いたかの前置きが、直前の記述と、後続の各要件の行の両方と重なっていた。

指摘の実文（diffo スレッド a3d752a5、2026-09-09）:

> 項目って用語が残ってる。というかここちょっと前で言及した話と全く重複してる

同じスレッドの追加の指摘（前置きを「以下の 2 つ」の箇条書きに直したあと）:

> この話の中でこの２つから引いただけ浮いてる。引いたってどういう意味？この項目いる？

before:

> 項目は、2 つの場所で IdP に求めている根本の業務要求から引いた。1 つは Shopify 接続の前段で、顧客が Shopify Customer Accounts から IdP にログインする部分と、スタッフが Shopify Admin に SAML SSO でログインする部分。

> もう 1 つは、IdP が発行する User JWT を BFF が Core へ転送する 4 経路（EC Front・Native App・Realm 管理アプリ・Vendor 管理アプリ）。

after（`628ac4321`・`38cb3c047`・`a8311ceec`）: 直前と重なる部分を落として 1 文にし、次に箇条書きへ直し、最後に前置きごと削除した。各要件の行が「根本の要求は…」と対象の経路を持っており、前置きとして重ねる必要が無い

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド a3d752a5 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:87`・`:89`（`9b49ac059` 時点）。修正は `628ac4321`・`38cb3c047`・`a8311ceec`

補記: 途中の箇条書き化は 10-3 にある。「項目」という汎用語への指摘は文レベルで、[文レベルの事例集](./sentence-level-review-cases.md) の 5-19 にある。

### 16-3 [efso-document] 前提の値に、別の章が持つ補足を足した

efso-document の PR #2912 の diffo レビュー。為替レートを置く箇条書きの項目に、用途の言い直しと、レートが振れた場合の試算を足していた。

指摘の実文（diffo スレッド 5164ad87、2026-09-09）:

> 換算用にとか、170 に振れるととかの補足はいらん

before:

> - 為替は換算用に ¥155/$ を仮置きする。¥170/$ に振れるとドル建て候補の円換算値は約 10% 上振れする

after（`c5aab0ae4`）: レートが仮置きであることは 6 章の未確認事項が持つため、値を置くだけにした

> - 為替は ¥155/$ を定数として使う

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 5164ad87 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:53`（`65a9c50b9` 時点）。修正は `c5aab0ae4`

### 16-4 [efso-document] この節で決めないことに、どこで決めるかを添えた

efso-document の PR #2912 の diffo レビュー。性能の節で序列を決めないと書くとき、どこで決めるかまで書いていた。

指摘の実文（diffo スレッド e8409fcd、2026-09-09）:

> 当社の負荷試験で決める→序列はここでは決めない

before:

> ここで分かるのは性能の要件を満たさない候補と、上回る根拠がある候補までで、序列は当社の負荷試験で決める。Shopify 側の制約は token エンドポイントの応答 1,000 ms で、Shopify から IdP への往復込み（`[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md#2-必須要件)` の性能）。

after（`862b95e46`）: どこで決めるかは 6 章の次の作業が持つため、この節では決めないことだけ書いた

> ここで分かるのは性能の要件を満たさない候補と、上回る根拠がある候補までで、序列はここでは決めない。Shopify 側の制約は token エンドポイントの応答 1,000 ms で、Shopify から IdP への往復込み（`[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md#2-必須要件)` の性能）。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド e8409fcd / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:105`（`73165d966` 時点）。修正は `862b95e46`

### 16-5 [efso-document] 参照先が持っている内容を、参照する側でも説明した

efso-document の PR #2912 の diffo レビュー。要件の重要度を説明する 2 箇所。
リンク先が持つ内容を「中身と、その重要度に置いた根拠」と数え上げていた。

指摘の実文（diffo スレッド 773678ee、2026-09-09）:

> 中身とその重要度に置いた根拠は→中身は

1 箇所だけ直したことへの追加の指示:

> 揃えて

before（3.3 節の導入と、2.2 節末の 2 箇所）:

> 7 つあり、中身とその重要度に置いた根拠は `[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md#3-推奨要件)` にある。

> 3 段階それぞれの要件の中身と、その重要度に置いた根拠は `[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md)` にある。

after（3.3 節は `a7ea955aa`、2.2 節末は `6ca658c40`）:

> 7 つあり、中身は `[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md#3-推奨要件)` にある。

> 3 段階それぞれの要件の中身は `[Auth0 代替候補の評価要件](./auth0-alternative-evaluation-requirements.md)` にある。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 773678ee / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:211`（`e95169664` 時点）。修正は `a7ea955aa` と `6ca658c40`

補記: 1 回目は指摘された 1 箇所だけを直し、同じ言い回しが他にもあることを返信で伝えて判断を仰いだ。同じ規範に当たる箇所が他にあるなら、指摘の対象外でも同時に直すのが期待されていた。

## 型 17: 記述を削るとき、他の場所に無い情報が落ちる

### 17-1 [efso-document] 削ったあとの状態どうしを突き合わせた

efso-document の PR #2912 の diffo レビュー。表を箇条書きに削ったあと、落ちた情報が他の節にあるかを突き合わせた場面。
突合を削ったあとの状態どうしで行ったため、削除の前後を比べたことにならず、2 件が落ちたまま残っていた。

指摘の実文（diffo スレッド 8fed050c、2026-09-09）:

> これ結局個別候補の比較のセクションに書いてない情報があったのかは確かめたの？あるなら残すべき。あと個別候補の比較を結論セクションに移動させたからむしろこっちには、個別候補の比較にない情報を残す場所にすべき

同じスレッドの追加の指摘:

> これ突合したのって編集を加える前の状態で比べた？削減した後の状態で比較したら正確じゃないかも

before（突合をやり直す前。落ちていた 2 件）:

> - トークンのクレーム拡張は Actions v2 で、外部エンドポイントを呼ぶ webhook のため言語を選ばない。Access Token の audience 指定は満たす

> - 運用形態はマネージドで、東京リージョンがある。Access Token の audience 指定は満たす

after（`11d6dd5e3`。セッション開始時点の表を全セル分解して突合し直した）:

> - トークンのクレーム拡張は Actions v2 で、外部エンドポイントを呼ぶ webhook のため言語を選ばない。セルフホストで動かすのは Go の単一バイナリと PostgreSQL。Access Token の audience 指定は満たす

> - 運用形態はマネージドで、東京リージョンがある。Access Token の audience 指定は満たす。Organization に属さないユーザーを扱えるかは未確認

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 8fed050c / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:221`・`:229`（`6e841e8b8` 時点）。修正は `6e841e8b8`（4 件を戻す）と `11d6dd5e3`（突合をやり直して 2 件を戻す）

補記: 落とした表は 13-3 にある。1 件目は 1 位と 2 位のセルフホストを比べる場面で片方だけ運用の重さを書いていたもの、2 件目は写像を 3 候補だけ書いて残る 1 候補が確認済みと読めていたもの。

## 型 18: 同じ対象を 2 箇所で扱うのに、役割を決めていない

### 18-1 [efso-document] 候補ごとの記述が 2 箇所にあり、どちらが要約かが決まっていなかった

efso-document の PR #2912 の diffo レビュー。個別候補の評価を 1.2 へ移したあと、推奨要件の節も候補ごとの箇条書きになり、同じ候補を 2 箇所で扱う形になっていた。

指摘の実文（diffo スレッド 8fed050c、2026-09-09）:

> では 1.2 を要約として、ここにはもう少し詳しく書くことにしようか。候補を箇条書きの最初のネストにするのをやめて 1.2 みたいにサブセクションにする？5 章にはまとめるべき

before（候補 7 件が同じ形で並ぶうちの 1 件）:

> - Keycloak
>     - SAML 2.0 IdP は SAML クライアントの登録で対応し、ユーザー管理 API は Refresh Token の rotation と失効まで満たす。Access Token の audience 指定も満たす。ライセンスは Apache-2.0。攻撃検知は未判定（`[5. 未確認事項と次の作業](#5-未確認事項と次の作業)`）
>     - 条件が付くのは運用形態。セルフホストのみで、可用性責任と 0.5〜1.0 FTE の運用体制を自社で持つ。この体制を継続確保できるかが、1 位の入れ替わる条件になっている（`[1.1 場合によって順位は変わりうる](#11-場合によって順位は変わりうる)`）。運用の重さは Java そのものより、HA のための Infinispan クラスタ、順番に上げる必要があるメジャーバージョンアップ、Java で書く SPI 拡張から来る
>     - トークンのクレーム拡張は Protocol Mapper を Java の SPI で書く。JavaScript の Script Mapper は preview のため、拡張はランタイムの Java (Quarkus) に縛られる
>     - Organization に属さないユーザーをそのまま扱えるので、Customer の写像を変えずに移せる。候補のうち 1 ユーザーが複数の Organization に属せるのも Keycloak だけ

after（`cf2c67b43`）: 1.2 を要約、3.3 を要件ごとの詳細と役割を分けたうえで、候補を h4 のサブセクションにした。1.2 の見出しと重ならないよう候補名に「の推奨要件」を付け、満たす要件も 1 行で触れ、要件の順序を候補間で揃えた

> #### Keycloak の推奨要件
>
> - SAML 2.0 IdP は SAML クライアントの登録で対応する
> - ユーザー管理 API は Admin REST API で `enabled` の切り替え・属性・外部 IdP リンクを扱い、Refresh Token の rotation と失効まで満たす
> - トークンのクレーム拡張は Protocol Mapper を Java の SPI で書く。JavaScript の Script Mapper は preview のため、拡張はランタイムの Java (Quarkus) に縛られる
> - 運用形態はセルフホストのみで、可用性責任と 0.5〜1.0 FTE の運用体制を自社で持つ。この体制を継続確保できるかが、1 位の入れ替わる条件になっている（`[1.1 場合によって順位は変わりうる](#11-場合によって順位は変わりうる)`）。運用の重さは Java そのものより、HA のための Infinispan クラスタ、順番に上げる必要があるメジャーバージョンアップ、Java で書く SPI 拡張から来る
> - Access Token の audience 指定は Client Scope と audience mapper で満たす
> - 攻撃検知は未判定（`[5. 未確認事項と次の作業](#5-未確認事項と次の作業)`）
> - ライセンスは Apache-2.0
> - Organization に属さないユーザーをそのまま扱えるので、Customer の写像を変えずに移せる。候補のうち 1 ユーザーが複数の Organization に属せるのも Keycloak だけ

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 8fed050c / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:214-218`（`19378d4b3` 時点）。修正は `cf2c67b43`

## 型 19: 表の列を削って、判定の違いが読めなくなる

### 19-1 [efso-document] 判定だけの表にしたら、同じ値ばかりが並んだ

efso-document の PR #2912 の diffo レビュー。性能の表を候補と判定の 2 列に削ったところ、9 行のうち 6 行が「未検証」「未判定」で埋まった。

指摘の実文（diffo スレッド 5ea5658a、2026-09-09）:

> ほとんど未検証だと見栄えが悪い。なんか他にかけることないんだっけ？検証しないと測れないっていう意味でいいの？それとも参考になる数値はあるの？

同じスレッドの追加の指摘（公開値の列を戻したあと）:

> 公開値の列を書くよりも、なぜ判定不能なのかの列が欲しい

before（見出し行と 2 行目まで）:

```text
| 候補 | 判定 |
|---|---|
| Keycloak セルフホスト | 公式実測が当社ピークを上回る |
| Zitadel セルフホスト | 未検証 |
```

after（`71b3bf873`。公開値の列を戻した）:

```text
| 候補 | 公開されている値 | 判定 |
|---|---|---|
| Keycloak セルフホスト | 公式実測 1,000 logins/s + 20,000 refresh/s | 当社ピークを上回る |
| Zitadel セルフホスト | 公式ロードテスト 153 iter/s | 未検証 |
```

after（`d9642a2f5`。公開値の列を「判定できない理由」に差し替えた）:

```text
| 候補 | 判定 | 判定できない理由 |
|---|---|---|
| Keycloak セルフホスト | 当社ピークを上回る | |
| Zitadel セルフホスト | 要検証 | 公開値は Zitadel 自身のロードテスト 153 iter/s だけで、k6 600 VU の 1 構成で測った値。当社の構成では測っていない |
```

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 5ea5658a / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:107-117`（`d2381b1b2` 時点）。修正は `71b3bf873` と `d9642a2f5`

補記: この表を 2 列まで削らせた前のスレッドは 13-2 にある。判定値そのものを 1 つに揃えた同じスレッドの指摘は 12-2。

## 型 20: 箇条書きの 1 項目に、別の場所へ押し出せる詳細を詰め込む

### 20-1 [efso-document] 比較に進めなかった候補の根拠を、全部その場に書いた

efso-document の PR #2912 の diffo レビュー。詳細比較に進めなかった 2 候補は候補ごとのドキュメントを持たないため、判定の根拠・出典・discovery 文書の中身までが箇条書きの 2 項目に集まっていた。

指摘の実文（diffo スレッド f1e15e6b、2026-09-09）:

> これ箇条書きどちらも長々とか書きすぎ。比較に進めなかったからここに情報全部詰め込まれてるってこと？ここ以外の場所に押し出すことってできない？

同じスレッドの追加の指摘（押し出し先の新設を別の PR にしてよいか尋ねたあと）:

> この PR に含めるべき。含めた上でやって。どう考えてもこの PR の範囲じゃない？

before:

> - GCP Identity Platform: Google のマネージド CIAM。自社アプリにログイン機能を組み込むためのサービスで、外部アプリのログイン先になる OP の機能を持たない。Firebase Auth 基盤に OIDC / SAML フェデレーションとマルチテナントを追加。外部 RP 向けの認可エンドポイントとトークンエンドポイントを持たず、ID トークンの発行者 `https://securetoken.google.com/{projectId}` に対応する discovery 文書は `issuer` / `jwks_uri` / `response_types_supported` 等の 5 項目だけで `authorization_endpoint` / `token_endpoint` / `end_session_endpoint` が無い。公式ドキュメントは外部 OIDC プロバイダを消費する側 (RP) としてのみ記述し、Shopify が接続する `/authorize` の提供に関する記載がない (`[Signing in users with OIDC](https://docs.cloud.google.com/identity-platform/docs/web/oidc)`)。自前の OP を立てて Identity Platform をその裏に置くラッパー構成は成立するが、自前実装の一案として `[自前 OP の裏側に GCP Identity Platform を置く場合](./auth0-alternative-selfbuild.md#4-自前-op-の裏側に-gcp-identity-platform-を置く場合)` で扱い、採らない。OP 機能の不在は Google の Issue Tracker の Feature Request（2022-11 起票、+112、更新なし）と Google 社員の回答 "Firebase Auth is not an OIDC provider." でも裏づけられる。探した範囲・discovery 文書の実物・ラッパー構成の検討は `[GCP Identity Platform を Shopify 向け OpenID Provider として使えるかの調査](./gcp-identity-platform-as-op.md)` に記した
> - Clerk: 開発者 (React / Next.js) 向けマネージド認証 SaaS。OIDC プロバイダ機能 (OAuth applications) に discovery / authorize / token / userinfo はある。満たさないのは Shopify の必須要件 2 件で、OAuth application に Logout URL の欄が無く (CA-16)、authorize エンドポイントが `login_hint` を受けない (CA-19)。application が持つ URL の項目は `redirect_uris` / `authorize_url` / `token_fetch_url` / `user_info_url` / `discovery_url` / `token_introspection_url` の 6 つで、ログアウト関連は 1 つも無い（`[Clerk Backend API OpenAPI spec (bapi/2026-05-12.yml)](https://raw.githubusercontent.com/clerk/openapi-specs/main/bapi/2026-05-12.yml)`）。`/oauth/authorize` が受けるクエリは `response_type` / `client_id` / `redirect_uri` / `scope` / `state` / `prompt` / `code_challenge` / `code_challenge_method` / `response_mode` / `nonce` の 10 個で、Tenant への所属も authorize では指定できず、どの Organization をトークンに載せるかは同意画面で利用者が選ぶ（`[Clerk Frontend API OpenAPI spec (fapi/2026-05-12.yml)](https://raw.githubusercontent.com/clerk/openapi-specs/main/fapi/2026-05-12.yml)`、`[How Clerk implements OAuth](https://clerk.com/docs/guides/configure/auth-strategies/oauth/how-clerk-implements-oauth)`）。公式ドキュメントが例に挙げるインスタンスの `[discovery 文書](https://well-hagfish-71.clerk.accounts.dev/.well-known/openid-configuration)` にも `end_session_endpoint` が無く、`backchannel_logout_supported` と `frontchannel_logout_supported` はどちらも `false`（2026-09-08 取得）

after（`c64b24fdc` と `6e841e8b8`）: GCP は既にあった調査ドキュメントへ、Clerk は同じ PR で新設した `[Clerk の評価](./auth0-alternative-clerk.md)` へ詳細を移し、この場には判定と参照だけ残した

> - GCP Identity Platform: Google のマネージド CIAM で、自社アプリにログイン機能を組み込むためのサービス。外部 RP 向けの認可エンドポイントとトークンエンドポイントを持たず、ID トークンの発行者に対応する discovery 文書にも `authorization_endpoint` / `token_endpoint` / `end_session_endpoint` が無い。判定の根拠と探した範囲、自前 OP の裏側に置くラッパー構成の検討は `[GCP Identity Platform を Shopify 向け OpenID Provider として使えるかの調査](./gcp-identity-platform-as-op.md)` にある
> - Clerk: 開発者 (React / Next.js) 向けマネージド認証 SaaS。OIDC プロバイダ機能 (OAuth applications) に discovery / authorize / token / userinfo はあるが、Shopify の必須要件を 2 件満たさない。OAuth application に Logout URL の欄が無く (CA-16)、authorize エンドポイントが `login_hint` を受けない (CA-19)。Tenant への所属も authorize では指定できない。判定の根拠と参考料金の算出は `[Clerk の評価](./auth0-alternative-clerk.md)` にある

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド f1e15e6b / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:232-233`（`71b3bf873` 時点）。修正は `c64b24fdc` と `6e841e8b8`

補記: 押し出し先のドキュメントが無い候補では、そのドキュメントを作るところまで同じ PR に含めることになった。

### 20-2 [efso-document] 参考料金の単価の内訳を、参照先が持っているのにその場にも書いた

efso-document の PR #2912 の diffo レビュー。比較に進めなかった 2 件の参考料金の下に置いた箇条書き。
段階単価の刻みと出典を並べていたが、同じ内容は各評価ドキュメントが既に持っていた。

指摘の実文（diffo スレッド e0f0838b、2026-09-09）:

> こんな詳しく書く必要ない。A と B から算出。程度でよい。詳細は別資料に移して

before:

> - GCP Identity Platform: Tier1 の段階単価（5 万まで無料 / 5〜10 万 $0.0055 / 10 万〜100 万 $0.0046 / 100 万〜1,000 万 $0.0032）を階段適用した計算値（`[Identity Platform の料金](https://cloud.google.com/identity-platform/pricing)`）。LINE を汎用 OIDC で繋ぐと Tier2 $0.015/MAU 扱いになり単価が上がる

after（`e95169664`）:

> - GCP Identity Platform: Tier1 の段階単価から算出。単価の内訳は `[GCP Identity Platform を Shopify 向け OpenID Provider として使えるかの調査](./gcp-identity-platform-as-op.md#54-費用)` にある

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド e0f0838b / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:127-128`（`d342eca18` 時点）。修正は `e95169664`

補記: 20-1 では押し出し先のドキュメントを同じ PR で新設したが、こちらは先に押し出し先を読んで同等の記述があることを確かめ、追記せず削るだけで済ませた。削ったことで参照先にしか残らなくなる記述（Clerk のセルフサーブ契約の可否、GCP の LINE 接続時の単価）は、返信で所在を示して戻すかどうかを尋ねた。この返信で使った呼び名が別の指摘を受けており、[norm-refit の PR レビューの文レベル指摘 事例集](./sentence-level-review-cases.md) の 5-26 にある。

## 型 21: 説明の順を直すために構成を組み替えて、かえって読みにくくする

### 21-1 [efso-document] 算出方法を先頭に出すために、入れ子を平らにして注意書きを後ろへ寄せた

efso-document の PR #2912 の diffo レビュー。金額の算出方法を説明する節。
提供形態を第 1 階層、その形態に付く注意書きを第 2 階層に置いた入れ子だった。

指摘の実文（diffo スレッド c0d9aea9、2026-09-09）:

> 結局どう算出しているのか言ってない。定価を使って外挿したという方だけ。しかも最後の最後に書いてあって読みづらい。
> こういうのにはこうやって算出したというだけの箇条書きでよい

1 回目の修正に対する差し戻し:

> 後ろにまとめたせいで余計読みづらくなった。元々の構成で文章だけ変えればよかったのに

before:

> - SaaS の候補は、公開価格表からの計算値（定価）
>     - この規模はどの候補も個別見積りの対象で、実額は未取得。定価より下がるとは限らず、Enterprise を MAU 非連動の最低契約額で出す候補もある
>     - EFS 規模の定価が公開されていない Zitadel Cloud と Ory Network は、下位プランの定価を EFS 規模まで当てた外挿値を載せている
> - セルフホストと自前実装の候補は、ライセンス費・インフラ稼働費・運用工数の 3 つを積んだ推定値。公開価格表が無いため。初期構築と移行の工数は一度きりなので年額に入れない

after 1 段目（`e95169664`。この形に差し戻しを受けた）:

> - SaaS（Amazon Cognito・Entra External ID・GCP Identity Platform・Clerk）: 公開価格表の単価に MAU を当てて月額を出し、12 倍した（定価）
> - EFS 規模の定価が無い SaaS（Zitadel Cloud・Ory Network）: 下位プラン（Zitadel は Pro、Ory は Production）の単価を EFS 規模まで線形に当てた（外挿）
> - セルフホストと自前実装（Keycloak・Zitadel・Ory の各セルフホスト、自前実装）: ライセンス費 + インフラ稼働費 + 運用工数を積んだ（推定）。ライセンス費は OSS なら 0 円、インフラ稼働費は GCP の料金表 × 推定ワークロード、運用工数は FTE × 人月 150 万円 × 12 か月
>
> 金額を読むときの前提。
>
> - SaaS の定価は実額ではない。この規模はどの代替候補も個別見積りの対象で、定価より下がるとは限らず、Enterprise を MAU 非連動の最低契約額で出す代替候補もある

after 2 段目（`6ca658c40`）:

> - SaaS（Amazon Cognito・Entra External ID・GCP Identity Platform・Clerk）は、公開価格表の単価で MAU 分の月額を出し、12 倍した（定価）。EFS 規模の定価が無い Zitadel Cloud と Ory Network は、下位プラン（Zitadel は Pro、Ory は Production）の単価を EFS 規模まで線形に伸ばした（外挿）
>     - 定価も外挿も実額ではない。この規模はどの代替候補も個別見積りの対象で、定価より下がるとは限らず、Enterprise を MAU 非連動の最低契約額で出す代替候補もある
> - セルフホスト（Keycloak・Zitadel・Ory）と自前実装は、公開価格表が無いため、ライセンス費 + インフラ稼働費 + 運用工数を積んだ（推定）

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド c0d9aea9 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:148-149`（`d342eca18` 時点）。修正は `e95169664` と `6ca658c40`

補記: 1 回目の指摘は「算出方法が書かれていない」「後ろにあって読みづらい」の 2 点だったが、対応は入れ子を平らにして注意書きを別ブロックへ移す組み替えになった。指摘が求めていたのは、既にある構成の第 1 階層へ算出方法を書くことだった。2 段目は構成を戻し、第 1 階層に算出方法、第 2 階層に注意書きを置いた。

## 型 22: 計算を示すのに、式と結果のどちらかしか書かない

### 22-1 [efso-document] 年額を先に出したあと、結果の無い式を続けた

efso-document の PR #2912 の diffo レビュー。代替候補ごとの年額の記述。
先に結果だけを出し、そのあとに結果を書かない式を並べたため、式と結果の対応が読めない。

指摘の実文（diffo スレッド 6d154573、2026-09-09）:

> 他の候補に対する書き方も同様だが、結局どの計算式でどの結果が導かれているのかがわからない。最初に Essentials で 1 億と書いてあり、その後に定価無料枠の上の計算が書かれている。その計算の結果が書かれていないのでそれが冒頭の 1 億の話なのか、それとも異なる何か別の式を書いたのかがわからない。で、別の式だとしたらそれはなんの参考値？結局どっちを見ればいいの？どんな条件だとどっちで、どんな条件だとどっち？全然読み取れない。他の候補も同様になってないかチェックして対応して

1 回目の修正に対する差し戻し:

> = 使えよ

before:

> 判定は確認次第。年額は Essentials で 約 1.00 億円（定価）に、リクエストレートクォータの引き上げが 約 1,900 万〜3,800 万円（推定）乗る。定価は無料枠 10,000 MAU の上が一律 $0.015/MAU で、月 $53,850 × 12 か月 × ¥155。

after 1 段目（`e95169664`。この形に差し戻しを受けた）:

> 判定は確認次第。年額は約 1.19 億〜1.38 億円で、Essentials の定価 約 1.00 億円にリクエストレートクォータの引き上げ 約 1,900 万〜3,800 万円（推定）を足した値。
>
> - 定価 約 1.00 億円: 無料枠 10,000 MAU の上が一律 $0.015/MAU で、月 $53,850 × 12 か月 × ¥155

after 2 段目（`0aef19a92`）:

> 判定は確認次第。年額は Essentials の定価 約 1.00 億円 + リクエストレートクォータの引き上げ 約 1,900 万〜3,800 万円（推定）= 約 1.19 億〜1.38 億円。
>
> - 定価: 無料枠 10,000 MAU の上が一律 $0.015/MAU。(360 万 − 1 万) × $0.015 = 月 $53,850 で、$53,850 × 12 か月 × ¥155 = 約 1.00 億円

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 6d154573 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:69`（`d342eca18` 時点）。修正は `e95169664` と `0aef19a92`

補記: 1 段目で式と結果を対にしたが、対応を地の文の語順で示したため差し戻された。2 段目で `=` を使い、演算の並びと結果を 1 つの式にした。指摘が「他の候補も同様になってないかチェックして」を含むため、全代替候補の金額の記述を点検した。結果だけで式が無かった外挿（Zitadel Cloud・Ory Network）には DAU / aDAU の出し方を足し、FTE 表記だけだった運用工数（Keycloak・自前実装）は円を明示した。

## 型 23: 少数の対象の情報を、種類ごとに別のブロックへ分ける

### 23-1 [efso-document] 2 件の理由・料金・算出元を 3 ブロックに分けて並べた

efso-document の PR #2912 の diffo レビュー。比較に進めなかった 2 件を扱う節。
落ちた理由の箇条書き、参考料金の表、料金の算出元の箇条書きが、同じ 2 件について順に並んでいた。

指摘の実文（diffo スレッド 788504f0、2026-09-09）:

> 箇条書き２つ、表で２つを比較、再度箇条書き２つ、という構成になってる。これもっとコンパクトにまとめられない？

before:

> - GCP Identity Platform: 自社アプリにログイン機能を組み込むためのサービスで、外部 RP 向けの認可エンドポイントとトークンエンドポイントを持たない。判定の根拠は `[GCP Identity Platform を Shopify 向け OpenID Provider として使えるかの調査](./gcp-identity-platform-as-op.md)`
> - Clerk: 開発者向けのマネージド認証 SaaS。OAuth application に Logout URL の欄が無く、authorize エンドポイントが `login_hint` を受けない。判定の根拠は `[Clerk の評価](./auth0-alternative-clerk.md)`
>
> | 代替候補 | MAU 100 万 | MAU 360 万 | MAU 1,000 万 |
> |---|---|---|---|
> | GCP Identity Platform | 年 約 820 万円 | 年 約 2,370 万円 | 年 約 6,180 万円 |
> | Clerk | 年 約 3,200 万円 | 年 約 1.05 億円 | 年 約 2.83 億円 |
>
> ※ 表 2 比較に進めなかった 2 件の参考料金（年額）
>
> - GCP Identity Platform: Tier1 の段階単価から算出。単価の内訳は `[GCP Identity Platform を Shopify 向け OpenID Provider として使えるかの調査](./gcp-identity-platform-as-op.md#54-費用)` にある
> - Clerk: 基本料金と MRU の段階単価から算出。課金単位が MAU ではなく MRU のため、MAU = MRU と仮定した上振れ値（推定）。単価の内訳は `[Clerk の評価](./auth0-alternative-clerk.md#5-料金)` にある

after（`6ca658c40`）:

> | 代替候補 | MAU 100 万 | MAU 360 万 | MAU 1,000 万 |
> |---|---|---|---|
> | GCP Identity Platform | 年 約 820 万円 | 年 約 2,370 万円 | 年 約 6,180 万円 |
> | Clerk | 年 約 3,200 万円 | 年 約 1.05 億円 | 年 約 2.83 億円 |
>
> ※ 表 2 比較に進めなかった 2 件の参考料金（年額）
>
> - GCP Identity Platform: 自社アプリにログイン機能を組み込むためのサービスで、外部 RP 向けの認可エンドポイントとトークンエンドポイントを持たない。年額は Tier1 の段階単価から算出。判定の根拠と単価の内訳は `[GCP Identity Platform を Shopify 向け OpenID Provider として使えるかの調査](./gcp-identity-platform-as-op.md)` にある
> - Clerk: 開発者向けのマネージド認証 SaaS。OAuth application に Logout URL の欄が無く、authorize エンドポイントが `login_hint` を受けない。年額は基本料金と MRU の段階単価から算出し、課金単位が MAU ではなく MRU のため MAU = MRU と仮定した上振れ値（推定）。判定の根拠と単価の内訳は `[Clerk の評価](./auth0-alternative-clerk.md)` にある

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド 788504f0 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:116`（`a7ea955aa` 時点）。修正は `6ca658c40`

補記: 最初の対応は表に「進めなかった理由」列を足して 3 ブロックを表 1 本に畳む形だったが、セルに 2 文とリンクが入り、`markdown-authoring.md` の「表のセルは流し読みできる短い判定値にし、文を書かない」に反した。表は金額だけに戻し、代替候補ごとの記述を 1 項目にまとめてブロックを 2 つにした。ブロック数を減らす要求と、表のセルを短く保つ規範は両立させる先が表の外にある。

## 型 24: 判定を書いて、その理由を参照先だけに置く

### 24-1 [efso-document] Cognito が使えない理由が、リンク先にしか無かった

efso-document の PR #2912 の diffo レビュー。代替候補ごとの評価のうち Amazon Cognito の段落。
判定だけを書き、その理由は詳細のリンク先にしか無かった。

指摘の実文（diffo スレッド b2f4e905、2026-09-10）:

> Organization SSO には使えないってどういう意味？SAML 対応してないってこと？

理由を先に書く案を出したことへの了承:

> そうして

before:

> Organization SSO には使えない。SLA は月間稼働率 99.9%。詳細は `[Amazon Cognito (user pools) の評価](./auth0-alternative-cognito.md)`。

after（`9d972ce88`）:

> Cognito は SAML では SP 側にしかなれないため、Organization SSO には使えない。SLA は月間稼働率 99.9%。詳細は `[Amazon Cognito (user pools) の評価](./auth0-alternative-cognito.md)`。

出所: 人間のレビュー（efso-document）

出典: efso-document の PR #2912 の diffo レビュー スレッド b2f4e905 / `to-be/idp/docs/investigation/auth0-alternative-idp-comparison.md:79`（`dda592e3d` 時点）。修正は `9d972ce88`

補記: 足した理由はリンク先の `auth0-alternative-cognito.md` が持っていた（「Cognito は SAML では SP 側にしかなれず、user pool を IdP の relying party として登録する側になる」）。この節は判定・年額・理由を書く場所で、他の候補には判定に理由が付いていた。

## 件数

型ごとの件数（延べ。1 事例が複数の型に当たる場合は各型で数える）:

- 型 1 表から読めることを地の文で繰り返す: 4 件
- 型 2 表の説明は地の文ではなく ※ に置く: 4 件
- 型 3 説明を消せるように、情報を表のセルへ持たせる: 1 件
- 型 4 参照の置き場と、指す先の見出しの有無: 3 件
- 型 5 不要な案内: 2 件
- 型 6 見出し直下の数文の見せ方: 4 件
- 型 7 作成過程の議論を読者向けの記述に混ぜる: 2 件
- 型 8 章の冒頭の前提が増えたら、最初のサブセクションへ出す: 1 件
- 型 9 参照先を読まないと意味が取れない細部を書く: 5 件
- 型 10 並列する項目を地の文に流す: 5 件
- 型 11 節の冒頭に、これから何を説明するかを書かない: 4 件
- 型 12 扱いを変えない区別を立てる: 2 件
- 型 13 全件を並べた表より、結論に効く差分だけを残す: 3 件
- 型 14 根拠に挙げた実績が、何を確かめていないかを書く: 3 件
- 型 15 1 点で足りるところに、当たり前のことや言い換えを足す: 2 件
- 型 16 他の場所が持っている内容を、ここにも書く: 6 件
- 型 17 記述を削るとき、他の場所に無い情報が落ちる: 1 件
- 型 18 同じ対象を 2 箇所で扱うのに、役割を決めていない: 2 件
- 型 19 表の列を削って、判定の違いが読めなくなる: 1 件
- 型 20 箇条書きの 1 項目に、別の場所へ押し出せる詳細を詰め込む: 2 件
- 型 21 説明の順を直すために構成を組み替えて、かえって読みにくくする: 1 件
- 型 22 計算を示すのに、式と結果のどちらかしか書かない: 1 件
- 型 23 少数の対象の情報を、種類ごとに別のブロックへ分ける: 1 件
- 型 24 判定を書いて、その理由を参照先だけに置く: 1 件
- 延べ合計: 61 件

実数（事例の数）: 55 件。複数の型に当たる事例が 5 件（1-3 が型 1 と型 2、3-1 が型 1 と型 3、8-1 が型 6 と型 8、12-1 が型 9 と型 12、11-4 が型 11 と型 16 と型 18）あるので、延べが 6 多い。

内訳（出所別）:

- 人間のレビュー: 55 件（efso-document の PR #2912 の diffo レビュー 2026-09-09: 52 件、2026-09-10: 3 件）
