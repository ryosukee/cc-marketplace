# norm-refit PR 3 確認・質問の一本化: 作業明細

実装者はこの明細だけを見て `rules/`（propose-before-implement の改名と拡張）、`plugins/claude-user-communication/`、
`plugins/claude-known-issues/`、marketplace.json・README.md・CLAUDE.md を編集する。
行番号はすべて main `b9fb206` の実読（`plugins/` と `rules/` は `a51f5a3` から変更なし。初版の実読・実測は 2026-08-25、ccm-f051 以降の確定の反映は 2026-08-26）。

結論。ask-with-choices skill（AQ、56 条項）を消し、確認・質問の規範を 2 つの器に分ける。
媒体に依らないもの（impl-spec から引き上げる 13 件、AQ18 の上流決定の明示、回答後のフロー）は
`rules/propose-before-implement.md` を「ユーザーへの確認」の rule に広げた `rules/user-confirmation.md`（改名）へ、
HTML フォーム固有のものは html-communication skill（HC）に残す。常時ロードは 8 ファイルのまま。
AQ から移すのは 2 件（AQ18、AQ36）で、残り 54 件は skill ごと消える。13 件は数えて一致した（C-1）。impl-spec 側の原文は消さず複製する。
HC 側の削除は HC92 / HC165（図表を語る文）と HC221（新語の条件付き許可）の 3 件で、どれも core が既に同趣旨を持つ。
HC の書き換えは「回答の受け取り」（1 文に縮める）、冒頭ブロックの見出し語（form「推奨案のまとめ」/ report「まとめ」。別 commit）、
HTML への移し替え禁止の 1 段落の追加。既知バグ一覧の `askuserquestion-rendering` は resolved 側へ移す（雛形は PR、配布済みは直接）。
claude-user-communication は 0.32.6 → 0.33.0、claude-known-issues は 0.3.1 → 0.3.2。

## A. AQ の条項（56 件）と skill の廃止

AQ の ID は監査の名簿（`norm-audit-roster.md:11-66`、原文は `norm-audit-raw/hc-fn-aq.md:2545-3006`）の番号。
`ask-with-choices/SKILL.md` は監査後に変わっていない（最終変更 `51ef947`、2026-07-23）ので、名簿の行番号がそのまま現行行。

処遇の値は 2 つ: 「削除」（skill ごと消える）と「移設」（HC へ実体を移す）。
処遇の出典に書く「auto」は `norm-audit-verdict-auto.md:44-47`（AQ 全件の機械判定。根拠は同 `:16-17`
「ask-with-choices skill 自体の廃止が確定済み（台帳 L963-964）。条項の中身に関わらず消える。振り分け基準は HC が持つ」）、
「dg18-34」は `norm-audit-verdict-dg18-34.md`、「dg35-49」は `norm-audit-verdict-dg35-49.md`。

### A-1. 条項の一覧

| ID | 現行行 | 実文（逐語） | 処遇 | 処遇の出典 | 移設先に既にある同趣旨 | 実装で書く文 |
| --- | --- | --- | --- | --- | --- | --- |
| AQ1 | 9-11 | レンダリングバグ（後述「AskUserQuestion を単独ターンで発行する（レンダリングバグ回避）」）が修正されるまで、AskUserQuestion は使用禁止。 | 削除 | dg18-34:63（DG19）、dg35-49:84（DG46） | HC `SKILL.md:15-16`（HC5。D で字句修正） | 無し |
| AQ2 | 12 | 代替: 簡単な確認はチャットのフリーテキストで質問し、 | 削除 | dg18-34:63（DG19） | HC:15（HC4）、入口 rule `rules/japanese-text-writing.md:18`「確認は 1 件ずつ出す」 | 無し |
| AQ3 | 12-13 | 複雑・設問が多い確認は [入り組んだ説明・報告・確認は HTML で行う](../html-communication/SKILL.md) の HTML フォームにする。 | 削除 | dg18-34:63（DG19） | HC:14（HC3）、core:59-61「設問が多い・前提説明が長い確認は HTML フォームにする」 | 無し |
| AQ4 | 14-15 | 修正の追跡と解除条件・解除手順は claude-known-issues plugin の台帳（エントリ id: `askuserquestion-rendering`）が管理する。 | 削除 | dg35-49:84（DG46） | claude-known-issues の一覧のエントリ `askuserquestion-rendering` の dependents（`~/.claude/plugins/data/claude-known-issues-cc-tools/known-issues.yml:13-19`）が逆向きに持つ | 無し（一覧側の記述は N） |
| AQ5 | 15-16 | バグ修正を確認したらこの警告を削除し、以下の本来の運用に戻す。 | 削除 | dg35-49:84（DG46） | 解除手順は一覧のエントリ側 `:16-19` が持つ | 無し |
| AQ6 | 18 | ユーザーに確認・質問をする際は、可能な限り `AskUserQuestion` ツールを使い、選択肢から選べる形式にすること。 | 削除 | dg35-49:59（DG36「実体は HC のフォーム設計節にある」） | HC:468「各設問に選択肢 (radio / checkbox) を用意し、推奨案には推奨バッジを付ける」 | 無し |
| AQ7 | 21 | 選択肢が明確に列挙できる場合は必ず選択肢形式にする | 削除 | dg35-49:59（DG36） | HC:369-371「確認も選択肢を持つ設問にする。」 | 無し |
| AQ8 | 22 | 選択肢に収まらない場合はユーザーが「Other」で自由入力できる | 削除 | dg35-49:59（DG36） | HC:472「選択肢に収まらない回答用に、設問ごとに「その他」の自由記述欄を付ける」 | 無し |
| AQ9 | 23 | **独立した複数の判断を求める場合は、1つの質問にまとめず複数の質問に分ける**（AskUserQuestion は最大4問まで対応。TUI ではタブ形式で表示され、各質問を独立して回答できる） | 削除 | auto | HC:362-364「複数の判断を 1 つの設問で一括承認させない。」、入口 rule:18 | 無し |
| AQ10 | 24 | **複数質問機能 (4 問まで) は積極的に活用する**。1 問ずつ送ると往復回数が増えてユーザーの待ち時間が伸びるため、まとめられるものはまとめる。 | 削除 | auto | HC:14（4 問超は HTML フォーム 1 ページに全設問） | 無し |
| AQ11 | 24 | 独立性が低い・順序依存があるなど分割が必要なケースだけ 1 問にする | 削除 | auto | 無し（ツール固有） | 無し |
| AQ12 | 25 | **5問以上の場合は複数回に分けて AskUserQuestion する**。 | 削除 | dg35-49:62（DG37「「4 問」はツール仕様由来の数値でツールと共に消える」） | HC:14 | 無し |
| AQ13 | 25 | 1回目の回答を待ってから2回目を発行する | 削除 | dg35-49:62（DG37） | 無し（ツール固有） | 無し |
| AQ14 | 26 | **複数回に分ける場合は、AskUserQuestion の前に全体計画を提示する**: 「全N問を M回に分けて確認します（1回目: Q1〜Q4、2回目: Q5〜Q6）」のように、総質問数・分割回数・各回の内容を事前に示す。ユーザーが見通しを持てるようにする | 削除 | dg35-49:62（DG37） | HC:291-294 が反対の規定（冒頭に設問の件数・所在を書かない） | 無し |
| AQ15 | 30-31 | 前提説明が長い・設問が多い（目安 4 問超）・比較表が必要な確認は、AskUserQuestion ではなく [入り組んだ説明・報告・確認は HTML で行う](../html-communication/SKILL.md) の HTML フォームを使う。 | 削除 | dg18-34:63（DG19「振り分け基準は HC3 が持つ」） | HC:14（HC3）、core:61 | 無し |
| AQ16 | 32 | AskUserQuestion は軽い確認・少数の設問に使う。 | 削除 | dg18-34:63（DG19） | HC:15（HC4） | 無し |
| AQ17 | 33-34 | ユーザーから「## HTML フォーム回答」で始まるテキストが貼られたら HTML フォームの回答として扱い、以降は「複数回の質問と回答後のフロー」の解釈まとめ以降と同じ手順で進める。 | 削除 | auto | HC:575（HC265。同文の前半） | 無し（後半のフローは B） |
| AQ18 | 38 | 複雑な判断を聞く時、選択肢 (option) を列挙する前に、その判断の上流にある **意味的・構造的決定** を先に明示する。 | 移設 | dg35-49:64（DG38「汎用部分（判断を聞く前に上流の決定を明示する）を HC の設問設計節へ。ツール固有記述は捨てる」）。置き先は台帳 `notes/norm-refit.md:1824-1845` で rule 側へ変更 | 無し。HC:376-381（HC202 / HC203）は後続の設問への依存を扱い、上流決定の明示ではない | R-2「問い方」の 2 項目め（rule 側） |
| AQ19 | 46 | 対処: AskUserQuestion を呼ぶ前の本文で、判断に関連する上流決定を表・箇条書きで整理し、その上で option を提示する。 | 移設 | dg35-49:64（DG38） | 同上 | R-2 に統合（上流決定の例 5 つを AQ19 の補足から取る） |
| AQ20 | 56 | 特に「phase 転換直後の命名 / 配置判断」「大規模 refactor の個別選択」など、直前の議論や別 phase で決まった前提に依存する質問は、その前提を明示しないと選択肢の意味が理解できない。 | 削除 | auto | HC:305-307「設問の節は自己完結させる。対象が何か（どのファイル・どの条項・どの提案か）と、選択肢に出る語の実体を、その節の中に書く」 | 無し |
| AQ21 | 60-62 | 承認は「目的」ではなく「具体的な手段・構成」に対して与えられる。実行中にブロッカーへ当たって手段を変える必要が生じたら、その時点で承認は失効している。代替手段がどれほど妥当に見えても、実装・状態変更に進む前に確認へ戻る。 | 削除 | auto | `rules/propose-before-implement.md` 節「実行中に手段が変わったら確認に戻る」（同文。台帳 `notes/norm-refit.md:1817` AQ9 の受け皿 = f008 Q3） | 無し |
| AQ22 | 63-64 | 「検証のため」「一時的」でも同じ。プロセス起動・デーモン設定・設定ファイル書き込みなど、会話の外に残る状態を作る操作は実装に含める | 削除 | auto | 同上（同文） | 無し |
| AQ23 | 65 | 読み取りだけの調査・比較検討は対象外。ブロッカーの分析と代替案の整理までは進めてよい | 削除 | auto | 同上（同文） | 無し |
| AQ24 | 66 | 確認時には「何が計画と変わるか」「増える管理物・残る状態」を明記する | 削除 | auto | 同上（同文） | 無し |
| AQ25 | 75 | ユーザーが判断するための材料 (現状、選択肢のトレードオフ、推奨案とその理由、影響範囲、関連ファイルパスや行番号) はできるだけ多く伝える。配置場所は以下の使い分け: | 削除 | dg35-49:67（DG39「汎用部分は HC234/HC235 が既に持つ」） | HC:469-471（比較表と裏取り）、HC:349-353（出典） | 無し |
| AQ26 | 77 | **AskUserQuestion 内 (`question` 文と option `description`)** にも、収まる範囲で十分な情報を入れる。ここを薄くしない | 削除 | auto | 無し（ツール固有） | 無し |
| AQ27 | 78 | **AskUserQuestion を呼ぶ前のテキスト出力** にも背景・比較・現状調査結果を出す。ここも省略しない | 削除 | auto | HC:469-471（設問の直前に多観点の比較表） | 無し |
| AQ28 | 79 | 両方やる。どちらか一方だけにしない | 削除 | auto | 無し（ツール固有） | 無し |
| AQ29 | 81 | ただし AskUserQuestion の各 option `description` には文字数の制約 (おそらく数行程度) がある。詳細を入れたいのに入り切らない場合は、**削るのではなく、AskUserQuestion を呼ぶ前のテキスト出力に追加で書き出す**。「入らないから事前に出す」のであって「事前に出すから option は削る」ではない。 | 削除 | dg35-49:67（DG39） | core:78「短さのために重要な情報を落とさない」 | 無し |
| AQ30 | 85-90 | AskUserQuestion を発行する前に、各選択肢について以下を本文で明示する。option `description` の中だけで完結させようとしない。（何をするか (具体的な作業内容、対象ファイル名、関数名、概算行数) / トレードオフ (利点・欠点・規模・リスク) / 推奨案とその根拠 (調査結果に基づくエビデンス、rule 名や公式 docs) / 影響範囲 (どのファイル / どの規約に関係するか)） | 削除 | dg35-49:67（DG39） | HC:343-348（`.proscons` の長所短所）、HC:358-361（他案を排除する理由）、HC:349-353（推奨の前提の出典） | 無し |
| AQ31 | 92 | option `description` は本文の要約として書く。 | 削除 | dg35-49:67（DG39） | HC:471「選択肢の description は比較表の結論の要約に徹する」 | 無し |
| AQ32 | 92 | 本文で詳細を出してから AskUserQuestion を呼ぶ。「事前テキスト → AskUserQuestion」の順序を守る。 | 削除 | auto | HC:469-470（設問の直前に比較表）、HC:463（設問は関連するセクションの直下） | 無し |
| AQ33 | 93 | ただしレンダリングバグ（後述「AskUserQuestion を単独ターンで発行する」）が存続する間は、この順序を同一ターン内ではなくターン分割で実現する。 | 削除 | auto | HC:266-267（HC147。HTML はバグの影響を受けない。D で字句修正） | 無し |
| AQ34 | 99 | **質問間に作業を挟まない**: 複数回に分けて質問する場合、前の回の回答が返ってきたらすぐに次の回の質問を発行する。質問間に調査・作業・反映を行わない。 | 削除 | auto | 無し（HTML フォームは 1 ページに全設問を出すので回を分けない） | 無し |
| AQ35 | 99 | すべての質問回答が終わってから次の作業に進む | 削除 | auto | 同上 | 無し |
| AQ36 | 100-104 | **全質問完了後は自由入力で確認する**: すべての回答が揃ったら、勝手に作業を始めず以下の手順を踏む: 1. 回答の解釈をまとめる（各質問の回答要約） 2. 次の作業の簡単なプランを説明する 3. AskUserQuestion を使わずに「他に気になることや変更点はありますか？」と自由入力での返答を促す 4. ユーザーが「特になし」等の回答であればそのまま作業に進む。追加の指示があれば反映する | 移設 | dg35-49:70（DG40「**移設は必須**。HC の「回答の受け取り」節は AQ への参照で実装されており、AQ 廃止で正典が消える」） | HC:577-578（HC266）がこの条項を参照しているだけで実体は無い | B の文（ccm-f051 Q2 の形。手順 3 は残さない） |
| AQ37 | 114 | このバグが直るまで、同一ターンでの「事前テキスト → AskUserQuestion」は機能しない。以下のフローで回避する: | 削除 | auto | HC:266-267（HC147） | 無し |
| AQ38 | 116 | 判断材料の本文（上流決定・各選択肢の詳細・推奨案）は、AskUserQuestion を発行しないターンの最終テキストとして出力する。 | 削除 | auto | 同上 | 無し |
| AQ39 | 117 | ターンの末尾は「読み終えたら何でもよいので返信してください。次のターンで質問 UI を出します」で締める | 削除 | auto | 同上 | 無し |
| AQ40 | 118 | ユーザーの返信を受けたターンでは、AskUserQuestion をターン最初のアクションとして単独発行する。 | 削除 | auto | 同上 | 無し |
| AQ41 | 119 | AskUserQuestion より前にツール呼び出しもテキスト出力も置かない | 削除 | auto | 同上 | 無し |
| AQ42 | 120 | 複数回に分けて質問する場合も、各回とも 2 と同様に単独発行する。 | 削除 | auto | 同上 | 無し |
| AQ43 | 120 | 「質問間に作業を挟まない」ルールはこのフローでも維持する | 削除 | auto | 無し（AQ34 と同じ） | 無し |
| AQ44 | 121 | 回答を受け取った後の作業（ファイル編集・調査・報告）は同一ターンで続けてよい。ターン末尾の最終テキストは正常に表示される | 削除 | auto | 無し（ツール固有） | 無し |
| AQ45 | 122 | 事前本文が不要な軽い質問は、テキストを添えずに AskUserQuestion を単独発行してよい。 | 削除 | auto | HC:15（HC4） | 無し |
| AQ46 | 123 | いずれの場合も、本文が読めなくても回答できるよう `question` 文と option `description` を自立させる（バグで本文が消えるケースへの保険） | 削除 | auto | HC:305-307（設問の節は自己完結させる） | 無し |
| AQ47 | 128-129 | このセクションはレンダリングバグ修正後の運用。バグ存続中は前セクションのターン分割フローが優先で、AskUserQuestion と同一ターンに本文を置かないため、末尾マーカーとバッファの出番はない。 | 削除 | auto | 無し（ツール固有） | 無し |
| AQ48 | 133 | **AskUserQuestion の直前に重要な情報（図表・ツリー・コードブロック等）を配置しない**。重要な情報は AskUserQuestion より十分前に出力を完了させる | 削除 | auto | 無し（ツール固有） | 無し |
| AQ49 | 134-141 | **バッファ行を入れる直前に、末尾マーカーを出力する**。以下のフォーマットを使う:（`---` / `*--- 出力ここまで / 以下 AskUserQuestion UI に隠れないように調整バッファ ---*`）これにより、ユーザーがスクロールした際に本文の末尾とバッファの境界を判別できる | 削除 | auto | 無し（ツール固有） | 無し |
| AQ50 | 142-146 | **バッファは以下の1行のみ**。隠れるのは最後の1行だけなので、末尾マーカーの後に以下を1行出力すれば十分（`*------------------------------------------------------------------------------*`） | 削除 | auto | 無し（ツール固有） | 無し |
| AQ51 | 150 | ユーザーが AskUserQuestion をキャンセルし、プロンプトで補足情報を返してきた場合、以下の手順で対応すること: | 削除 | auto | 無し（キャンセルはツール固有。HTML フォームの未回答は HC:486「未回答の設問がある状態でもコピーは許可し、未回答は `未回答` と明記する」） | 無し |
| AQ52 | 152 | **回答カバレッジの判定**: キャンセルされた AskUserQuestion の各 question に対して、ユーザーの補足が回答を含んでいるか判定する | 削除 | auto | 同上 | 無し |
| AQ53 | 153 | **話題の逸脱判定**: ユーザーの補足が元の質問とは別の話題（修正依頼、方針変更、新しい指示など）を含んでいるか判定する | 削除 | auto | 同上 | 無し |
| AQ54 | 154-161 | **判断の明示**: 以下のフォーマットで判断を出力し、ユーザーが確認できるようにする:（`## AskUserQuestion 補足の解釈` / `- Q1「{質問内容}」→ {回答あり: 要約 / 回答なし}` / `- Q2「{質問内容}」→ {回答あり: 要約 / 回答なし}` / `- 別の話題: {あり: 要約 / なし}`） | 削除 | auto | B の手順 1（回答の解釈をまとめる）が同型 | 無し |
| AQ55 | 163 | **未回答の質問がある場合**: 別の話題の対応を優先しつつ、未回答の質問が作業に必要なら改めて確認する | 削除 | auto | 無し | 無し |
| AQ56 | 163 | （ただし既に回答済みの質問は再度聞かない） | 削除 | auto | `html-communication/references/review-norms.md:28`「確定と未確定の峻別: 確定事項を無断で再質問・変更しない。」 | 無し |

内訳: 削除 54 / 移設 2（AQ18 + AQ19 を rule の 1 項目に、AQ36 を rule の 1 節と HC の 1 文に）。
性質が「汎用」の 9 件（AQ18 / AQ20〜AQ25 / AQ56）は、AQ18 以外はすべて現行の rule・HC・review-norms に同文か同趣旨があることを上の表で確かめた。
`norm-audit-verdict-auto.md:52-57` の「実装で条項を触るとき、原文を読んで違和感があれば止めて吟味側へ回す」に従って全 56 件の原文を読み、吟味側へ回すものは無かった。
汎用の 9 件のうち AQ18 の置き先が HC から rule に変わったのは、判定の変更ではなく台帳 `notes/norm-refit.md:1824-1845`（2026-08-25）の構成変更による。

### A-2. AQ への参照（廃止で切れる箇所）

repo 内で `ask-with-choices` / `AskUserQuestion` を grep した結果（2026-08-25、`notes/` と `.handover/` を除く）のうち、AQ skill そのものを指しているもの。

- `plugins/claude-user-communication/.claude-plugin/plugin.json:3`（description に「選択肢形式の確認 (ask-with-choices) の 2 skill」）→ H
- `plugins/claude-user-communication/README.md:3,7-8`（「2 つの skill を提供する」と ask-with-choices の項目）→ H
- `plugins/claude-user-communication/skills/html-communication/SKILL.md:16`（HC5 の相対リンク）→ D
- 同 `:267`（HC147 の相対リンク）→ D
- 同 `:577-578`（HC266 の相対リンク。回答後のフローの参照）→ B
- `.claude-plugin/marketplace.json:64`（description）→ H
- `CLAUDE.md:80`（Plugin 一覧の「+ 選択肢形式の確認」）→ H
- `README.md:53`（plugin 一覧の「+ 選択肢形式の確認の 2 skill」）→ H
- `plugins/claude-known-issues/config/known-issues.template.yml:13-20`（`askuserquestion-rendering` の dependents と解除手順）→ N
- `~/.claude/plugins/data/claude-known-issues-cc-tools/known-issues.yml:13-19`（同エントリの配布済みの一覧。repo 外）→ N（PR の外の直接作業）
- `plugins/claude-user-communication/skills/html-communication/references/patterns/progress-tree/example.html:15`（パターン集の例示データ「ask-with-choices の廃止 未着手」）→ 未特定 J
- `evals/` 配下: 言及なし。ケース自体が無く、`evals/README.md` と `evals/run.sh` だけ（2026-08-25 実測）

AskUserQuestion の使用指示（impl-spec 16 / dotclaude 13 / mkdocs-setup 6 / session 1。計画 `norm-refit-plan.md:359-362` の 41 行から claude-known-issues の雛形 5 を除いた分）は PR 4 の管轄で、PR 3 では触らない（L）。

### A-3. skill を消す手順

1. `plugins/claude-user-communication/skills/ask-with-choices/` をディレクトリごと削除する（`SKILL.md` 163 行の 1 ファイルだけ。2026-08-25 実測）
2. A-2 の参照を B / D / H で書き換える
3. `docs/retired-plugins.md` に 1 件足す（未特定 J。既定案は足す）。
   形式は既存エントリに揃える: 名前・廃止日・最終版 0.32.6・削除 commit・理由・復元コマンド
   `git show <削除 commit>^:plugins/claude-user-communication/skills/ask-with-choices/SKILL.md`
4. 消した後に `grep -rn 'ask-with-choices' --exclude-dir=notes --exclude-dir=.handover --exclude-dir=.git .` を実行し、
   残るのが example.html の例示データ（J で残すと決めた場合）と、N で書き換えた known-issues の雛形の resolved 記述だけであることを確かめる

## B. DG40 の本文化（AQ36 → 回答後のフロー）。A より先に行う

申し送り 1（`norm-audit-implementation-notes.md:12-16`、逐語）:

> `html-communication/SKILL.md` の「回答の受け取り」節は ask-with-choices への参照で
> 実装されている（HC266）。AQ を先に消すと、回答後のフロー（解釈まとめ → プラン提示 →
> 自由入力での最終確認）が消える。AQ36 の内容を HC 内へ本文化してから AQ を消す

同じ PR の中で行うので commit の順序の問題になる。B と R の commit を A の削除 commit より前に置く。

フローの形は ccm-f051 Q2（台帳 `notes/norm-refit.md:1740-1822`）で決まった。逐語:

> Q2 IS52（続ける設問があるかを尋ねない）の趣旨は「ターミナルの会話で無駄な 1 ホップを挟まない」こと。
> HTML フォームには自由記述欄があるので同じ問題は起きない。回答後のフロー（AQ36 の本文化）は
> 「解釈と回答のまとめを出し、自明なものは作業に進み、自明でないものは確認を取る」という通常のターンへ
> すぐ移る形に書く。AQ36 手順 3（自由入力での最終確認を独立したターンで求める）は残さない

置き場は台帳 `notes/norm-refit.md:1824-1845` が rule 側（`rules/user-confirmation.md`）と定めた。
DG40 の判定「フロー全文を HC 内へ自己完結で書く」（`norm-audit-verdict-dg35-49.md:71`）は、この構成変更で rule 側の本文化に置き換わる。

現行（HC `SKILL.md:573-578`、逐語）:

```markdown
## 回答の受け取り

ユーザーから「## HTML フォーム回答」で始まるテキストが貼られたら HTML フォームの回答として扱う。

以降の解釈まとめ・作業フローは [ユーザーへの確認は選択肢形式で](../ask-with-choices/SKILL.md) の
「複数回の質問と回答後のフロー」と同じ。
```

書き換え後（HC 側。既定案は 1 文だけ残す。未特定 J）:

```markdown
## 回答の受け取り

ユーザーから「## HTML フォーム回答」で始まるテキストが貼られたら HTML フォームの回答として扱う。
```

rule 側の文（R-2 の節「回答を受けたら」）:

```markdown
## 回答を受けたら

回答が揃ったら、解釈と回答のまとめを出し、自明なものはそのまま作業に進み、自明でないものは確認を取る。
まとめを出した後に、追加の指示を求めるだけのターンを挟まない。
```

AQ36 からの変更点は 3 つ。

- 手順 1・2（解釈のまとめ・次のプランの説明）を「解釈と回答のまとめ」の 1 文に畳んだ。ユーザー補足「今のように解釈や回答のまとめを出した上で」に合わせた
- 手順 3・4（自由入力での返答を促し、「特になし」なら進む）を落とし、「自明なものは作業に進み、自明でないものは確認を取る」に置き換えた。
  独立したターンで最終確認を求める形は残さない（Q2）
- 道具の名指し（AskUserQuestion）は書かない。道具が消えると意味を失う（申し送り 5 の SS18 / SS55 と同じ扱い、`norm-audit-verdict-dg35-49.md:86`）

HC 側に 1 文だけ残す理由: 回答後の進め方は媒体に依らず、rule が常時ロードで届く。HC に同じ文を再掲すると到達点 1「plugin 側に汎用の再実装を残さない」に反し、
rule への参照を置くことは台帳 `:1824-1845`「plugin 側から rule への参照は置かない」が禁じる。
代案は HC にも同じ 1 文を再掲する（plugin 自己完結を優先する読み方）。どちらを採るかは未特定 J。

index の `answered` 更新（HC:210-211）はこの節に再記述しない。「生成・表示方法」が正で、1 回書きの原則。

## C. impl-spec から引き上げる確認の規範 13 件

### C-1. 数え合わせ

台帳 `notes/norm-refit.md:1024-1037`（2026-08-22 の訂正）は「`norm-audit-verdict-lift.md` の 3 件（IS4 / IS34 / IS52）と
`norm-audit-verdict-partial.md` の 10 件」で 13 件と定める。

- lift 側: `norm-audit-verdict-lift.md:42-59` の表に IS4 / IS34 / IS52 の 3 件。処遇 `移設` / 置き先 `plugin`（html-communication の設問設計節）
- partial 側: `norm-audit-verdict-partial.md:112-116`（「引き上げ」の「確認の規範 10 件」の節）は ID を列挙していない。
  同 `:101`「突合が付けた引き上げ先の分布は 確認 10 / core 5 / その他 5 / subagent 4 / 判断 3」に従い、
  突合 `norm-audit-raw/is-generic-screening.md` の「部分的」節（`:205-639`）で `引き上げ先: 確認` を持つ条項を数えた。
  IS15 / IS36 / IS37 / IS57 / IS59 / IS61 / IS79 / IS94 / IS197 / IS234 の 10 件（実測 2026-08-25）

3 + 10 = 13 で一致。同じ 13 件を `notes/idea-hub-handoff.md:60-67`（2026-08-26 のエントリ）が列挙している。

判定の置き先 `plugin`（HC）は、台帳 `notes/norm-refit.md:1824-1845` の構成変更で `rules/user-confirmation.md` に変わった。
判定ファイルの置き先の語彙（`verdict-schema.md`）に「常時ロードの rule」は無いので、この 13 件の置き先は判定ファイルを直さず、台帳のエントリを正とする。

数え合わせで見えた 1 点。「既存なし」節（`is-generic-screening.md:33-203`）で `引き上げ先: 確認` を持つのは IS4 / IS34 / IS52 / IS233 の 4 件だが、
IS233（確認の工程を飛ばす条件）は lift の判定で `維持` / `移設先`（`norm-audit-verdict-lift.md:33`）に落ちている。
対になる IS234（飛ばした旨を報告する）は partial の 10 件に入る。PR 3 では判定どおり IS234 だけを引き上げる。

### C-2. 13 件の一覧

現行位置は 2026-08-25 の grep 実測（impl-spec は監査後に変わっていない。最終変更 `d6e8e0f`、2026-05-05）。
実文は `is-generic-screening.md` の原文欄の逐語。「同一条項の他 ID」は同ファイルの記載で、impl-spec の他 skill に残る同文。
「実装で書く文」は R-2 の完成形に入れる文。

| ID | 現行位置 | 実文（逐語） | 処遇の出典 | rule / HC に既にある同趣旨 | 実装で書く文 |
| --- | --- | --- | --- | --- | --- |
| IS4 | `requirements/SKILL.md:25` | 質問の粒度・観点はプロジェクトのドメインとアーキテクチャに合わせる | lift:46,57 | 無し（HC:469-471 は比較表の観点を「その選定で効く観点」とするだけ） | 質問の粒度と観点は、対象のドメインと構造に合わせる |
| IS34 | `requirements/SKILL.md:115`（他 ID: IS131 = `design:156`、IS226 = `test-plan:156`、IS45 / IS140 = `requirements:125`） | 自明な質問をしない。Phase 2 の調査で判明したことは聞かない | lift:47,58 | 無し。HC:369-371「確認も選択肢を持つ設問にする」は逆に設問を増やす向き | 調査で判明したことは質問しない。自明に決まる判断は質問せず報告にとどめる |
| IS52 | `requirements/SKILL.md:160` | 終了を判断するのは skill 側。「まだ質問はありますか?」とは聞かない。 | lift:48,59、趣旨は ccm-f051 Q2 | 無し | 設問を出し切ったかは自分で判断する。「まだ質問はありますか」のように、続ける設問の有無だけを尋ねるターンを挟まない |
| IS15 | `requirements/SKILL.md:59-60` | ユーザーに質問する前に、自分で調べられることは調べる。「コードを読めばわかること」を質問するのは skill の品質に反する。 | partial:112-116、screening「一般化: 要」 | 無し | ユーザーに質問する前に、自分で調べられることは調べる。読めば分かることを聞かない |
| IS36 | `requirements/SKILL.md:117`（他 ID: IS132 = `design:157`） | 構造的な判断 (分類、グルーピング、カテゴリ分け、適用範囲のマッピング) は skill が勝手に作らない。構造はユーザーとインクリメンタルに合意しながら組み立てる。 | partial:112-116、screening「一般化: 要」 | `rules/propose-before-implement.md:3-6` は 1 回の関門だけ（同ファイルを広げるので同じ rule に並ぶ） | 分類・グルーピング・適用範囲のマッピングは自分で作らず、ユーザーと少しずつ合意しながら組み立てる |
| IS37 | `requirements/SKILL.md:117`（他 ID: IS133 = `design:157`） | 「全部」「統一」「一律」等の方針が来た場合は、全件・全パターンを洗い出して提示し、各々が本当に含まれるか確認する。 | partial:112-116、screening「一般化: 不要」 | `review-norms.md:51` は文書内の列挙の網羅性で、指示の解釈ではない | 「全部」「統一」「一律」の方針が来たら、全件・全パターンを洗い出して提示し、各々が本当に含まれるかを確認する |
| IS57 | `requirements/SKILL.md:178`（他 ID: IS148 = `design:199`、IS270 = `test-plan:277`） | ユーザーへの再質問が必要なもの: Phase 3 に戻り AskUserQuestion で確認してから修正する | partial:112-116、screening「一般化: 要」 | HC:524-530（反復の終了条件）はユーザーへ戻る枝を持たない | レビュー指摘のうち判断が要るものは、確認の工程へ戻ってユーザーに確認してから直す |
| IS59 | `requirements/SKILL.md:183`（他 ID: IS150 = `design:204`） | 技術的事実の検証が必要なもの: まず検証し、検証結果をユーザーに提示した上で判断を確認する。検証結果から「自明」に見えても、判断はユーザーに委ねる | partial:112-116、screening「一般化: 不要」 | `propose-before-implement.md:10-11` の判定基準は「選択肢を潰すか」で、自明に見えても省かない歯止めは無い | 技術的な事実の検証が要る判断は、先に検証し、検証結果を提示したうえで判断を問う。検証結果から自明に見えても、判断はユーザーに委ねる |
| IS61 | `requirements/SKILL.md:186-187`（他 ID: IS153 = `design:208`、IS273 = `test-plan:285`） | 分類に迷ったら「ユーザーへの再質問が必要」側に倒す。推測で埋めた判断が間違っていた場合のコストは、追加質問のコストより高い。 | partial:112-116、screening「一般化: 要」 | 無し | 問うか自分で決めるかに迷ったら、問う側に倒す。推測で埋めた判断の誤りのコストは、追加の確認のコストより高い |
| IS79 | `requirements/SKILL.md:241`（他 ID: IS176 = `design:273`、IS295 = `test-plan:344`） | 推測で要件を埋めない。不明な点は必ずユーザーに質問する | partial:112-116、screening「一般化: 要」 | 無し（core:145-146 は「未確認と明示する」まで） | 推測で埋めない。不明な点はユーザーに質問して確定させる |
| IS94 | `design/SKILL.md:51`（他 ID: IS180 = `design:276`） | 逸脱する場合は理由を明示する | partial:112-116、screening「一般化: 不要」 | 無し | 対象の既存パターンから逸脱するときは、理由を明示して確認する |
| IS197 | `test-plan/SKILL.md:45` | フィードバック内容をユーザーに報告し、上流文書の修正が必要か確認する | partial:112-116、screening「一般化: 要」 | 無し | 作業中に入力元の文書の不備を見つけたら、報告して修正の要否を確認する |
| IS234 | `test-plan/SKILL.md:182` | スキップする旨をユーザーに報告する。 | partial:112-116、screening「一般化: 不要」 | 無し | 確認の工程を飛ばしたときは、飛ばしたことを報告する |

「実装で書く文」は screening の一般化欄の文を基にし、impl-spec 固有の語（Phase 番号・skill・要件 / 設計判断 / テスト方針）を落とした。
IS52 は ccm-f051 Q2 の趣旨（ターミナルの会話で無駄な 1 ホップを挟まない）に合わせ、尋ねない対象を「続ける設問の有無だけを尋ねるターン」に限定した。
IS94 は原文が述部だけなので、他 ID の IS180「プロジェクトの既存パターンを無視しない。逸脱する場合は理由を明示してユーザーに確認する」から主語を補った。
IS234 は原文が「スキップする旨」だけなので、同じ行の前半（IS233「インタビューの必要がない場合 (調査で全て確定した場合) は Phase 3 をスキップして」）から対象を補った。

### C-3. 置き場は rule 側。HC には足さない

13 件と AQ18 の置き場は `rules/user-confirmation.md`（R-2）。HC には「設問を立てる前に決めること」の節を立てない。
HC に残るのは HTML フォーム固有の「確認（設問）を含めるときの作り」（HC:452-486）だけで、変更なし。

### C-4. impl-spec 側の原文は消さない（確定）

ccm-f051 Q1（台帳 `notes/norm-refit.md:1740-1822`）: 「impl-spec 側の 13 件の原文は消さず、html-communication へ複製する。
idea-hub への申し送りは「cc-marketplace 側はこう整理した。そちらも整理してほしい」という伝え方にする（「HC が正」とは書かない）」。
複製先は構成変更で rule 側になった（R-2）。

impl-spec 3 skill は手つかずで移設先へ同行する。C-2 の他 ID 14 箇所もそのまま。
idea-hub への申し送りは `notes/idea-hub-handoff.md:60-67`（2026-08-26 のエントリ）に追記済み。PR 3 で行う notes の作業は無い。

### C-5. IS52 と AQ36 手順 3（決着）

screening（`is-generic-screening.md:138-144`）が「正面から衝突する」と書いた 2 条項は、ccm-f051 Q2 で AQ36 手順 3 を残さないと決まり、衝突は消えた。
rule 側には IS52 の文（「問い方」）と回答後のフロー（「回答を受けたら」）が並ぶが、どちらも「無駄な 1 ホップを挟まない」向きで揃っている。

## R. `rules/user-confirmation.md`（propose-before-implement の改名と拡張）

決定（台帳 `notes/norm-refit.md:1824-1845`、2026-08-26）: 媒体に依らない確認・質問の規範は `rules/propose-before-implement.md` を
「ユーザーへの確認」の rule に広げてそこへ置き、ファイル名を `rules/user-confirmation.md` に変える。決め手は「ターミナルでの確認に届く器であること
（候補 C の条件ロードと D の HC skill は届かない）と、常時ロードを増やさないこと（候補 B は 9 になる）。既存の 2 節（提案と実装を分ける / 手段が変わったら確認に戻る）が
同じ「いつユーザーに戻るか」の主題で、足しても主題が揃う」。回答の実文は「A」。

### R-1. 現行の `rules/propose-before-implement.md`（32 行、逐語）

```markdown
# 設計判断を含む作業は提案と実装を分ける

「考えて」「検討して」「どうするか決めて」型の依頼は、案の提示までが依頼であり、
実装の許可ではない。設計判断（名前・場所・構造・恒常運用・rule / skill の変更など、
複数の妥当な選択肢からユーザーが選びうるもの）を含む作業は、
案を提示して承認を得てから実装する。

- 系列の進行承認（例: 「順番にやろう」「進めて」）は、その工程の中で新たに生じる
  個別の設計判断への承認を含まない
- 判定基準: この変更を実装してしまうと、ユーザーが選びたかったかもしれない選択肢を
  提示なしで潰すか。潰すなら提案で止める。やり直しが安価な機械的作業なら進めてよい

## 実行中に手段が変わったら確認に戻る

承認は「目的」ではなく「具体的な手段・構成」に対して与えられる。
実行中にブロッカーへ当たって手段を変える必要が生じたら、その時点で承認は失効している。
代替手段がどれほど妥当に見えても、実装・状態変更に進む前に確認へ戻る。

- 「検証のため」「一時的」でも同じ。プロセス起動・デーモン設定・設定ファイル書き込みなど、
  会話の外に残る状態を作る操作は実装に含める
- 読み取りだけの調査・比較検討は対象外。ブロッカーの分析と代替案の整理までは進めてよい
- 確認時には「何が計画と変わるか」「増える管理物・残る状態」を明記する

why: 承認は手段に対して与えられているので、手段が変わった時点で承認の根拠が消えている。
「目的への前進は承認済み」と読み替えると、ユーザーが選ぶはずだった代替手段の選択を
提示なしに潰し、会話の外に残る状態まで作ってしまう。

## why

「考えて」型の依頼は、選択肢を出してもらってから自分で選ぶための依頼で、
選ぶ工程そのものをユーザーが持っている。案を出さずに実装まで進めると、
その工程を奪い、やり直しのコストがユーザー側に移る。
```

### R-2. `rules/user-confirmation.md` の完成形

`git mv rules/propose-before-implement.md rules/user-confirmation.md` の後に、この内容にする。
既存 2 節の条項は一字も変えない。変えるのは、冒頭（見出しと導入文）、既存の第 1 節に見出しを付けること、末尾の「## why」を第 1 節の直下へ
「why:」段落として移すこと（第 2 節と同じ形。節が 6 つになるので、末尾に置くと何の why か分からなくなる）、新しい 4 節の追加。

```markdown
# ユーザーへの確認

判断をユーザーに委ねる場面の規範。何を確認に回し何を自分で決めるか、実行中に前提が変わったときの戻り方、
問う前の取捨と問い方、回答を受けた後の進め方を定める。

## 設計判断を含む作業は提案と実装を分ける

「考えて」「検討して」「どうするか決めて」型の依頼は、案の提示までが依頼であり、
実装の許可ではない。設計判断（名前・場所・構造・恒常運用・rule / skill の変更など、
複数の妥当な選択肢からユーザーが選びうるもの）を含む作業は、
案を提示して承認を得てから実装する。

- 系列の進行承認（例: 「順番にやろう」「進めて」）は、その工程の中で新たに生じる
  個別の設計判断への承認を含まない
- 判定基準: この変更を実装してしまうと、ユーザーが選びたかったかもしれない選択肢を
  提示なしで潰すか。潰すなら提案で止める。やり直しが安価な機械的作業なら進めてよい

why: 「考えて」型の依頼は、選択肢を出してもらってから自分で選ぶための依頼で、
選ぶ工程そのものをユーザーが持っている。案を出さずに実装まで進めると、
その工程を奪い、やり直しのコストがユーザー側に移る。

## 実行中に手段が変わったら確認に戻る

承認は「目的」ではなく「具体的な手段・構成」に対して与えられる。
実行中にブロッカーへ当たって手段を変える必要が生じたら、その時点で承認は失効している。
代替手段がどれほど妥当に見えても、実装・状態変更に進む前に確認へ戻る。

- 「検証のため」「一時的」でも同じ。プロセス起動・デーモン設定・設定ファイル書き込みなど、
  会話の外に残る状態を作る操作は実装に含める
- 読み取りだけの調査・比較検討は対象外。ブロッカーの分析と代替案の整理までは進めてよい
- 確認時には「何が計画と変わるか」「増える管理物・残る状態」を明記する

why: 承認は手段に対して与えられているので、手段が変わった時点で承認の根拠が消えている。
「目的への前進は承認済み」と読み替えると、ユーザーが選ぶはずだった代替手段の選択を
提示なしに潰し、会話の外に残る状態まで作ってしまう。

## 調べれば分かることは問わない

- ユーザーに質問する前に、自分で調べられることは調べる。読めば分かることを聞かない
- 調査で判明したことは質問しない。自明に決まる判断は質問せず報告にとどめる
- 技術的な事実の検証が要る判断は、先に検証し、検証結果を提示したうえで判断を問う。
  検証結果から自明に見えても、判断はユーザーに委ねる

## 推測で埋めずに問う

- 推測で埋めない。不明な点はユーザーに質問して確定させる
- 問うか自分で決めるかに迷ったら、問う側に倒す。推測で埋めた判断の誤りのコストは、追加の確認のコストより高い
- 分類・グルーピング・適用範囲のマッピングは自分で作らず、ユーザーと少しずつ合意しながら組み立てる
- 「全部」「統一」「一律」の方針が来たら、全件・全パターンを洗い出して提示し、各々が本当に含まれるかを確認する
- 対象の既存パターンから逸脱するときは、理由を明示して確認する
- 作業中に入力元の文書の不備を見つけたら、報告して修正の要否を確認する
- レビュー指摘のうち判断が要るものは、確認の工程へ戻ってユーザーに確認してから直す

## 問い方

- 質問の粒度と観点は、対象のドメインと構造に合わせる
- 複雑な判断を問うときは、選択肢を並べる前に、その判断の上流にある決定を先に明示する。
  上流の決定とは、何が A で何が B かという分類、何層構造でどこに何を置くかという構造、
  何を優先軸にするかという原則、どの層が他に依存してよいかという依存関係、前の工程で確定した事項。
  上流を示さずに選択肢を出すと、選択肢の前提が伝わらずに誤答されるか、判断材料が足りないと差し戻される
- 設問を出し切ったかは自分で判断する。「まだ質問はありますか」のように、続ける設問の有無だけを尋ねるターンを挟まない
- 確認の工程を飛ばしたときは、飛ばしたことを報告する

## 回答を受けたら

回答が揃ったら、解釈と回答のまとめを出し、自明なものはそのまま作業に進み、自明でないものは確認を取る。
まとめを出した後に、追加の指示を求めるだけのターンを挟まない。
```

R-4 で入口 rule の 2 条項を移すと決めた場合は、「問い方」の末尾に次の 2 項目を足す。

```markdown
- 確認は 1 件ずつ出す。判断が 2 件以上あるなら 1 メッセージに並べず、順に出す
- 選択肢に出す語は、選択肢より前に実体を書く
```

新しい 4 節の出所は、「調べれば分かることは問わない」= IS15 / IS34 / IS59、「推測で埋めずに問う」= IS79 / IS61 / IS36 / IS37 / IS94 / IS197 / IS57、
「問い方」= IS4 / AQ18（+ AQ19 の例）/ IS52 / IS234、「回答を受けたら」= AQ36（ccm-f051 Q2 の形）。文は C-2 の「実装で書く文」と B の文をそのまま使う。

上流決定の項目は AQ18 の原文「その判断の上流にある **意味的・構造的決定** を先に明示する」を核に、
例 5 つを AQ19 の補足（`hc-fn-aq.md` AQ19「分類決定 (何が A カテゴリで何が B カテゴリか)」「構造決定 (何層構造でどこに何を置くか)」
「原則 (何を優先軸にするか)」「依存関係 (どの層が他を import 可・不可か)」「過去 phase の確定事項 (新 phase の入力になるもの)」）から、
理由 2 つを AQ18 の補足 why（「ユーザーが「判断材料が足りない」でキャンセルする」「選択肢の前提が伝わらず誤答される」）から取った。
3 つ目の why「option `description` に収まらない長文説明が必要になる」はツール固有なので捨てた（DG38「ツール固有記述は捨てる」）。
AQ19 の「本文で」「表・箇条書きで整理し」は媒体の値なので落とし、「先に明示する」だけにした。

### R-3. `rules/rule-authoring.md` との照合

- 命名（`:14`）: kebab-case の名詞句。`user-confirmation.md` は適合
- 冒頭（`:18-19`）: 何を定めるかを 1〜3 行。paths が無い（常時ロード）ので適用範囲の言い直しは無し。完成形の導入文は 2 行
- 条項（`:23-29`）: 判定基準は真偽を判定できる形でだけ置く。新しい 4 節に判定基準は足していない（screening の一般化文に無いものを作らない）
- 参照（`:48-54`）: 自動ロードで載る文書と外側の構造に言及しない。完成形に plugin 名・skill 名・他 rule 名は無い。
  台帳 `:1824-1845`「plugin 側から rule への参照は置かない」と対で、rule 側からも HC を指さない
- why の書き方（`:70-72`）: 日付付きの具体事例を書かない。既存の why 2 つはそのまま。新しい節に why は足していない
- 改訂（`:76-80`）: 既存記述を書き直しの根拠に使わない。既存 2 節は条項を変えないので対象外

### R-4. 参照が切れる箇所と、入口 rule の 2 条項

改名で切れる参照は repo 内に無い（2026-08-26、`propose-before-implement` と「提案と実装を分ける」で grep。ヒットは `rules/propose-before-implement.md:1` 自身だけ。
`notes/` は対象外で触らない）。`README.md:82-91` の rule 一覧表は propose-before-implement の行をもともと持たない（bash-state-mutation-isolation / skill-invocation も無い）。
`~/.claude/rules/cc-marketplace` は `rules/` ディレクトリへの symlink（実測）なので、ディレクトリ内の改名で切れない。
`~/.claude/plugins/data/` と `~/.claude/settings.json` にも言及なし（grep 実測）。

README の rule 一覧表に `user-confirmation` の行を足すかは未特定 J（既定案は足す。表が 3 rule を欠いているのは別の観測）。

入口 rule `rules/japanese-text-writing.md:18-19` の 2 条項（逐語）:

```markdown
確認は 1 件ずつ出す。判断が 2 件以上あるなら 1 メッセージに並べず、順に出す。
選択肢に出す語は、選択肢より前に実体を書く。
```

台帳 `:1824-1845` の決めなかった範囲「実装時に条項単位で決める（明細で扱う）」。前提として、台帳 `notes/norm-refit.md:1506-1522` と `:1539-1551`（2026-08-25）は
「質問の運用（確認は 1 件ずつ・選択肢の語の実体を先に）は入口の最小規範に残す」と決めている。既定案は 2 条項とも新 rule の「問い方」へ移し、入口から消す
（未特定 J、ユーザーに問う）。

- 「確認は 1 件ずつ出す。判断が 2 件以上あるなら 1 メッセージに並べず、順に出す」: 確認の出し方の規範で、新 rule が確認の器になった以上そこが 1 箇所の正。
  入口の最小規範は「数行返答の書き方」に戻る。どちらも常時ロードなので届く範囲は変わらない。
  残す側の理由は、台帳 `:1539` の決め手「数行の確認を書く場面で core を読まずに効く」で、これは新 rule でも満たされる
- 「選択肢に出す語は、選択肢より前に実体を書く」: 選択肢の書き方で、上流決定の明示（R-2「問い方」2 項目め）と同じ「先に実体を示す」型。同じ節に並べると 1 箇所になる。
  残す側の理由は、これは文の書き方（執筆規範）でもあること

移す場合、入口 rule は 19 行から 17 行になる。計画の到達点 3「入口 rule 約 20 行」の範囲内。

## D. HC5 / HC147 の字句修正（AQ への参照と、レンダリングバグの時限条件を落とす）

申し送り 6（`norm-audit-implementation-notes.md:43-46`、逐語）:

> 「レンダリングバグ修正までは AskUserQuestion を使わず…ask-with-choices 冒頭の警告参照」の
> うち、参照部分が切れる。基準だけ残す

処遇は HC3 / HC4 / HC5 とも `維持` / `plugin`（`norm-audit-verdict-dg18-34.md:62`、訂正 3 `:46-51`「基準だけを残して参照を落とす字句修正が要る」）。
加えて ccm-f051 Q4（台帳 `:1740-1822`）で、既知バグ一覧の `askuserquestion-rendering` は「AskUserQuestion を使わない構成に移るので解決として resolved 側へアーカイブする」と決まった（N）。
その解除手順 3「html-communication skill の適用基準から、レンダリングバグを理由とした注記を外す」を PR 3 で実施するので、「レンダリングバグ修正までは」の時限条件も外す。
AskUserQuestion を使わないことは、バグ対応ではなく構成として固定される。

現行（HC:14-16、逐語）:

```markdown
- HTML にする: 前提説明が長い調査報告、複数の表・比較を含む設計判断、設問が概ね 4 問を超える確認、順序立てた説明が必要なもの
- テキストのまま: 短い報告、単発〜少数の質問（レンダリングバグ修正までは AskUserQuestion を使わず、
  チャットのフリーテキストで質問する。[ユーザーへの確認は選択肢形式で](../ask-with-choices/SKILL.md) 冒頭の警告参照）
```

書き換え後（行 14 は変更なし。行 15-16 だけ）:

```markdown
- テキストのまま: 短い報告、単発〜少数の質問（AskUserQuestion は使わず、チャットのフリーテキストで質問する）
```

一覧のエントリへのポインタは書かない（AQ4 が持っていたポインタは DG46 で削除。一覧側の dependents が逆向きに HC を指す。
`plugins/claude-known-issues/skills/entry/SKILL.md:47-49`「ワークアラウンドを担う rule・skill の側には、この plugin や一覧のエントリへの言及を置かない」）。

HC147（HC:266-267。処遇 `維持` / `plugin`、`norm-audit-verdict-auto.md:41` の HC139〜HC147）は AQ をリンクで指し、前提もバグの存在。

現行（逐語）:

```markdown
- ターン構造と無関係にブラウザで表示されるため、
  [ユーザーへの確認は選択肢形式で](../ask-with-choices/SKILL.md) に記載のレンダリングバグの影響を受けない
```

書き換え後（既定案。未特定 J。代案は項目ごと消す）:

```markdown
- ターン構造と無関係にブラウザで表示されるので、ターミナルの描画の制約を受けない
```

`norm-audit-verdict-auto.md:52-57` の「移設・廃止の前提が変わった」に当たる（バグ回避という前提が、一覧の resolved 化で消える）ので、
維持のまま字句を直すか消すかは実装者が決める。媒体の性質（ブラウザ表示）は HTML を選ぶ根拠として残るので、既定案は字句修正。

## E. 段階 2 Q4: 新語の条件付き許可を禁止へ戻す（HC221）

決定（台帳 `notes/norm-refit.md:1008-1011`、ccm-f030 Q4）: 「**全面禁止を維持する**（推奨と逆。ユーザー回答）」「`html-communication/SKILL.md:358` の条件付き許可を禁止へ戻す」
「判定の変更: HC221 を `保留` から `削除` / 現在地へ。既存の禁止（`japanese-text-writing.md:20` と SK31）が実体として残る」。
判定ファイルは `norm-audit-verdict-partial.md:89,94-97`（HC221 `削除` / 現在地。HC219 / HC220 は禁止と両立するので `維持`）。

決定時の `SKILL.md:358` は現行の `:406-409`。現行（逐語）:

```markdown
**自分で作った語を説明なしに使わない。**
既存の一般語・公式名を先に探し、あればそれを使う。無ければ作ってよいが、
初出で必ず定義する。実例として、設問の見出しに「スコープ手段」という造語を置き、
定義を書かないまま選択肢を問うたことがある（回答不能になった）。
```

決定が「実体として残る」とした既存の禁止は、PR 1 で core へ移っている。現行の core（`rules/japanese-text-writing/references/core.md:159-163`、逐語）:

```markdown
- 用語は 1 概念 1 語。同義語をローテーションしない。
  新しい概念に名前を付ける前に既存の一般用語を探し、独自語を作らない。
  日本語の造語を無理に作らない。無理に作った日本語よりも英語の方に一般的な用語があるなら、
  単語・熟語のレベルで英語を混ぜてよい。
  「ツール」「AI」のような曖昧な広い語に後退しない
```

入口 rule `rules/japanese-text-writing.md` には新語の条項は無い（2026-08-25 実測。「独自語」「造語」「新語」で `rules/` を grep して core:160-161 だけ）。
決定の「`japanese-text-writing.md:20`」は決定時の入口 rule の行で、いまは core が実体。core 側の変更は無い。

書き換え後（HC:406-409）:

```markdown
**自分で作った語を使わない。**
既存の一般語・公式名を先に探し、それを使う。実例として、設問の見出しに「スコープ手段」という
造語を置き、定義を書かないまま選択肢を問うたことがある（回答不能になった）。
```

変更点は 3 つ。

- 「無ければ作ってよいが、初出で必ず定義する。」（HC221 の実文）を消す
- HC219「自分で作った語を説明なしに使わない。」の「説明なしに」を落とす。条件付き許可を消した後にこの限定が残ると、説明すれば作ってよいと読める（未特定 J）
- HC220「既存の一般語・公式名を先に探し、あればそれを使う。」の「あれば」を落とす。無いときの分岐が消えたため

実例の文は残す。造語を定義なしに置いた失敗例で、全面禁止の下でも禁止の理由として機能する。
節末の判定基準（HC:427-428「その説明を消したとき、直後の表や設問が読めなくなるか。なるなら補足に置く。ならないならその語は本文に要らない」）は
用語の説明一般の要否の基準で、新語の可否ではないので残す。

`references/review-norms.md:55,58`（FN「用語の出自: AI が持ち込んだ命名・専門用語はユーザー定義と区別し、初出で必ず定義する」
「新語導入の規律: 概念に名前を付ける前に、既存の一般用語・業界標準語を調査し、あればそれを使う」）は FN 全体が T4 保留
（`norm-audit-verdict-typespec.md:116-124` FN41 の扱いと同じ）なので PR 3 では触らない。

## F. HC92 / HC165 の削除と core への一本化

決定（台帳 `notes/norm-refit.md:1207-1209`、ccm-f038 Q6）: 「html-communication と core の重複解消（HC92 / HC165）は PR 3 に置く」
「決め手: PR 3 は html-communication の SKILL.md を本格的に書き換える唯一の PR で、plugin 側の変更が 1 つの PR に束なる」。
計画 `norm-refit-plan.md:336-337`「HC92 / HC165（図を語る文 / 表を語る文）の plugin 側を削除し core へ一本化する（f038 Q6。到達点 1 の検証条件「plugin 側に汎用の再実装が残らない」の穴を塞ぐ）」。

判定は B 群の「分割」（`norm-audit-verdict-typespec.md:45-67`、`type-specific-screening.md:724`）。禁止（汎用の核）は core へ `移設`、
代替の担い手（caption・ハイライト・補足 pane）は媒体固有の値として `維持` / `plugin`。
「割った後の文がそれぞれ単独で読めることを確かめる」（同 `:66-67`）。

core に既にある同趣旨（`core.md:107-109`、逐語）:

```markdown
- 図・表を語る文を本文に置かない。図表の直前のリード文も、図表を解説する段落も書かない。
  何の図表かはキャプション（または表の直下に置くそれに相当する 1 行）が担う。
  図表を見なくても新しく分かることがある文だけを本文に置く
```

HC92 / HC165 の禁止部分はこの 3 行に全部入っている（リード文の禁止・解説段落の禁止・キャプションが担う・判定文）。
core へ足す文は無い。

### F-1. HC92（HC:173-175）

現行（逐語）:

```markdown
- 図も表と同じ作法でキャプションを付ける。雛形の `.fig` で包み、`.cap` を図の下に置く。
  字の大きさと色は表の `caption` と揃える。番号を振り、何の図かを書く。
  図を語る文を本文に置かない（表と同じ）
```

書き換え後（行 175 の 1 文を消す。行 173-174 は媒体固有の値で `維持`）:

```markdown
- 図も表と同じ作法でキャプションを付ける。雛形の `.fig` で包み、`.cap` を図の下に置く。
  字の大きさと色は表の `caption` と揃える。番号を振り、何の図かを書く
```

### F-2. HC165（HC:316-319）

現行（逐語）:

```markdown
- **表を語る文を本文に置かない。** 表の直前にリード文を置かず、表について解説する段落も書かない。
  何の一覧かは `caption` が、注目すべき行はハイライトが担うので、それを日本語で言い直す文は
  文章自身を語る文にあたる。表の前提・条件・読みどころは補足（`.su`）へ出し、
  表のセルから参照マーカーで飛ばす
```

書き換え後（禁止と理由を消し、HTML 固有の器の指定だけ残す）:

```markdown
- 表の前提・条件・読みどころは補足（`.su`）へ出し、表のセルから参照マーカーで飛ばす
```

残す 1 文は core:236「詳細は表の直下の補足に出す。媒体が脚注・補足の別枠を持つならそちらへ出す」の HTML 側の器の指定で、単独で読める。
「何の一覧かは `caption` が」は HC:320「`caption` は表の下に置く」が、「注目すべき行はハイライトが」は HC:321-322「表のどの行が効くかはハイライトが示す」が持つので、消しても値は残る。

### F-3. 検査している側

`plugin-release.md`「規範を変えたら、それを検査している側を一緒に直す」に従って「語る文」「リード文」で plugin 内を grep した（2026-08-25）。

- `scripts/validate-page.sh` と雛形固有検査 15 種、`agents/page-reviewer.md`、`references/review-norms.md`: 該当なし。
  表を語る文を機械検査する検査は無く、page-reviewer の観点にも無い
- `templates/page.html:126,142,379,394` のコメント 4 箇所（「表を語るリード文は本文に置かないので、」「図を語る文は本文に置かない（表と同じ）」等）:
  雛形の中の作者向けコメントで、規範の実体ではない。core に同じ禁止が残るので誤りにはならない。既定案は触らない（未特定 J）

## G. 「長い本文を HTML ページへそのまま移し替えない」を HC へ

決定（台帳 `notes/norm-refit.md:1390-1397`、2026-08-23）: 「「長い本文を HTML ページへそのまま移し替えない」は decision-docs.md から削除し、
PR 3 で html-communication skill 側へ入れる」「変更した確定: f038 Q3 の個別処置「HTML 化の再構成規範を decision-docs の構成節へ書き戻す」。
PR #6 のレビューで「これは html-communication skill に書くべき内容では」と指摘され、HTML への変換作業は媒体固有の運用で skill の管轄、と判断が変わった」。

出典の実文（PR #6 レビュー行コメント。2026-08-25 に `gh api` で取得）:

- 指摘 3838531953（2026-08-23、`rules/japanese-text-writing/references/decision-docs.md` 当時の行 39）: 「これは html communication skill に書くべき内容では？」
- 返信 3838600076: 「そのとおりなので decision-docs から削除し、html-communication skill へ入れる作業として PR 3 の中身に追記しました (46ac934)。
  これは ccm-f038 設問 3 の個別処置「decision-docs へ書き戻す」の変更になるので、台帳に決定の変更として記録済みです。」

原文の系譜（逐語）:

- 旧 `rules/user-communication-format.md:20-21`（`git show 9d9b399:rules/user-communication-format.md`。監査 ID は UC10、`norm-audit-raw/jr-uc-mf.md:236-241`）:
  「HTML にするときは、長文をそのまま HTML へ移し替えない。結論・概要から詳細へ辿れる構造（セクション分け、比較表、出典の分離）に組み直す。」
- PR #6 の途中版 `decision-docs.md`（commit `22b0cfb` で追加、`46ac934` で削除）:
  「長い本文を HTML ページへそのまま移し替えない。結論・概要から詳細へ辿れる構造（セクション分け、比較表、出典の分離）に組み直す」
- 現行の `rules/` には無い（「移し替え」で grep して 0 件、2026-08-25）

置き場の既定案は「適用基準」（HC:12-16）の末尾に段落を 1 つ足す（未特定 J）。媒体を HTML に決めた直後に効く規範で、
HC の description「ターミナルに長い報告を書き始める前が発動点で、書き終えてからでは遅い」と同じ場面を指す。

```markdown
HTML にすると決めたら、ターミナル向けに書いた（書きかけた）長文をそのまま HTML へ移し替えない。
結論・概要から詳細へ辿れる構造（セクション分け、比較表、出典の分離）に組み直す。
```

HC:243「構成は「前提（この文書は何の話か）→ 結論・概要 → 詳細 →（あれば）設問」の順。セクション番号か目次を付ける」（M で見出し語を書き換える）は
組み直した後の並びの規定で、移し替えの禁止とは別の条項。重複にはならない。

## M. 冒頭ブロックの見出し語を form「推奨案のまとめ」/ report「まとめ」に（別 commit）

決定（台帳 `notes/norm-refit.md:1847-1864` と、その上書き `:1866-1879`、2026-08-26）: 雛形の冒頭ブロック（`.concl`、見出し語「結論」）は form では推奨の要約、
report では現状の要約で、結論と呼べる中身は答えを 1 つ出す場合だけ。見出し語を form は「推奨案のまとめ」、report は「まとめ」に分ける。
skill の構成規定は「前提 → まとめ（報告では結論や現在地。フォームでは推奨案のまとめ）→ 詳細 → 設問」の趣旨に書き換え、skill 内の「結論ブロック」の呼び方も揃える。
CSS のクラス名 `.concl` は変えない。回答の実文は「report でどちらが適してるのか現状の html を確認してみて、直感経験的にはまとめのが適してると思った」。

PR 3 の中の別 commit にする。

### M-1. `html-communication/SKILL.md`（4 箇所）

HC:243-245（逐語）:

```markdown
- 構成は「前提（この文書は何の話か）→ 結論・概要 → 詳細 →（あれば）設問」の順。セクション番号か目次を付ける
- 結論の前に 2〜3 文の前提を置く。何のトピックか、どういう経緯でこの文書が要るのかを、
  他の作業から戻った読者がコンテキストを復帰できる粒度で書く。いきなり結論から始めない
```

書き換え後:

```markdown
- 構成は「前提（この文書は何の話か）→ まとめ（報告では結論や現在地。フォームでは推奨案のまとめ）→ 詳細 →（あれば）設問」の順。
  セクション番号か目次を付ける
- まとめの前に 2〜3 文の前提を置く。何のトピックか、どういう経緯でこの文書が要るのかを、
  他の作業から戻った読者がコンテキストを復帰できる粒度で書く。いきなりまとめから始めない
```

HC:259-260（逐語）:

```markdown
      何を問われているかで、推奨は選択肢のバッジと結論ブロックが持つ。
      説明の節の見出しは従来どおり主張型（結論を見出しに載せる）
```

書き換え後（「結論を見出しに載せる」は節の見出しの話で、冒頭ブロックの呼び方ではないので残す）:

```markdown
      何を問われているかで、推奨は選択肢のバッジと冒頭のまとめブロックが持つ。
      説明の節の見出しは従来どおり主張型（結論を見出しに載せる）
```

HC:287-288（逐語）:

```markdown
- 冒頭に地図を置く: 前提（何の話か・経緯）→ この文書の目的。
  前提の後に結論・答えを出す（前提は結論の手前に置く 2〜3 文、背景の長い経緯とは別物）
```

書き換え後:

```markdown
- 冒頭に地図を置く: 前提（何の話か・経緯）→ この文書の目的。
  前提の後にまとめを出す（前提はまとめの手前に置く 2〜3 文、背景の長い経緯とは別物）
```

HC:500-501（逐語）:

```markdown
- `ref-order`（脚注番号・補足英字が初出順でない）で落ちたら、`node scripts/renumber-refs.mjs <ページ>`
  で振り直してから再検査する。pane 内の並びも揃える。結論ブロックは初出順の対象外
```

書き換え後:

```markdown
- `ref-order`（脚注番号・補足英字が初出順でない）で落ちたら、`node scripts/renumber-refs.mjs <ページ>`
  で振り直してから再検査する。pane 内の並びも揃える。冒頭のまとめブロックは初出順の対象外
```

HC:295「`結論: …`」（ラベル + コロンの例）と HC:566「結論・要約・セクション構成の説明・表の再現・推奨の根拠は、すべてページの中にある」は
冒頭ブロックの呼び方ではないので変えない。

### M-2. `templates/page.html`（見出し語の出し分けと注釈 3 箇所）

form / report で分ける指示は上書きエントリ `:1866-1879`（ユーザー発言「form は推奨案まとめにすべきでは？」）。

form / report の切り替え機構は手作業（`page.html:7-10` のヘッダ注釈。form 用の要素を生かし、report 用は `<!-- report 用（form では削る…） -->` でコメントアウトして同梱。
`:467`、`:493-494`、`:513-514`）。`scripts/assemble-page.mjs` の `--report` は `<script>` を付けないだけ（`:12`、`:53`）で、本文の文字列は差し替えない。
既定案は雛形の同じ形に合わせ、form 用の見出し語を生かして report 用をコメントで並べる（未特定 J。代案は `assemble-page.mjs --report` で置換）。

`page.html:355-358`（逐語）:

```html
<div class="concl">
<span class="eyebrow">結論</span>
<p>{{読者が最初に知りたい答え}}</p>
</div>
```

書き換え後:

```html
<div class="concl">
<span class="eyebrow">推奨案のまとめ</span> <!-- report では「まとめ」にする -->
<p>{{form: 推奨案の要約 / report: 結論か現在地}}</p>
</div>
```

`page.html:7-10` のヘッダ注釈（逐語）:

```text
  form / report の共通部分は同一。型ごとに次を切り替える。
    form   : 下部バー #bar（情報・入力・操作の 3 ゾーン）と設問 pane #q-pane を使う
    report : #bar を #footer-nav に差し替え、#q-pane と .rng[data-q] の対応を削る。
             #preview / QS 定義 / 回答コピーの JS も削る。範囲と追従は使わない
```

書き換え後（report 行に 1 文足す）:

```text
  form / report の共通部分は同一。型ごとに次を切り替える。
    form   : 下部バー #bar（情報・入力・操作の 3 ゾーン）と設問 pane #q-pane を使う。冒頭ブロックの見出し語は「推奨案のまとめ」
    report : #bar を #footer-nav に差し替え、#q-pane と .rng[data-q] の対応を削る。
             #preview / QS 定義 / 回答コピーの JS も削る。範囲と追従は使わない。冒頭ブロックの見出し語は「まとめ」
```

`page.html:376`（逐語）「推奨は選択肢のバッジと結論ブロックが持つ。主張型にするのは説明の節だけ -->」→「推奨は選択肢のバッジと冒頭のまとめブロックが持つ。主張型にするのは説明の節だけ -->」。

`page.html:93`「「結論: …」のようにラベルとコロンで段落を作る形は使わない」はラベル + コロンの例で、そのまま。

### M-3. 検査スクリプト

「結論」「concl」で `scripts/` を grep した（2026-08-26）。文字列「結論」を判定に使う検査は無い。

- `scripts/check-page.mjs:198-199`（コメント）「脚注番号・補足英字は本文の初出順。起点は最初の説明節 (.secnum) か設問の範囲 (section.rng) で、前提・結論ブロックは対象外」。
  判定は `.secnum` と `section.rng` の位置（`:200-203`）で、冒頭ブロックのクラス名も見出し語も見ていない。コメントの「結論ブロック」を「冒頭のまとめブロック」に直す
- `scripts/renumber-refs.mjs:8`（コメント）「結論ブロックは初出順の対象外 (検査と同じ)」→ 同じく「冒頭のまとめブロック」に直す
- `agents/page-reviewer.md`、`references/review-norms.md`: 「結論ブロック」への言及なし（`review-norms.md:41` の「結論を再記述せず」は設問セクションの結論で別物）

既存ページ（共通ページディレクトリの form / report）は書き換えない（HC:498-499「検査対象はそのセッションで生成・改稿したページだけ」の運用に合わせる）。

## N. 既知バグ一覧の `askuserquestion-rendering` を resolved 側へ移す

決定（ccm-f051 Q4、台帳 `notes/norm-refit.md:1740-1822`。解釈の確定は同 `:1881-1894`、ユーザー発言「アーカイブはその解釈でおけ」、2026-08-26）: エントリは PR 3・4 で AskUserQuestion を使わない構成に移るので、
バグの修正を待たずに resolved 側へ移す。雛形は PR 3 で（claude-known-issues の version bump を同じ PR に入れる）、配布済みの一覧は直接直す。
ユーザー補足「そもそもその機能を使わない設定値を使うので解決としてアーカイブする」。

手順の型は `plugins/claude-known-issues/skills/review/SKILL.md:45-48`（逐語）に合わせる。

```text
2. 解除候補があれば、エントリの `dependents` をユーザーに提示し、解除してよいか確認する。
   承認されたら dependents の各場所を直し、エントリに `resolved_at`（日付）と
   `resolved_version` を足して `known-issues.resolved.yml` へ移す。
   最後に `grep -c resolved_at known-issues.yml` が 0 であることを確かめる（移し忘れの検知）
```

エントリの型は `plugins/claude-known-issues/skills/entry/SKILL.md:26-45`（8 項目。`id` / `title` / `summary` / `affected` / `refs` / `dependents` / `how_to_verify` / `log`）。
「エントリに status は無く、どちらのファイルにあるかが状態」（同 `:13-14`）。resolved 側の既存エントリ（配布済み `known-issues.resolved.yml:3-34`、`claude-dir-write-protection`）は
8 項目の後ろに `resolved_at`（引用符付きの日付）と `resolved_version` を持ち、dependents の各行の頭に「(廃止 2026-08-25)」「(実施済み 2026-08-25)」を付けている。同じ形にする。

### N-1. 移した後のエントリ（配布済みの一覧・雛形とも同じ本文）

現行の `dependents`（雛形 `known-issues.template.yml:13-20`、配布済み `known-issues.yml:13-19`。逐語）:

```yaml
  dependents:
  - claude-user-communication plugin の ask-with-choices skill (冒頭の AskUserQuestion
    使用禁止の警告とターン分割節)
  - claude-user-communication plugin の html-communication skill (複雑な確認を HTML フォームに寄せる適用基準)
  - '解除時の手順（旧 release_steps）: 1. ask-with-choices skill 冒頭の WARNING (AskUserQuestion
    使用禁止) を削除する 2. 同 skill の「AskUserQuestion を単独ターンで発行する (レンダリングバグ回避)」節を削除し、 ターン分割への言及を元に戻す
    3. html-communication skill の適用基準から、レンダリングバグを理由とした注記を外す 4. この台帳エントリの status を
    resolved にする'
```

書き換え後（`id` / `title` / `summary` / `affected` / `refs` / `how_to_verify` と既存の `log` 行はそのまま。`dependents` を差し替え、`log` に 1 行、末尾に 2 項目を足す）:

```yaml
  dependents:
  - "(廃止 2026-08-26、norm-refit PR 3) claude-user-communication plugin の ask-with-choices skill。
    AskUserQuestion を使わない構成に移り、skill ごと削除した"
  - "(実施済み 2026-08-26、norm-refit PR 3) claude-user-communication plugin の html-communication skill の適用基準。
    レンダリングバグを理由とした注記を外し、AskUserQuestion を使わずフリーテキストで質問する指定だけ残した"
  - "(norm-refit PR 4 で実施) 他 plugin の AskUserQuestion の使用指示 (impl-spec 16 / dotclaude 13 / mkdocs-setup 6 / session 1 の言及行)。
    フリーテキストと HTML フォームへ書き換える。実施したら log に足す"
  - '解除時の手順（旧 release_steps）: (実施済み 2026-08-26) 上の 3 件のとおり。バグの修正は待たない'
  log:
  （既存の行はそのまま）
  - '2026-08-26: AskUserQuestion を使わない構成へ移行し、依存が消えた（norm-refit PR 3）。バグの修正を待たずに resolved へ移す'
  resolved_at: '2026-08-26'
  resolved_version: 2.1.246
```

上の例の日付 2026-08-26 は PR 3 の実装日に置き換える（dependents の括弧内、log、`resolved_at` の 4 箇所）。`resolved_version` はそのときの `claude --version` の値。2026-08-26 の実測は 2.1.246（呼び出し元の指示にある 2.1.245 とは 1 つ違う。実装日に測り直す）。
`how_to_verify` の「未作成」はそのまま残す（resolved 側は全件突合の対象にならない。既存の resolved エントリも `how_to_verify` を持ったまま）。

### N-2. 雛形 `plugins/claude-known-issues/config/known-issues.template.yml`（PR に入れる）

雛形は `scripts/lib/state.sh:24-31` が、配布先に `known-issues.yml` が無いときに `cp` するだけの初期値。resolved 側の雛形は存在しない
（`config/` にあるのは `known-issues.template.yml` 1 本。2026-08-26 実測）。`state.sh` は resolved ファイルを作らない。

既定案は雛形からこのエントリ（`:3-52`、次のエントリ `task-tools-unavailable` の直前まで）を削除する（未特定 J、ユーザーに問う。呼び出し元の指示「resolved 側の雛形へ移す」と違う）。
理由は、雛形が新規インストール先の未解決の初期値で、解決済みの履歴は配布先ごとの記録だから。代案は `config/known-issues.resolved.template.yml` を新設して N-1 のエントリを置くことだが、
それを読むスクリプトが無いので、置いても配布先に届かない。届かせるなら `state.sh` の変更が要り、PR 3 の範囲を超える。

雛形の残りのエントリ（`task-tools-unavailable`）は変更なし。雛形のエントリ数は 2 → 1。

### N-3. 配布済みの一覧（PR の外の直接作業。PR 3 のマージ後に行う）

`~/.claude/plugins/data/claude-known-issues-cc-tools/` の `known-issues.yml`（6 エントリ）と `known-issues.resolved.yml`（1 エントリ）。

1. `known-issues.yml` の `askuserquestion-rendering`（`:3-` の 1 エントリ）を N-1 の形に書き換える（`dependents` の差し替え、`log` の追記、`resolved_at` と `resolved_version` の追加）
2. そのエントリを `known-issues.yml` から切り取り、`known-issues.resolved.yml` の `entries` の末尾へ移す
3. `grep -c resolved_at ~/.claude/plugins/data/claude-known-issues-cc-tools/known-issues.yml` が 0 であることを確かめる
4. `state.json` は触らない（`reviewed_version` / `pending_version` / `last_full_review_at` は review skill と hook の管轄）

`.claude/` 配下の書き込みは bypassPermissions なら直接 Edit できる（`entry/SKILL.md:22-23`）。

### N-4. 件数の記述

`CLAUDE.md:81` と `README.md:59` の「8 項目、未解決と解除済みを別ファイル」の 8 はエントリの項目数（`entry/SKILL.md:26`「8 項目。増やさない」）で、エントリの件数ではない。
resolved 化で変わらない。plugin README（`plugins/claude-known-issues/README.md`）も件数を書いていない。編集は version だけ（H）。

## H. plugin の release 作業

### H-1. claude-user-communication 0.32.6 → 0.33.0

skill の廃止と構成規定の変更なので minor を上げる。

- `plugins/claude-user-communication/.claude-plugin/plugin.json:3-4`
    - description 現行: 「ユーザーへの確認・提示のコミュニケーション方法。入り組んだ内容の HTML ページ提示 (claude-html-communication) と選択肢形式の確認 (ask-with-choices) の 2 skill + 提示前レビューの page-reviewer agent を提供する」
    - 書き換え後: 「ユーザーへの確認・提示のコミュニケーション方法。入り組んだ内容の HTML ページ提示 (claude-html-communication) の 1 skill + 提示前レビューの page-reviewer agent を提供する」
    - version: `"0.33.0"`
- `.claude-plugin/marketplace.json:64`
    - 現行: 「ユーザーへの確認・提示のコミュニケーション方法。入り組んだ内容の HTML ページ提示 (claude-html-communication) と選択肢形式の確認 (ask-with-choices) の 2 skill を提供する」
    - 書き換え後: 「ユーザーへの確認・提示のコミュニケーション方法。入り組んだ内容の HTML ページ提示 (claude-html-communication) の 1 skill を提供する」
- `plugins/claude-user-communication/README.md:3-8`
    - 行 3「ユーザーへの確認・提示のコミュニケーション方法を定める plugin。2 つの skill を提供する。」→「ユーザーへの確認・提示のコミュニケーション方法を定める plugin。1 つの skill を提供する。」
    - 行 5-6 の html-communication の項目に「HTML フォームの設問の作りと回答の受け取り」を足す
    - 行 7-8（ask-with-choices の項目）を消す
- `CLAUDE.md:80`
    - 「| claude-user-communication | 0.32.6 | communication | HTML ページ提示 (claude-html-communication) + 選択肢形式の確認 + 生成ページの機械検査 15 種 + …」
      → version を 0.33.0 に、「+ 選択肢形式の確認」を消す
- `README.md:53`
    - 「| claude-user-communication | 0.32.6 | ユーザーへの確認・提示。HTML ページ提示 (claude-html-communication) + 選択肢形式の確認の 2 skill。…」
      → version を 0.33.0 に、「HTML ページ提示 (claude-html-communication) + 選択肢形式の確認の 2 skill」を「HTML ページ提示 (claude-html-communication) の 1 skill」に
- `README.md:113` の install 行は変更なし

html-communication の `SKILL.md` の description（発動条件）は変えない（ccm-f051 Q3 で確定）。evals の実行は要らない。

### H-2. claude-known-issues 0.3.1 → 0.3.2

雛形の変更（N-2）だけなので patch を上げる（既定案）。

- `plugins/claude-known-issues/.claude-plugin/plugin.json:4` version: `"0.3.2"`
- `CLAUDE.md:81` の version を 0.3.2 に。概要は変更なし（N-4）
- `README.md:59` の version を 0.3.2 に。概要は変更なし

### H-3. rules

- `git mv rules/propose-before-implement.md rules/user-confirmation.md` と R-2 の内容
- `README.md:82-91` の rule 一覧表に行を足す（未特定 J。既定案は足す）:
  `| user-confirmation | 常時 | ユーザーへの確認。提案と実装の分離、手段が変わったら確認に戻る、何を問い何を問わないか、回答後の進め方 |`
- `rules/japanese-text-writing.md:18-19` は R-4 の判断に従う

rule は symlink で即時反映される。改名したファイルはセッション開始時に読まれるので、次のセッションから効く（`rule-authoring.md:43-44`）。

### H-4. マージ後

`plugin-release.md` の 4〜6: commit + push、`claude plugins marketplace update cc-tools`、
`claude plugins update claude-user-communication@cc-tools`、`claude plugins update claude-known-issues@cc-tools`。
その後に N-3 の配布済みの一覧の直接作業。

## I. 実装時の注意

### I-1. 申し送り（`norm-audit-implementation-notes.md` から PR 3 に係る 3 件）

1.（`:12-16`）DG40 の本文化を AQ 廃止より先に → B と R。同一 PR 内の commit 順で満たす

6.（`:43-46`）HC5 の字句から AQ への参照を落とす → D

13.（`:100-104`、逐語）

> cuc と session は「指摘 0 件まで繰り返さない。もう 1 度だけ」へ移行済みだが、
> impl-spec 3 skill は「指摘がゼロになるか、最大 5 回に達したら終了する」。
> 移設前に揃えるか、移設後に idea-hub の PR 8 で揃えるかを決める

ccm-f051 Q5 で PR 5 の対象に足すと決まり、計画 `norm-refit-plan.md` の PR 5 節に反映済み。PR 3 では触らない（L）。

4.（AskUserQuestion の除去は言及行だけ）と 5.（SS18 / SS55 の簡素化）は PR 4 の管轄で、PR 3 では触らない。

### I-2. 判定ファイル側の申し送りで PR 3 に係るもの（逐語）

- `norm-audit-verdict-dg35-49.md:97-98`「**DG40 の移設は AQ 廃止の前提条件**。HC の「回答の受け取り」節が AQ を参照しているので、先に本文化しないと AQ 廃止で手順が消える」→ B / R
- `norm-audit-verdict-dg18-34.md:101`「**HC5 の字句修正**。AQ 廃止で参照先が消えるので、基準だけ残して参照を落とす」→ D
- `norm-audit-verdict-typespec.md:107`「**B 群の分割 10 件は、割った後の文が単独で読めることを確かめる**」→ F-1 / F-2
- `norm-audit-verdict-lift.md:55-59`「**一般化が要る**。impl-spec 固有の語（Phase 2・Phase 3）を落とす。文案は下記」→ C-2 の「実装で書く文」がその文案を基にしている
- `norm-audit-verdict-auto.md:52-57`「実装で条項を触るとき、原文を読んで違和感があれば止めて吟味側へ回す」→ A-1 で全件読んだ。該当なし。
  HC147 は「前提が変わった」に当たり、D で扱う

### I-3. norm-refit-ops の手順

- PR を出す前に、台帳の直近の確定（PR #10 = PR 2 のマージ以降に積んだエントリ）を列挙し、反映先ごとに実装と突合する。
  この明細を書いた時点（2026-08-26、台帳 `notes/norm-refit.md:1506-1894`）で PR 3 を反映先に持つ確定は 5 件
  （`:1740` ccm-f051 = B / C-4 / H / N、`:1824` 構成変更 = R、`:1847` と `:1866` 見出し語 = M、`:1881` resolved 化 = N）。
  他は反映済みか plugin への反映なし。PR を出す時点で再度数える
- セルフレビュー: markdownlint（`rules/user-confirmation.md`、`plugins/claude-user-communication/README.md`、`SKILL.md`）→ 相対リンクの解決
  （`SKILL.md` 内の `../ask-with-choices/` が 0 件、`./references/` と `../../agents/` が実在）→ review agent（観点は規範の欠落と実文の改変。
  R-2 の既存 2 節が R-1 と一字一致することを含める）→ 指摘を直す → PR
- レビュー指摘の一般化の検討と、文レベルの修正の `sentence-level-review-cases.md` への逐語追記（出所付き）
- PR に notes を混ぜない。台帳・計画・`ccm-r003` の更新は main へ直接
- rule の変更を含むので、PR ブランチを checkout している間は自分のセッションに載る規範が変わる（`.claude/rules/norm-refit-ops.md`「作業ブランチの注意」）。
  改名後の rule はセッション開始時に読まれるので、ブランチ上での動作確認は新しいセッションで行う

### I-4. 検査している側

`plugin-release.md`「規範を変えたら、それを検査している側を一緒に直す」に従って grep した結果（HC92 / HC165 / HC221 は 2026-08-25、見出し語は 2026-08-26）。

- HC92 / HC165 / HC221: F-3 と E に書いたとおり、検査スクリプト・page-reviewer・review-norms に該当なし。雛形のコメントだけ
- 冒頭ブロックの見出し語: M-3。判定に「結論」の文字列を使う検査は無く、コメント 2 箇所を直す
- AQ 廃止: 検査スクリプトは AQ を参照しない（A-2 の grep）

### I-5. 観測（PR 3 では触らない）

- HC:297-298「user global rule「日本語テキストの執筆要点」が AI 口調の定型として禁じており、適用範囲に HTML 報告が含まれる」は
  入口 rule の現行タイトル「日本語テキストの執筆規範」と食い違う。HC160 系の `維持` 条項の中の参照で、PR 3 の項目のどれにも属さない。
  plugin 自己完結（`plugin-design.md`「rule の存在を暗黙前提にしない」）にも反するので、別途直す候補として台帳の未解決課題に載せる。
  `templates/page.html:94`「（user global rule が AI 口調の定型として禁じている。HTML 報告も適用範囲）」も同型
- `norm-refit-impl-inventory.md:71-74`（2026-08-15 の棚卸し）の C2「AQ8 を設問設計節へ移設」「表規範 2 件（L177-179）を削除し core を参照」と
  C3「`references/review-norms.md` L49-51 の「」前提の verbatim 規範を新記法へ」は旧観測。判定は AQ8 `削除`（DG36）、移設は AQ18 / AQ36、
  表規範は F の 2 件、C3 は FN で T4 保留。棚卸しの値は引かない
- `README.md:82-91` の rule 一覧表は propose-before-implement / bash-state-mutation-isolation / skill-invocation の 3 行を欠く（2026-08-26 実測）。
  user-confirmation の行を足すとき（H-3）に、他の 2 行を足すかは PR 3 の範囲外
- 判定ファイル（lift / partial）の 13 件の置き先 `plugin`（HC）は、台帳 `:1824-1845` の構成変更で rule 側に変わったが、判定ファイルは書き換えない（C-1）

## J. 未特定（実装者が決める判断）

「ユーザーに問う」と付けたものは、実装前に 1 つのフォームにまとめて問う（norm-refit-ops）。
ccm-f051 で決着した 5 件（原文の複製 / 回答後のフロー / description / 既知バグ一覧 / 終了条件）は外した。

1. R-4: 入口 rule `japanese-text-writing.md:18-19` の 2 条項を新 rule の「問い方」へ移すか残すか。既定案は 2 条項とも移す。
   **ユーザーに問う**（台帳 `:1506` / `:1539` で入口に残すと決めた条項の移動）
2. N-2: 雛形 `known-issues.template.yml` の `askuserquestion-rendering` を削除するか、resolved 側の雛形を新設して移すか。既定案は削除。
   **ユーザーに問う**（呼び出し元の指示「resolved 側の雛形へ移す」と違う。resolved 側の雛形は存在せず、読むスクリプトも無い）
3. B: HC「回答の受け取り」を 1 文だけにするか、rule 側と同じ回答後のフローの 1 文を再掲するか。既定案は 1 文だけ（到達点 1 と、台帳 `:1824` の「plugin 側から rule への参照は置かない」）
4. D: HC147 を「ターミナルの描画の制約を受けない」に直すか、項目ごと消すか。既定案は直す
5. E: HC219「自分で作った語を説明なしに使わない」の「説明なしに」を落とすか。既定案は落とす（条件付き許可を消した後に残ると、説明すれば作ってよいと読める）
6. M-2: 雛形の見出し語の出し分けを、form 用を生かして report 用をコメントで並べる形にするか、`assemble-page.mjs --report` で置換するか。既定案は前者（既存の切り替え機構と同じ形）
7. G: 移し替え禁止の置き場。既定案は「適用基準」の末尾。代案は HC:243（構成の順）の直前の箇条書き
8. A-3: `docs/retired-plugins.md` に skill 廃止の記録を足すか。既定案は足す（同ファイルは plugin だけでなく version-check の機能廃止も載せている）
9. H-3: `README.md:82-91` の rule 一覧表に user-confirmation の行を足すか。既定案は足す
10. F-3: 雛形 `templates/page.html` のコメント 4 箇所（表・図を語る文）を書き換えるか。既定案は触らない
11. A-2: パターン集の例示データ `progress-tree/example.html:15`「ask-with-choices の廃止」を書き換えるか。既定案は触らない（例示データ。残骸 grep の除外として A-3 に書いた）
12. R-2: 末尾の「## why」を第 1 節の直下の「why:」段落へ移す形にするか、末尾に残すか。既定案は移す（節が 6 つになり、末尾では何の why か分からない）

## K. PR 3 の編集箇所の一覧

commit の順序: R → B → D / E / F / G → A（削除）→ M（別 commit）→ N → H。R と B が A より前であれば他は任意。

| ファイル | 節・行 | 操作 | 項目 |
| --- | --- | --- | --- |
| `rules/propose-before-implement.md` → `rules/user-confirmation.md` | 全体 | `git mv` と R-2 の内容（冒頭・見出し・why の位置・新 4 節） | 1、2 |
| `rules/japanese-text-writing.md` | 18-19 | R-4 の判断で移す（J-1） | 1 |
| `plugins/claude-user-communication/skills/html-communication/SKILL.md` | 573-578「回答の受け取り」 | 1 文に縮める（B） | 2 |
| 同 | 15-16（HC5） | 相対リンクと時限条件を落とす（D） | 3、4 |
| 同 | 266-267（HC147） | 相対リンクとバグの前提を落とす（D） | 3、4 |
| 同 | 406-409（HC219〜HC221） | 条件付き許可を消す（E） | 6 |
| 同 | 175（HC92） | 1 文を消す（F-1） | 7 |
| 同 | 316-319（HC165） | 器の指定 1 文だけ残す（F-2） | 7 |
| 同 | 12-16「適用基準」の末尾 | 段落を足す（G） | 8 |
| 同 | 243-245 / 259 / 288 / 501 | 見出し語「まとめ」（M-1。別 commit） | 5 |
| `plugins/claude-user-communication/skills/html-communication/templates/page.html` | 7-10 / 355-358 / 376 | 見出し語の出し分け（M-2。別 commit） | 5 |
| `plugins/claude-user-communication/skills/html-communication/scripts/check-page.mjs` | 199（コメント） | 「冒頭のまとめブロック」（M-3。別 commit） | 5 |
| `plugins/claude-user-communication/skills/html-communication/scripts/renumber-refs.mjs` | 8（コメント） | 同上 | 5 |
| `plugins/claude-user-communication/skills/ask-with-choices/SKILL.md` | 全体 | ディレクトリごと削除（A-3） | 3 |
| `plugins/claude-user-communication/.claude-plugin/plugin.json` | 3-4 | description と version（H-1） | 3 |
| `plugins/claude-user-communication/README.md` | 3、5-6、7-8 | 1 skill 化（H-1） | 3 |
| `plugins/claude-known-issues/config/known-issues.template.yml` | 3-52 | エントリを削除（N-2、J-2 の既定案） | 4 |
| `plugins/claude-known-issues/.claude-plugin/plugin.json` | 4 | version 0.3.2（H-2） | 4 |
| `.claude-plugin/marketplace.json` | 64 | description（H-1） | 3 |
| `CLAUDE.md` | 80、81 | version と概要（H-1、H-2） | 3、4 |
| `README.md` | 53、59、82-91 | version と概要、rule 一覧の行（H-1、H-2、H-3） | 1、3、4 |
| `docs/retired-plugins.md` | 末尾 | 1 件足す（A-3、J-8 を採るとき） | 3 |

※ 表 1 PR 3 の編集箇所。「項目」は計画 `norm-refit-plan.md:333-355` の PR 3 節の箇条書きの番号（1 = 媒体非依存の確認規範を rule へ、2 = DG40 の本文化、
3 = AQ skill の削除、4 = 既知バグ一覧の resolved 化、5 = 冒頭ブロックの見出し語、6 = 段階 2 Q4、7 = HC92 / HC165、8 = HTML への移し替え禁止）

PR の外で行う作業（PR に混ぜない）:

- `~/.claude/plugins/data/claude-known-issues-cc-tools/` の `known-issues.yml` → `known-issues.resolved.yml` の移動（N-3。PR 3 のマージ後）
- `notes/idea-hub-handoff.md` の追記は済み（`:60-67`）。台帳のエントリ（未特定の決着分）、`norm-refit-plan.md` の現在地、`ccm-r003` は main へ直接

## L. PR 3 で触らないもの

- PR 4 の管轄: AskUserQuestion の使用指示（impl-spec `requirements/SKILL.md:52,110,119,178,245` / `design/SKILL.md:75,148,159,199,277` /
  `test-plan/SKILL.md:93,98,155,170,277,347` の 16 行、dotclaude 13、mkdocs-setup 6、session 1。計画 `:359-362`）、SS18 / SS55 の簡素化。
  既知バグ一覧のエントリは PR 3 で resolved へ移すので（N）、計画 `:369`「既知バグ台帳の `askuserquestion-rendering` を更新する」は PR 4 では resolved 側の `log` への追記になる。
  C-2 の IS57 の原文行（`requirements:178` / `design:199` / `test-plan:277`）は PR 4 の対象行でもあり、PR 3 は複製するだけなので衝突しない
- PR 5 の管轄: review agent の責務（`page-reviewer.md:112` の「どう直すか」、IS335 / IS340 の引き上げ）、
  impl-spec の spec-reviewer と 3 skill の終了条件（`requirements/SKILL.md:192` / `design/SKILL.md:214` / `test-plan/SKILL.md:291`。ccm-f051 Q5）。
  HC:524-530「反復の終了条件」も PR 5 が「改善案が返らない前提へ合わせる」で触る
- impl-spec の移設分: `移設先` と判定された 200 件超と内部重複 91 条項（`norm-refit-plan.md:468-472`、idea-hub の PR 8）。
  C-2 の 13 件の原文と他 ID 14 箇所もそのまま（C-4）
- T4 送り: FN（`review-norms.md`）全件を含む 18 件。E で触れた FN の新語 2 条項、`norm-refit-impl-inventory.md:73` の C3
- core と分類別の詳細規範: PR 3 で `rules/japanese-text-writing/references/` に足す文・消す文は無い。F と E は core が既に持つ文への一本化で、core 側は変更なし。
  `rules/` の変更は user-confirmation の新設（改名）と、R-4 の判断で入口 rule の 2 行だけ
- HC の「確認（設問）を含めるときの作り」（HC:452-486）: HTML フォーム固有で変更なし
- HC:297-298 と `page.html:94` の rule タイトルの食い違い（I-5）
- 既存ページ（共通ページディレクトリ）の見出し語「結論」（M-3）
