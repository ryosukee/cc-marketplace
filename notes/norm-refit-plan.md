# norm-refit: 実装計画

norm-refit をどの順で進めるかの正典。何をどこまでやるか、依存が何か、次に何をするかは
この文書だけを見れば分かる。

2026-08-20 に全面改訂した。条項 1349 件の全数監査（`notes/artifacts/norm-audit-*`）で
前提が変わったため、旧版の段階 0〜4 の区分ごと組み直している。
旧版は `git show b2a0a7b:notes/norm-refit-plan.md` で読める。

役割の分担は次のとおり。この文書に決定の経緯や決め手を書かない。進行状態も書かない。

| 置き場 | 持つもの |
| --- | --- |
| この文書 | 到達点、段階の定義、作業の一覧と順序、依存、待ち条件 |
| [norm-refit: 確定事項メモ](./norm-refit.md) | 何をどう決めたか、決め手、出典 |
| `notes/artifacts/norm-audit-raw/` | 条項 1349 件の抽出と、既存規範との突合 |
| `notes/artifacts/norm-audit-verdict-*.md` | 条項ごとの処遇の判定 |
| `notes/artifacts/norm-audit-implementation-notes.md` | 実装で踏む注意 15 件 |
| task list | いま何に取りかかっているか |

## 到達点

norm-refit が終わった状態を、検証できる形で定義する。段階 5 の完了時点でこの 6 つが成立する。

| # | 到達点 | 検証方法 |
| --- | --- | --- |
| 1 | 汎用規範の実体が 1 箇所にある | 重複グループ 47 の処遇が全件実装済み。`core.md` と種別 3 ファイルに実体があり、plugin 側に汎用の再実装が残らない（条件ロードの穴を埋める分を除く。下記 4 を参照） |
| 2 | 現行規範どうしの矛盾がゼロ | 判明している 5 件が決着し、実文に反映されている |
| 3 | 層の役割が実装されている | 入口 rule 約 20 行 / 詳細規範 4 ファイル / 条件ロード / plugin の 4 層に条項が配られ、常時ロードは 9 ファイルのまま |
| 4 | 条件ロードの穴が塞がっている | 記法規範が PR 本文・HTML ページ・確認フォーム・ターミナル返答の 4 媒体に届く。届かせる手段は段階 2 で決める |
| 5 | impl-spec の汎用分が引き上げ済み | 移設先へ同行させる分と cc-marketplace へ残す分が分かれ、引き上げ分が詳細規範に実装されている |
| 6 | 監査の対象外だった 18 ファイルが整理済み | 後続タスク #7 の完了。norm-refit の終了条件には含まない（下記「終了条件に含まないもの」） |

到達点 6 だけが終了条件の外にある。1〜5 が成立した時点で編集凍結が解け、norm-refit は完了する。

## 現在地

2026-08-20 時点。**段階 1・2 が完了。段階 3（実装）に着手できる。**

- 条項 1349 件の抽出と検算が完了（`norm-audit-raw/` の 6 ファイル）
- **1349 件すべてに処遇が付いた**（`norm-audit-verdict-*.md` の 8 ファイル）。
  名簿 `norm-audit-roster.md` から ID ごとに判定ファイルを引ける
- 段階 2 の 6 件が `ccm-f030` で決着した（2026-08-20）。判定への反映も済み。
  残る `保留` は T4 送り 18 件と RD20 の 1 件だけ
- PR #4 が draft で開いている（`norm-refit/pr1-detailed-norms`、未マージ）

判定の内訳（名簿から実測）。

| 判定の付き方 | 条項 |
| --- | ---: |
| 機械的に決まる | 663 |
| 重複グループ 47 | 322 |
| 種別固有ほか | 121 |
| 部分的 | 88 |
| 既存なし | 86 |
| 既存あり | 70 |

重複グループ 47 の判定は旧明細に対して追認 113 / 訂正 24。

## 段階の全体像

| 段階 | 中身 | 到達点 |
| --- | --- | --- |
| 1 | 判定を閉じる | 1349 条項すべてに処遇が付く |
| 2 | 決着が要る 6 件 | 実装に着手できる。ユーザー判断を含む |
| 3 | 実装（PR 群） | 到達点 1・2・3・5 が成立する |
| 4 | T4 の設計と実装 | 到達点 4 が成立し、保留 18 件が解ける |
| 5 | 完了処理 | 編集凍結が解け、norm-refit が終わる |

段階 3 と段階 4 は時間軸で重ねられる。T4 の設計は実装の成果物に依存しない。
実装の完成を待つのは段階 5 の facet 参照の切り替えだけ。

段階の所要は書かない。見積もりの根拠が無く、推定を置くと根拠のない数字が独り歩きする。

## 段階 1 判定を閉じる

残る 365 条項に処遇を付ける。突合は完了しており、材料は揃っている。

作業は 4 つ。すべて機械的で、新しい判断は生じない。

### 1-1 既存ありの 70 件を閉じる

`is-generic-screening.md` と `plugin-generic-screening.md` の `既存あり` 分類。
同趣旨が既存規範にあるので、処遇は次で一意に決まる。

- 実体が詳細規範側にある → 条項は `削除`（plugin から消す）。ただし plugin が
  自己完結原則で持つ必要があるものは `維持`
- 実体が plugin 側にしかない → その plugin が実体。`維持`
- 実体が impl-spec 側にある → `移設先` へ同行

判定基準: その plugin が単独でインストールされたとき、この条項が無いと壊れるか。
壊れるなら `維持`、壊れないなら `削除`。

### 1-2 既存なしの 85 件を閉じる

引き上げ候補。突合が `移設先の候補` を付けているので、それを検証して確定させる。

- 候補が `core` / `判断` / `参照` / `読み物` → `移設`。一般化の要否が付いているものは
  文案を実装明細へ書く
- 候補が `据え置き` → `維持`
- impl-spec 由来のものは、引き上げる（cc-marketplace に残す）か
  同行させる（`移設先`）かを 1 件ずつ決める。**これが段階 1 の主作業**

### 1-3 部分的の 92 件を閉じる

既存に一部あるが、限定・条件・判定基準が足りないもの。処遇は 2 通り。

- 足りない部分だけを既存へ追記 → `統合`。追記する文案を実装明細へ書く
- 既存とは別の規範として立てる → `移設`

判定基準: 足りない部分を既存の条項へ足したとき、既存の条項が読めなくなるか。
なるなら別に立てる。

### 1-4 種別固有 60 件・判断保留 26 件・MF 媒体固有 32 件を閉じる

`type-specific-screening.md` の 3 群。

- A 群（種別固有 60）: 行き先候補がファイル単位で付いている。そのまま `移設`。
  EW と NW の統合で 1 本になる 4 組は `統合`
- B 群（判断保留 26）: 突合が「汎用寄り / 媒体固有寄り / 分割 / 執筆規範の外」に分類済み。
  `分割` の 10 件は条項を 2 つに割り、汎用側を `移設`、媒体固有側を `維持`
- C 群（MF 媒体固有 32）: `markdown 記法固有` 17 件は `維持`。
  `記法一般` 9 件と `混在` 6 件は段階 2 の条件ロード設計の対象

### 完了条件

1349 条項すべてが処遇と置き先を持ち、集計が取れる。
`保留` が残ってよいのは、段階 2 の 5 件と T4 送りの分だけ。

## 段階 2 決着が要る 6 件

**完了（2026-08-20、ccm-f030）。** 決定の内容と決め手は台帳のエントリにある。

| # | 論点 | 決定 |
| --- | --- | --- |
| 1 | 表の補足の置き場 | 媒体条件を付けて両立 |
| 2 | 「決め手」の名指し | html-communication はそのまま。他では言及せず削除 |
| 3 | 地図の「読み方」 | 落とす |
| 4 | 新語の可否 | 全面禁止を維持 |
| 5 | レビューの改善案 | 全 agent が「指摘まで」 |
| 6 | 記法規範の到達 | 媒体非依存分を詳細規範へ移す |

2・4・5 は既存の実装を変える。html-communication の新語の条件付き許可を禁止へ戻し、
handover-reviewer と page-reviewer から「どう直すか」を落とす。

以下は決着前の記録。論点の中身を引くときに使う。

### 2-1 矛盾 5 件の決着

現行の規範どうしが逆を向いている。実文の突合で確認済み（`norm-audit-raw/README.md`）。

| # | 対立 | 検討済みの案 |
| --- | --- | --- |
| 1 | 表の補足を直下に置く（`reference-docs.md:19`）/ 置かない（`html-communication/SKILL.md:289`） | 媒体条件を付けて両立。「表の直下か、媒体が別枠を持つならそちら」 |
| 2 | 「決め手」を名指しする（`decision-docs.md:17`）/ 語を使わない（`SKILL.md:285`） | 両立。DD 側に「媒体が視覚表現を持つ場合はそちらに委ねてよい」を足す |
| 3 | 地図に「読み方」を含める（`decision-docs.md:16`）/「読み方」の節を書かない（`SKILL.md:263`） | 未検討 |
| 4 | 独自の新語を作らない（`japanese-text-writing.md:20`）/ 作ってよいが初出で定義（`SKILL.md:358`） | 実際の失敗事例を根拠に一方へ統一する |
| 5 | 改善案の提示は呼び出し元の責務（`spec-reviewer.md:96`）/ どう直すかまで書く（`handover-reviewer.md:105`・`page-reviewer.md:112`） | 倒す方向を決める |

1 と 2 は媒体条件で両立させる案が立っている。3・4・5 は倒す方向を決める必要がある。
5 件とも、決着しないと同じ規範が 2 通りに書かれた状態が実装後も残る。

5 件目は 2026-08-20 に追加した。一度「衝突でない」と判定したが、検索語を `改善案|修正案` に
限定して実際の対立文言「どう直すか」を取り逃がしていた。語で探して 0 件だったことを、
規範が無いことの根拠にしてはいけない実例。

### 2-2 条件ロードの穴を塞ぐ設計

`rules/markdown-formatting.md` は `paths: **/*.md` の条件ロードで、
markdown ファイルを扱うときしか読み込まれない。PR 本文・HTML ページ本文・確認フォーム本文・
ターミナル返答の 4 媒体に届かない。

github-pr はこれを自覚してトンマナ規範を持ち直している
（`generate-body.md:63`「PR 本文は `.md` ファイルではないため、Markdown 向けの整形ルールが
自動適用されない」）。

該当する条項は 15 件（C 群の `記法一般` 9 + `混在` 6）と、汎用でありながら実体が
条件ロードの下にある 5 件（MF13〜MF16 の和欧混植、HC159）。

穴を塞ぐ手段の候補は次の 3 つ。どれを採るかで、GP / HC のトンマナ再実装を削れるかが決まる。

- `paths` を広げる。ただし常時ロードに近づき、総量が増える
- 記法規範を媒体非依存の分と markdown 固有の分に割り、前者を詳細規範へ移す
- 各 plugin が持ち直す現状を追認し、条項として正当化する

**この設計が決まるまで、GP / HC のトンマナ条項は `保留` のまま実装しない。**

候補は 2 つ足りていなかった。生成物を検査する側に判定を足す案が 2 通りある。

- 機械検査で事後に拾う（markdownlint plugin・page-reviewer の機械検査に足す）。
  読み込みに依存しないが、生成時には効かず、意味判断が要る条項を拾えない
- review agent で事後に拾う。意味判断が要る条項も拾えるが、agent の起動コストがかかる

**20 件のうち 6 件は条項を割る必要がある**（MF5 / MF9 / MF10 / MF21 / MF32 / MF44）。
C 群の判定で「混在」とされたもので、1 条項の中に記法一般の核と markdown 固有の値が同居している。
B 群の分割 10 件と同じ扱い（汎用の核を移設、媒体固有の値を維持）にする。
単純に 20 件をまるごと移す形にはならない。

この項目は `ccm-f030` の設問 6 として出した。**T4 へ送らない。**
段階 2 で直接決める（この節の定義どおり）。

## 段階 3 実装

### PR #4 の扱い

判定結果と PR #4 の内容を突合し、採否を決める。

- 判定と一致する部分 → 活かす
- 判定と食い違う部分 → 直す
- 判定の前提が変わった部分 → 作り直す

**作り直しはユーザー承認済み**（2026-08-19）。突合の結果、作り直しのほうが安いと判断したら
ブランチごと切り直してよい。

判定で分かっている食い違いは次のとおり。

- `.claude/rules/coding.md` の 5 行の変更は、監査を経ていない機械的修正。後続タスク #7 で見直す
- `user-communication-format.md` の 42 行削除は判定と一致（`削除` / 現在地）
- 詳細規範 4 ファイルの新設は判定と一致するが、**中身が監査前のもの**。
  段階 1 の判定で `core` へ移すと決まった 36 件以上が反映されていない

### PR の切り方

旧版は PR 4 本に固定していた。監査で条項数が 4 倍になったので切り直し、**5 本**にした。

切り方の原則は 2 つ。

1. **参照が切れる区間を作らない。** rule は symlink で即時反映、plugin は version bump と
   更新を経るため、反映タイミングが非対称。存在しない参照先を指す区間ができると、
   その間セッションが壊れる
2. **1 PR = 1 レビュー単位。** 人間がレビューできる大きさに収める

### PR ごとの中身

直列で、順序を入れ替えられない。

#### PR 1 詳細規範の新設と plugin 解体

最大の PR。詳細規範 4 ファイル（core / 参照 / 判断 / 読み物）を新設し、
japanese-text-writing plugin を解体する。

- `core` へ移す 38 件（移設 25 + 統合 13）と、種別 3 ファイルへ移す 20 件を実文にする
- 入口 rule `rules/japanese-text-writing.md` を 2 節・約 20 行へ圧縮する
- `rules/user-communication-format.md` を削除し、判断の件数の 2 点を入口 rule へ移す
- plugin 6 ファイルと marketplace エントリ・README・CLAUDE.md を消す
- 段階 2 の決定を実文へ反映する。地図から「読み方」を落とす（Q3）、
  `decision-docs.md` の「決め手の観点を本文で名指しする」を削除する（Q2）
- 保持規則 L1 へ統合する条項（DG6 / DG9 / DG17）はこの PR で同時に移す

#### PR 2 記法規範の分割

Q6 の決定を実装する。`markdown-formatting.md` から媒体非依存分を core へ移す。

- 記法一般 9 件 + 和欧混植 4 件 + HC159 を core へ移す
- 混在 6 件（MF5 / MF9 / MF10 / MF21 / MF32 / MF44）は核を割って core へ、
  markdown 固有の値は現在地に残す
- markdown 固有 17 件は条件ロードのまま
- `markdown-formatting.md:9-10` の参照文を新しい詳細規範のパスへ直す

**PR 1 の後にしか置けない。** 移し先の core が存在してからでないと参照が切れる。

#### PR 3 確認・質問の一本化

ask-with-choices skill を廃止し、html-communication へ寄せる。

- DG40 の本文化を先に行う。HC の「回答の受け取り」節が AQ を参照しているので、
  本文化しないと AQ 廃止で手順が消える
- impl-spec から引き上げる確認の規範 13 件（1-2 の 3 件 + 1-3 の 10 件）を HC へ移す
- AQ skill と marketplace エントリを消す
- 段階 2 Q4 の決定を反映する。HC の新語の条件付き許可を禁止へ戻す

#### PR 4 AskUserQuestion の除去

全 plugin から使用指示を消す。**16 箇所**（impl-spec / dotclaude / mkdocs-setup /
session / claude-known-issues）。

- 除去対象は AskUserQuestion への言及行に限定する。impl-spec の「質問の原則」
  「品質基準」節には DG17 系の条項が同居しており、節ごと消すと巻き込む
- 確認手段を「フリーテキスト。設問が多い確認は HTML フォーム」へ書き換える
- `SS18` / `SS55` の字句を簡素化する（ツール名指しの否定を落とす）
- 既知バグ台帳の `askuserquestion-rendering` を更新する

#### PR 5 review agent の責務統一

段階 2 Q5 の決定を実装する。

- `handover-reviewer.md:105` と `page-reviewer.md:112` から「どう直すか」を落とす。
  指摘 1 件の構成は「何が問題か」「なぜそう言えるか」の 2 点になる
- impl-spec から IS335（入力文書側の修正も指摘する）を両 agent へ引き上げる
- 呼び出し元（handover skill / html-communication skill）の記述を、
  改善案が返らない前提へ合わせる

### PR に入れないもの

- **impl-spec の条項**（`移設先` と判定した 200 件超）。idea-hub の PR 8 で移設される。
  norm-refit では触らない
- **T4 送りの 18 件**。段階 4 の管轄
- **RD20**。参照固有か汎用かの判定が未了。段階 3 で参照ファイルの実文を書くときに決める

### 実装時の注意

`notes/artifacts/norm-audit-implementation-notes.md` の 15 件に従う。
順序を縛るもの 3 / 作業単位を縛るもの 3 / 文面を縛るもの 6 / 扱わないもの 1 / 決着していないもの 2。

## 段階 4 T4

保留 18 件が集まる先。段階 3 と並走できる。

### レビュー機構のゼロベース再設計

現行の op-review 実装は KAO 側と efso-document 側の 2 箇所にある。両方を読んで diff を取り、
新しい方を基準にする。専用 reviewer の廃止可否も検討する。

設計入力として、LLM によるレビューは可読性を判定できないという調査結果がある。

**監査で分かった追加の入力**: 機械検査と agent レビューで終了条件が分かれている。
機械検査は `total` が 0 まで反復、agent レビューは「指摘 0 件まで繰り返さない。もう 1 度だけ」。
判定可能性の違いによる正しい設計だが、**impl-spec だけが旧方式（最大 5 回）で取り残されている**。

### 情報デザイン norm 48 本の置き場

100 本の性格分類は汎用執筆 52 / HTML・フォーム固有 41 / プロダクト UI 固有 7。
実装が統合するのは汎用執筆 52 本で、残り 48 本がここの対象。

統合先は詳細規範 4 ファイルではない。idea-hub Q81 の用途 3 分割では
op-review リソースと作成時指針が候補だが、48 本をどう割るかは未確定。

boilerplate repo の廃止判断と norm 1 本単位の明細も、この項目に含めて扱う。

### 参考デザイン例の reference

html-communication skill の `references/patterns/` に実物がある（パターン 5 件）。
T4 が 41 本の置き場を決めるとき、このパターン集との関係を整理する。

### 独立の設計入力 2 件

質問駆動手順の採否と、外部言及の解消（T5 から合流）。

## 段階 5 完了処理

### facet 参照の切り替え

切り替え先は実装が作る詳細規範なので、この作業だけは実装のマージ後にしか置けない。

### 編集凍結の解除

切り替え直前に、写し元 commit `6dfddfc31b326d86c47d496646321551f07e5206` からの差分を取る。
差分が出たら凍結が破れているので、写しへ反映してから切り替える。

### 到達点の検証

到達点 1〜5 が成立していることを確認する。検証方法は「到達点」の表にある。

## 編集凍結

対象は product-boilerplate 側の norms 全体（100 本）と、実装が統合する汎用執筆分 52 本。

- 開始は段階 3 の実文に着手した時点
- 解除は段階 5 の facet 参照の切り替え時点
- 実効の担保は写し元 commit の記録による。基準点は
  `6dfddfc31b326d86c47d496646321551f07e5206`（2026-08-19 記録。`docs/norms/` は
  README を含めて 6 ファイル 915 行、作業ツリーはクリーン）

## PR の出し方

**適用範囲は norm-refit の段階 3・4・5 と、後続タスク。**

実装は必ず PR にする。main へ直接入れない。PR に notes を混ぜない（レビュースコープが膨れる）。
notes の更新は main へ直接入れる。

PR を出す前に、必ずセルフレビューを通す。順序は次のとおりで、飛ばさない。

1. 実装して、機械で確かめられるもの（lint・リンク解決・壊れた参照・実際に発火させる検証）を通す
2. review agent を起動してセルフレビューする。観点は、規範の欠落と実文の改変
3. 指摘を直す
4. PR を出す

レビューは PR ごとにゼロベースで行い、前の PR のレビュー結果を前提にしない。

## norm-refit の終了条件に含まないもの

- **後続タスク #7**（未監査の rule 18 ファイル 955 行の整理）。
  `.claude/rules/` 3 本は判定軸に「user global 側へ取り込めるか」を含み、
  取り込み先の最終形が決まってからでないと判定できない
- **段階 4 で扱わない後続 10 件**（旧版の段階 4）。paths 除外の破損検知、HTML 雛形の
  グルーピング、配色調査の置き場、悪文・造語の検知、日本語の構造的曖昧性、
  primary-sources-first の移管など
- **op-pr-review-assist の設計**。段階 4 のレビュー機構の再設計と合流させる
- **impl-spec の内部重複の解消**（原文完全一致 36 種・91 条項）。
  3 skill を抱えたまま product-workflow へ統合されるので、idea-hub の PR 8 で扱う

## この計画で捨てたもの

旧版から引き継がなかったもの。理由を残す。

- **段階 0〜4 の区分**。監査の結果に合わせて 5 段階へ組み直した。
  旧段階 0 は完了済み、旧段階 1 は本計画の段階 3、旧段階 2〜3 は段階 4〜5 に対応する
- **PR 4 本の固定**。条項数が 4 倍になり、切り直しが要る
- **A1〜A11 / B1〜B6 / C1〜C6 の記号体系**。監査で条項単位の ID が付いたので、
  ファイル単位の記号は粒度が粗すぎる。`notes/artifacts/norm-refit-impl-inventory.md` は
  参照しない（削除はしない。過去の判断の記録として残す）
- **「未確定」の節**。旧版は 3 件を挙げていた。並行の切り替え閾値と中間成果の扱いは
  段階 3・4 が並走できると確定したので消滅。編集凍結の実効担保は commit 基準点で決着済み。
  参考デザイン例の粒度は段階 4 へ送った
