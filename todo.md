外のセッションからの依頼など
内容はちゃんと確認してないので、解釈・咀嚼した上でユーザーと議論して判断すること

#  diffo でやりとりしている時はターミナルに重複の返答をする必要はない。「対応して回答した」レベルでよい。重複しない論点があるならターミナル出力してもよい
- ref-skill の plugin で作って install したい diffo の作業の時に必ず読み込む。今はこの１つだけだがおそらく今後増える気がする

# cc-marketplace への依頼

渡し先は `/Users/ryosuke/ghq_root/github.com/ryosukee/cc-marketplace`。
2026-08-28 から 08-30 のセッションで出たもの。1 つのプロンプトに束ねて渡す。

## 1. シーケンス図のパターン集入り

html-communication のパターン集へシーケンス図を足す。依頼文は
`~/Desktop/html-communication-sequence/PROMPT.md` に作成済み。

## 2. 全問未回答で差し戻されたフォームの index status の扱い

回答が全問未回答で返ってきたとき、index のエントリを `awaiting` のままにするか別の状態にするか。

## 12. html-communication のページで設問 pane を落とす事故

form を作るとき `<aside id="q-pane">` を本文に書き忘れると、3 pane のときに設問 pane が空のまま残る。
雛形にはコメントで存在するが、本文を書き起こすと落ちやすい。`validate-page.sh` が検出しないため、
検査 0 件でも壊れた状態で提示できてしまう。検査項目に足すか、`assemble-page.mjs` が form のときに
`#q-pane` の有無を確かめる形にしたい。

# cognitive-rhythm-writing とか実際どれくらい反映されてる？

# こういうところからも principle や norm を参考にしたい
- https://github.com/cursor/plugins/tree/main/pstack

# ask user question のレンダリング問題が解消されたから禁止じゃなくて良さそう。ただ元々あった 1 行手前にバッファ出力するのはいまだに必要そう、それは known-issues に入れてもいいかも

# こういう事例があった
  - core.md はこのセッションの前半（完了報告を書く前）に読み込んでいて、その内容がコンテキストに残った状態で PR 本文を書きました。書く直前に読み直してはいません。加えて、core.md の分類判定では PR 説明は「判断文書」に当たるのに、decision-docs.md は一度も読んでいません。いま読んで、本文全体をその規範で見直します。


# cc-marketplace に rule を 1 本追加してほしい。`rules/bulk-edit-verification.md` を新規ファイルとして作る。

## 何が起きたか

efso-document / IdP のスライド作成セッション（2026-08-27 〜 08-28）で、同じ形の失敗が 2 回起きた。

1 回目。`~/.local/share/claude-html-communication/index.html` の JS 配列 `entries` から 1 件を消す処理を書いた。
`(?:[^\n]*\n)*?` を `re.DOTALL` で使ったため、正規表現が配列の先頭から対象までをまとめて掴み、
71 件のうち 65 件が消えた。削除の後に実行した確認は「対象の文字列が消えたか」だけで、
残りが何件あるかは見ていない。バックアップ・git・Time Machine のいずれからも戻せず、
各ページの実体（`<title>` / generator の版 / radio の name）から再構成した。

2 回目。同じ index.html から status を抜き出す正規表現がブロックを跨いで一致し、4 件を取りこぼした。
その値をそのまま別セッションへ送っていて、指摘されるまで気づかなかった。

どちらも、編集の検証を「意図した効果が出たか」で済ませたことが原因。
副作用は同じコマンドが作ったものなので、そのコマンドが成功したことでは検知できない。

## 入れてほしい文面

既存の `rules/bash-state-mutation-isolation.md` は「状態変更と復旧を連結しない」がテーマで、
編集の検証とは別の軸になる。ファイルを増やしたくないなら既存へ節として足す形にもできるが、
新規ファイルで入れる判断になっている。

```markdown
# まとまったデータを機械的に編集したら、残るはずのものが残っているかで検証する

設定ファイル・台帳・配列など、複数の項目が並んだデータをスクリプトや正規表現で編集したとき、
「意図した変更が入ったか」だけを見て終わらない。「触るはずのないものが無事か」を、
全体の件数か一覧で確かめる。

- 判定基準: 編集の後に実行した確認は、消えてはいけないものを見ているか。
  変更対象だけを見ているなら、まだ検証していない
- 件数は編集の前に数えておく。後から数えても、減ったことに気づけない
- 1 件を消す編集では「残りが N-1 件あること」を確かめる。「その 1 件が消えたこと」では足りない

## 構造の一部を正規表現で取り出さない

`.*?` や `(?:...)*?` は非貪欲でも、探している目印がその区間に無ければ境界を越えて伸びる。
項目が並んだデータから 1 項目だけを取り出す・消すつもりの式が、
先頭から目的の項目までをまとめて掴むことがある。

- パーサがあるならパーサを使う。JSON・YAML・HTML はいずれも標準のパーサがある
- パーサが無い形式では、項目の区切りで先に分割してから、分割された断片の中だけを検索する
- 正規表現で通すなら、取り出した断片が 1 項目に収まっているかを確かめてから使う

## why

編集の検証を「意図した効果が出たか」で済ませると、意図しない副作用は検証の外に落ちる。
副作用は同じコマンドが作ったものなので、そのコマンドの成功だけでは検知できない。
```

## 一緒に諮って見送った 2 件

同じ確認フォーム（eidp-f016）で 3 件を諮り、上の 1 件だけを採用した。残り 2 件は「入れない」。

- 測定の除外条件の一意性（`rules/primary-sources-first.md` の「測ったものと本当に測りたかったものが
  同じかを確かめる」の下へ追記する案）。スライドの下端の空きを測るスクリプトで、
  地色を全面に敷いた枚を外すための条件「空きが 0」が、下端まで溢れた枚とも一致し、
  5 枚が溢れているのに「溢れなし」と 2 回報告した
- 指定された具体値の扱い（`rules/propose-before-implement.md` への追記案）。
  指定されたスライドのタイトルを自分の案に差し替え、その事実をターミナルの返答にだけ書いた。
  提示前のレビューで指摘されて戻した

どちらも rule にはしない判断なので、この依頼には含めない。記録として書いてある。



# cc-marketplace セッションへの依頼: シーケンス図を html-communication のパターン集とギャラリーへ入れる

このファイルの中身をそのまま cc-marketplace のセッションへ渡してください。

---

claude-user-communication plugin の html-communication skill に、シーケンス図のパターンを足すか
どうかを検討してほしい。efso-document の to-be/idp セッションからの持ち込みで、判断は
そちらに委ねる。以下は背景と、参照できる実物の所在。

## 背景

認証経路の比較を説明する確認フォームで、シーケンス図が要った。登場人物 4〜6、矢印 10〜20 本、
番号付き、図の中に「問題:」で始まる帯を置いて弱点を名指しする形。パターン集に該当するものが
無かったので、Python で SVG を生成して雛形へ埋め込んだ。ユーザーから「このシーケンス図は
ギャラリーに追加していいな」と要望が出ている。

## 参照できる実物

`~/Desktop/html-communication-sequence/` に一式を置いた。scratchpad ではないので消えない。

- `README.md`: 各ファイルの説明と使い方
- `seq_core.py`: 生成の本体（外部依存なし。Python 3 の標準ライブラリのみ）
- `seq6.py` / `seq5.py`: 図の定義の例
- `f005-figs6.html` / `f005-figs6.css`: `python3 seq6.py` の出力。この一式だけで再現できる

ページに載った状態は次の 2 つで見られる。

- `/Users/ryosuke/.local/share/claude-html-communication/eidp-f005.html`（図 6 枚。認証の 4 案の比較）
- `/Users/ryosuke/.local/share/claude-html-communication/eidp-f017.html`（federation proxy と
  Transaction Tokens の Basic Flow の対応。作成中の場合あり）

## 決めてほしいこと

1. パターン集に「シーケンス図」を入れるか。入れるなら生成手段をどう持つか
    - skill の資産としてスクリプトを持つ（他のパターンと違い実行が要る点をどう扱うか）
    - Mermaid など別の手段に寄せる（雛形は self-contained が要件で、外部 CDN を読めない）
    - CSS とマークアップの型だけを示し、SVG は作る側が用意する
2. `gallery.html` に載せるなら、生成物である図をどう載せるか。他のパターンは静的なマークアップで完結している
3. 図の中のテキストを機械検査の対象にするか。いまの検査は `<style data-scope="figures">` を対象外に
   しているが、SVG 内の `<text>` が対象に入るかは決まっていない
4. パターン化するなら、図の色の既定と凡例の形。skill は「図の中は色を自由に使ってよい。凡例と字面を
   併記し、図の外へ持ち出さない」と定めているので、その枠内で既定を決められる

## 補足

図の題材（認証経路）は efso-document 固有だが、形（縦線・番号付きの矢印・注記の帯）は他の
プロジェクトでも使える想定。生成スクリプトは 3 セッションにまたがって作り直しが起きており、
skill 側に置けばその重複が消える。


# cc-marketplace の norm-refit に、スライドの情報構造と見せ方の素材を合流させてほしい。
そもそも marp でのスライド作成を skill にした方がよいか、規範を個別にどこに分配すべきかなど考えて

## 素材の場所

`~/work/memo/norm-refit/`

- `README.md` — 渡し方と、崩してはいけない条件
- `slide-design-learnings.md` — 依頼の本体
- `images/` — 実例の before / after / 案の画像 16 枚。本文から相対パスで参照している

## 何が書いてあるか

2026-08-26 の OIDC カンファレンス参加報告スライド（marp）を作る過程で、
ユーザーの実測フィードバックから判明したことを実例つきで記録したもの。
規範として確定したものではなく、規範にする候補の素材。

初版は 37 枚のほぼ全部が「見出し + 箇条書き + 地の文」で、
「全体的にほとんど箇条書きと文字ダラダラで読みづらい」という評価だった。
問題は文字数ではなく、どの情報構造も同じ箇条書きに流し込んでいたこと。
文字量の多い 4 枚を選んで各 2 案を作り、実際にレンダリングした画像で比較してもらった回答から、
情報構造ごとの見せ方を 4 つに整理してある。

- 並列で同格の項目が N 個あるとき
- 2 つの経路・構成を対比するとき
- 判断・決断を含まない情報のとき
- 1 つの強い主張・引用があるとき

メタな学びとして 3 項（個別の判断を一般規則へ勝手に昇格させない / 試用中の追加であることを成果物の上で示す /
提示の形式そのものへの要求）と、未整理の起点が 4 項ある。

## お願いしたいこと

「情報構造から見せ方への対応」の各項を、条件付きの規範として既存の norm へ合流させるかを判断してほしい。

条件（どの情報構造のときか）を落として結論だけを規範にすると、当てはまらない場所で誤適用される。
そこを崩さないこと。

## この後のセッションで追加で判明したこと（まだ素材に書いていない）

同じスライドを 37 枚から 27 枚へ組み直す過程で、さらに 4 つ出ている。
素材に追記するかどうかも含めて判断してほしい。

- **図は枚ごとに形を選ぶ。一律の型に押し込まない。**
  手順を「左の箱 → 矢印 → 右の箱」の 1 行にして縦に積む型を 2 枚で試したところ、
  「左右の箱を矢印で繋いだだけの同じ形が縦に並んでいる上に中身はちょこちょこ変わるので本当に読みづらい」
  として却下された。同じ形の反復は、行ごとの差分を読ませる形になって負荷が上がる。
  代わりにレーン（登場人物）を横に並べて矢印を引くシーケンス図にしたら通った
- **確認フォームには前の版の画像も並べる。**
  枚ごとに「今の版」だけを出していたが、前の回の指示が反映されたかを読み手が確かめられない。
  前の版のレンダリング画像を並べると、変更点の文章による説明が薄くても判断できる
- **英語の原文を和訳した箇所を鉤括弧で括らない。**
  逐語引用の直後に和訳を「」で括ると、両方が原文の引用に見える。
  圧縮するなら鉤括弧を使わず地の文で要約する。これは執筆規範の側の話かもしれない
- **marp は inline の `style` 属性を落とす。**
  レイアウトを `style="grid-column: 1 / 3"` のように書いても出力 HTML に残らない。
  桁指定などはテーマ CSS のクラスに移す必要がある。これは作業手順の側の話かもしれない

# cc-marketplace の session plugin に、引き継ぐものが無いときの分岐を入れてほしい。

**方針は決定（2026-09-01、ユーザー判断）: 分岐を入れる。**
着手の時期は未定のため復元タスクからは外し、この依頼を保持先とする。
下の「決めてほしいこと」3 点は未決のまま残っている。

対象: `plugins/session/skills/end/SKILL.md`（必要なら `plugins/session/skills/handover/SKILL.md` も）

## いま何が起きるか

`end` は debrief → retrospective → handover を順に実行し、handover は draft を確定して `todo/` へ移す。
このフローには「引き継ぐ作業が無い」場合の分岐が無い。

そのため、作業が全部終わって復元タスクが 0 件でも、引き継ぎ資料が `todo/` に置かれる。
次セッションの `start` はそれを読み込み、「続きの起点」を提示しようとする。
起点が無いので、次セッションは資料を読んで初めて「やることは無い」と分かる。

## 入れてほしい分岐

復元タスクが 0 件のとき、handover は `todo/` を経由せず `archive/` へ直接置く。
そのうえで、資料の冒頭に「引き継ぐ作業が無いので archive へ直行した」ことと、
次セッションの `start` はこれを読み込まないことを明記する。

判定は復元タスクの件数で決まる。0 件なら archive へ直行、1 件以上なら従来どおり `todo/` へ。

## 実際にやった形

2026-08-28 のセッション（efso-document / to-be/idp、カンファレンス参加報告スライドの仕上げ）で、
この分岐を手で実行した。資料の冒頭に置いた注記はこの形。

```markdown
> [!NOTE]
> 引き継ぐ作業が無いため、todo/ を経由せず archive/ へ直接置いた資料。
> 次セッションの start はこの資料を読み込まない。スライドは完成しており、復元タスクは 0 件。
> 残っていた 2 件のタスクは、ユーザーの判断でやる必要がないものとして「却下と決めたこと」へ移した。
```

資料そのものは `~/ghq_root/github.com/omnisinc/efso-document/to-be/idp/.handover/archive/oidc-slides-completed.md`
に置いてある。手元で見られるなら実例として参照してよい。

## 一緒に直してほしい検査の穴

`plugins/session/skills/handover/scripts/check-handover.mjs` の `task-table` 検査は、
復元タスクの表が無いと必ず findings を積む。

```js
if (!tableStarted) {
  findings.push({
    check: "task-table",
    line: taskSection.line,
    message: "復元タスクの表が無い。復元タスクが無いなら「なし」と明記する",
  });
}
```

メッセージは「復元タスクが無いなら『なし』と明記する」と指示しているが、
「なし」と書いても `tableStarted` は false のままなので指摘は消えない。
指示どおりに直しても通らない検査になっている。

今回は「なし」と書いたうえで、この 1 件を残したまま archive へ置いた。
分岐を入れるなら、復元タスク 0 件を検査側でも通るようにしてほしい。

## 決めてほしいこと

- 判定を復元タスク 0 件だけで行うか、ユーザーの明示（「引き継ぐものは無い」）も条件に入れるか。
  今回はユーザーが明示したうえで復元タスクも 0 件だった
- 注記の文面を skill が定型として持つか、その場で書かせるか
- `start` 側にも手当てが要るか。`archive/` は読まない作りなので不要に見えるが、
  「前回は引き継ぎ無しで終わった」ことを次セッションが知る手段が無くなる。
  知る必要があるかどうかの判断が要る

# cc-marketplace の rules に、差分を説明するときの基準点についての規範を入れてほしい。

反映先の候補は 2 つ。どちらが適切かは判断してほしい。

- `rules/japanese-text-writing/references/decision-docs.md`（判断文書の規範。報告・変更説明はここの管轄）
- `rules/primary-sources-first.md`（「測ったものと本当に測りたかったものが同じかを確かめる」の隣。
  自分の記憶ではなく実物を基準にするという軸が近い）

## 何が起きたか

2026-08-28 のセッションで、スライドの修正内容を確認フォームにまとめて提示した。
枚ごとに「前の版からの変更」を書く欄がある。そこで 2 回、事実と違うことを書いた。

1 回目。ある枚について「前の版で落としていた一文を補足に戻した」と書いた。
実際にはその一文は、同じセッションの中で自分が一度消して、あとで自分が戻したもの。
読み手が最後に見た版から見ると、その一文は最初から最後まで補足にあり、変わっていない。

2 回目。「枚を 1 つ落としたので、26 枚目は前の版では 27 枚目だった」と書いた。
これは 1 つ前の回では正しかったが、この回の前の版とは枚数も並びも同じで、今回の差分ではなかった。

どちらも提示前のレビューで差し戻された。

## なぜ失敗したか

差分の基準点を、読み手が最後に見た版ではなく、自分の編集履歴に置いていた。

1 回目は「セッション中に消して戻した」という自分の作業の軌跡をそのまま書いた。
読み手はその軌跡を見ていないので、「一度失われた情報が復旧された」という誤った理解になる。

2 回目は、前の回に書いた説明文をそのまま持ち越した。
説明文が指す「前の版」が回ごとに変わることを検算しなかった。

根にあるのは、差分を「自分が何をしたか」の記録として書いていたこと。
差分の説明は読み手のためのもので、基準点は読み手の側にある。

## 規範の案

```markdown
## 差分は、読み手が最後に見た版を基準に書く

前の版からの変更を説明するとき、基準点は読み手が最後に見た版に置く。
自分の編集履歴を基準にしない。

- セッション中に消して戻したもの、書き換えてから元に戻したものは、読み手から見れば変化していない。
  作業の軌跡をそのまま差分として書かない
- 前の回に書いた差分の説明文を次の回へ持ち越すときは、それが指す「前の版」が
  変わっていないかを毎回確かめる。回が進むと基準点が動く
- 判定基準: その説明を、読み手が持っている版と今の版だけを並べて検算できるか。
  自分の作業の記憶を持ち出さないと説明が成り立たないなら、基準点がずれている
```

## 自己検証の結果

適用シミュレーション。1 回目の場面で「補足に戻した」と書こうとした瞬間にこの規範を読んだ場合、
「読み手が持っている版と今の版を並べて検算できるか」で止まる。
前の版の補足にその一文があることは確かめられるので、「戻した」とは書けなくなる。止まる。

2 回目の場面では、2 項目が直接当たる。持ち越した説明文の「前の版」を確かめ直すことになる。止まる。

スコープ。ファイル名・ツール名は本文に入れていない。
確認フォームに限らず、PR の説明・変更履歴・リリースノート・進捗報告のすべてに当たる。

## 決めてほしいこと

- 反映先。decision-docs.md か primary-sources-first.md か、それ以外か
- 3 項目のうち 2 項目目（前の回の説明文の持ち越し）を残すか。
  1 項目目と 3 項目目で覆えるとも言えるが、実際に落としたのはこの形なので残したい


# cc-marketplace の執筆規範に、鉤括弧の 2 つの用法が衝突する場合の扱いを入れてほしい。

反映先の候補は 2 つ。両方に足す形もありうる。

- `rules/japanese-text-writing/references/core.md`（「引用」の節）
- `rules/markdown-formatting.md`（もし「複合語句のスコープを鉤括弧で明示する」の条項がここにあるなら、その隣）

## 何が起きたか

2026-08-28 のセッションで、英語のキーノートを引用したスライドを作った。
発表者ノートに原文を `"..."` で逐語引用し、その直後に和訳を「」で括って並べた。

```markdown
- 2 の原文は "identification is not trust. Discovery is not authority. Metadata is not accountability."。
  その手前で「自己申告のエージェントカードは統治ではない」「署名付きでも、鍵が何かに署名したことを示すだけ」と言っている
```

「」の中は和訳であって逐語引用ではない。直前に英語の逐語引用があるため、
両方が同格の引用に見える形になっていた。提示前のレビューで指摘された。

地の文に直したあと、同じ回に書き換えたスライド本文の側にも同型が残っていた。

```markdown
自分は何ができるかを書いた自己申告のカードは、署名を付けても「鍵が何かに署名した」ことしか示さない。
```

こちらの「」は複合語句のスコープを示す用法で、それ自体は既存の規範が認めている。
ただし同じ枚に英語の逐語引用があり、原文 "A signature tells us a key signed something." の和訳でもあるため、
やはり引用に見えた。2 ラウンド目のレビューで指摘された。

## なぜ失敗したか

鉤括弧が 2 つの役割を兼ねていることを、書く側が意識していなかった。

既存の規範は 2 つとも正しい。
「引用は原文のまま転記し、引用符で括る。圧縮したいときは引用符を使わず地の文で要約する」（core.md）と、
「複合語句にはカギカッコでスコープを明示する」（markdown-formatting）。

衝突するのは、逐語引用と同じ文脈に鉤括弧のスコープ用法が現れたとき。
読み手には両者を見分ける手がかりが無い。
1 回目は規範への違反だが、2 回目は規範に沿ったスコープ用法が引用に見えた例で、
どちらの規範を読んでも止まらない。

## 規範の案

```markdown
### 逐語引用と同じ文脈では、鉤括弧のスコープ用法を使わない

原文が外国語の箇所で、和訳を鉤括弧で括らない。地の文で要約する。
逐語引用と同じ段落・同じ箇条書きの中では、複合語句のスコープを示す鉤括弧も使わない。
読み手には、鉤括弧の中身が原文の訳なのか、書き手が範囲を示しただけなのかを見分ける手がかりが無い。

- 判定基準: その鉤括弧の中身を、原文と 1 対 1 で対応させられるか。
  対応させられないなら引用ではないので、鉤括弧を外して地の文にする
- スコープを示す必要が残るなら、語順を変えるか文を分けて、鉤括弧なしで一意にする
```

## 自己検証の結果

適用シミュレーション。1 回目の場面では、和訳を「」で括ろうとした瞬間に判定基準が当たる。
原文と 1 対 1 で対応しない要約なので、鉤括弧を外すことになる。止まる。

2 回目の場面では、同じ枚に英語の逐語引用があるので「逐語引用と同じ文脈」に当たる。
スコープ用法でも使わないことになり、止まる。

スコープ。逐語引用が無い文書では従来どおりスコープ用法を使える。
制限が掛かるのは引用と同居する箇所だけなので、広すぎない。

## 決めてほしいこと

- 反映先。core.md の「引用」節に足すか、スコープ用法を定めている側に足すか、両方に相互参照を置くか
- 制限の範囲。「同じ段落・同じ箇条書き」で足りるか、文書全体に広げるか。
  今回落としたのはどちらも同じ枚（= 同じ文書の同じ画面）の中だった

# norm-refit と無関係で、引き継ぎの復元タスクに長く残っていたもの

2026-09-01 に復元タスクを精査して、norm-refit の計画に属さないものをここへ移した。
復元タスクからは消してある。

## mkdocs-setup の frontmatter が効いていない

`plugins/mkdocs-setup/skills/*/SKILL.md` の 1 行目が `# mkdocs-setup` で、`---` が 5 行目にある。
frontmatter として認識されず、全フィールドが無効になっている。2026-09-01 に実物で確認済み。

直すと `description` が効き始めて発動条件が変わるので、evals が要る
（`.claude/rules/plugin-release.md` の「Evals の作成・実行トリガー」）。

## 共通ページ index の他プロジェクト分の確認

2026-08-27 に `index.html` が古いスナップショットへロールバックした形跡があり、
cc-marketplace のエントリは実態に合わせて直したが、他プロジェクト
（idea-hub・efso-idp・infra・dotfiles・bambu・efso-core-review・voice-recorder・diy-hub）の
エントリは実態を把握していないので触っていない。それぞれのセッションが自分の分を確認する必要がある。

# efso-idp セッション（2026-09-02）からの依頼

## subagent-delegation.md を v2.1.257 の system prompt 文言に合わせて見直す

依頼元: efso-document/to-be/idp のセッション（2026-09-02）。known-issues の差分突合（v2.1.252 → v2.1.257）で分かったこと。

- Opus 5 で subagent 起動を抑えていた system prompt の既定文（「Do not call the AgentTool unless the user requested it」ほか 1 行）は v2.1.257 で消え、同じゲートが新スロット `opus5_reduced_delegation` として再実装された。既定文は「Do not use the Agent tool, workflows, or deep-research unless the user, a CLAUDE.md file, or a skill asks for it」の 1 行
- 許可主体に「CLAUDE.md file, or a skill」が明記された。rule の why にある「skill が手順として定めた起動が巻き添えで止まる」は harness 側で解消している
- user global rule（`~/.claude/rules/cc-marketplace/subagent-delegation.md`）が「a CLAUDE.md file」に含まれるかは未確定。rule の内容は system prompt の claudeMd の枠で届いているので含まれる可能性は高いが、実測していない
- known-issues の `agent-tool-gated-by-system-prompt` は `summary` / `affected` / `how_to_verify` を新スロットに合わせて更新済み（2026-09-02）。dependents にこの rule が載っている

rule の中身は 7 点で、打ち消し（起動は原則許可）は 1 点だけ。残り 6 点（Workflow と deep research は都度確認、単発の読み取りは直接、起動したら報告、待つ間は依存しない作業、依頼文の条件は絞る意図があるものだけ、成果物は要件で検算）は打ち消しとは別の運用規範。

決めてほしいこと:

- user global rule が新文言の許可主体に含まれるかを実測する（Opus 5 セッションで rule だけを置き、CLAUDE.md や skill の指示なしに subagent を起動するかを見る）
- 含まれるなら、打ち消しの 1 点を落として運用規範 6 点の rule に組み替えるか、そのまま残すか
- 含まれないなら、rule は現状維持で known-issues の dependents もそのまま

## Chrome DevTools MCP の設定をグローバルの持ち物にしたい

依頼元: efso-document/to-be/idp のセッション（2026-09-02）。

いまの chrome-devtools MCP の設定は efso-document の `to-be/idp/.mcp.json`（project scope、`.gitignore` で追跡外）にだけある。中身は `npx chrome-devtools-mcp@latest --browserUrl=http://127.0.0.1:{port}` の server が 8 本（`chrome-devtools-1` から `-8`、ポート 9222 から 9229。2026-09-02 に番号付きへ揃えて 8 本にした）。Chrome 本体は `ref-external-spec` の手順で `--remote-debugging-port` と `--user-data-dir=/tmp/chrome-mcp-profile` を付けて手で立てる。

これをプロジェクトの持ち物ではなく、cc-marketplace 側で管理するグローバルの持ち物にしたい。html-communication をはじめ Chrome MCP を使う skill は他プロジェクトにもあり、プロジェクトごとに `.mcp.json` を手で作っている状態を無くしたい。

要求:

- server の定義（4 本分）を user scope の設定として配る。配り方は `claude mcp add --scope user` の手順書か、`~/.claude.json` へ入れる形か、cc-marketplace の判断に任せる
- Chrome の起動手順（ポートごとに別の `--user-data-dir` で立てる、`pkill -f remote-debugging-port` を打たない）を cc-marketplace 側の reference に持ち、efso-document の `ref-external-spec` からはそこを参照する形にする
- 並列 agent への渡し方（agent ごとに使う server 名を指示する、専用タブを作って毎回 select_page する、snapshot の前に list_pages で URL を確かめる）も同じ reference に書く

参照先（efso-document 側の実物）:

- MCP 設定: `/Users/ryosuke/ghq_root/github.com/omnisinc/efso-document/to-be/idp/.mcp.json`（追跡外。8 本の server 定義）
- Chrome の起動手順と 403 回避: `/Users/ryosuke/ghq_root/github.com/omnisinc/efso-document/to-be/idp/.claude/skills/ref-external-spec/SKILL.md` の「Chrome MCP による取得手順」。並列 agent への渡し方もここへ足す（2026-09-02 のセッションで着手）
- 並列で走らせたときの運用の記録: efso-document の `to-be/idp/.handover/` の 2026-09-02 の draft

決めてほしいこと:

- server の本数の既定値（今回は 8 本にした）
- Chrome を手で立てる運用のままにするか、MCP server 起動時に `--isolated` で自前起動させる形へ寄せるか。403 回避は非 headless の専用プロファイルで成立しているので、後者に寄せるなら headless にしないこと

# cache-keepalive を monitors.json へ移行できないか検討する

依頼元: efso-document/to-be/idp のセッション（2026-09-10）。

毎セッション手で `/cache-keepalive` を叩いているので自動起動にしたい、という要求から出た。
このセッションでは SessionStart hook 方式で作る判断になった（ユーザー選択）。
`monitors/monitors.json` への移行は、下の未確認 1 点が解ければ成立する。

## いまの形と移行先

いまは skill が Monitor ツールで張る。SKILL.md がセッション JSONL のパスを解決し、
モデルが Monitor ツールの呼び出しへ埋め込む。毎セッション Claude のターンを 1 つ使う。

移行先は plugin の `monitors/monitors.json`。host が persistent Monitor task として自動 arm するので、
Claude のターンを消費しない。`.claude/rules/plugin-design.md`「監視機構の選択」が
「継続的な監視は monitor」と定めており、cache-keepalive は常駐監視なのでこちらが本来の機構。

## 確認済み（Claude Code 2.1.267 のバイナリから）

- スキーマはトップレベルが配列。フィールドは `name` / `command` / `description` / `when`。
  `persistent` に相当するフィールドは無く、持続性は機構の前提
- `when` は `always`（セッション開始と plugin リロードで arm）か `on-skill-invoke:<skill>`。既定は `always`
- 宣言の経路は 3 つ。`plugin.json` の `monitors` に配列を直接書く / 同じキーに plugin root からの
  相対パス文字列を書く / どちらも書かなければ `<pluginRoot>/monitors/monitors.json` が自動でロードされる
- command で置換されるのは `${CLAUDE_PLUGIN_ROOT}` / `${CLAUDE_PROJECT_DIR}` / `${CLAUDE_PLUGIN_DATA}` と、
  host の `process.env` から解決される `${ENV_VAR}`。`${user_config.*}` は例外を投げて拒否される
- 二重 arm の防止は 2 段。同一 plugin 内の `name` 重複を zod が弾き、実行時は
  `${pluginName}:${name}` をキーにしたセッション単位の Set で重複排除する
- 機能ゲート `pluginMonitors` があり、通常セッションでは有効

## 障害: セッション JSONL のパスを渡す公式経路が無い

monitor は stdin で何も受け取らない。hook は stdin の JSON で `session_id` / `transcript_path` /
`scratchpad_dir` を受け取れるが、monitor にはこの経路が無い。監視対象の JSONL を特定できないと
keepalive が成立しないので、ここが移行の可否を決める。

未確認: plugin monitor の子プロセスに `CLAUDE_CODE_SESSION_ID` が注入されるか。
バイナリには子プロセスの env を組む関数があり `CLAUDE_CODE_SESSION_ID` を入れているが、
monitor の spawn がその経路を通るかはバイナリからは読めなかった。
`ps -Eww` は他プロセスの env を返さないため実測もできていない。
monitors.json を作って新セッションを開けば分かる。

波括弧付きの `${CLAUDE_CODE_SESSION_ID}` は host 側の `process.env` からロード時に展開されるので、
monitor スクリプトへ渡すなら波括弧なしの `$CLAUDE_CODE_SESSION_ID` を使う。

## 過去に monitors.json を入れて消した経緯

`plugins/claude-known-issues/monitors/monitors.json` が commit `aa7079e`（0.1.0）で入り、
`6ac9006`（0.2.0）で削除されている。削除理由は「one-shot スクリプトを monitor に置いたため、
host が即終了ストリームを stream ended として検知し、毎セッション開始時に無意味な終了通知が出た」。
cache-keepalive は常駐監視なのでこの理由は当たらない。

## 一緒に直すもの

SKILL.md の「セッション JSONL」セクションが `${CLAUDE_SESSION_ID}` を使っているが、
この変数は空。実際に値を持つのは `CLAUDE_CODE_SESSION_ID`（2026-09-10 に実測）。
hook 方式でも monitors.json 方式でもこれは直す。

# github-pr の Pre-open checklist から、セルフレビューの項目が落ちる

依頼元: efso-document/to-be/idp のセッション（2026-09-11）。

`plugins/github-pr/skills/create/references/generate-body.md` の Pre-open checklist は、
セルフレビューと行指定コメントの確認を 1 項目に抱き合わせている
（`- [ ] セルフレビューをし、行指定コメントの内容を確認した`）。

ショート版は行指定コメントを付けない規定（`short/rules.md` の「行指定コメントは不要（変更が自明なため）」）なので、
この項目は丸ごと落ちる。実際に落とした。セルフレビューはショート版でも要るので、項目が消えるのは困る。

## やってほしいこと

セルフレビューと行指定コメントの確認を別項目に分ける。項目は次の 5 つになる。

- PR body の内容を確認した（必要に応じて手動で書き換えてよい）
- セルフレビューをした
- 行指定コメントの内容を確認した
- How to check のリスト内容が妥当であることを確認した
- How to check の各項目を確認しチェックをつけた

行指定コメントの項目をロング版だけに出すか（括弧書きで示すか、テンプレートを版ごとに分けるか）は判断に任せる。

# handover が archive への参照だけで済ませてしまう

依頼元: efso-document/to-be/idp のセッション（2026-09-11）。

`plugins/session/skills/handover/SKILL.md` のステップ 3 は、やらないと決めたものを
`## 却下と決めたこと` セクションへ書くと定めるが、内容を転記するとは書いていない。

結果として、複数セッションにわたって参照チェーンが積み上がっていた。

- 2026-09-11 の draft: 「（前セッションからの引き継ぎ分は archive の `auth0-removal-epics-and-open-prs.md` にある）」
- その `auth0-removal-epics-and-open-prs.md`: 「（前セッションからの引き継ぎ分は archive の `continue-alt-idp-requirements.md` にある）」
- `continue-alt-idp-requirements.md`: ここに初めて実内容 14 件がある

archive を 2 段辿らないと却下の中身が読めない。handover は次セッションが 1 本読めば
再開できる資料のはずで、参照チェーンはその目的に反する。

## やってほしいこと

handover 1 本で自己完結させることを skill に定める。

- 「却下と決めたこと」に archive や過去の handover への参照だけを書かない。引き継ぐ却下は実内容を転記する
- 転記の対象は「次セッションが再提案しうるもの」に絞る（既にある規定）。絞った結果 0 件なら、その節に「無し」と書く
- 同じことが他の節（ゴール・原則、参照すべき資料）にも当たるかは判断に任せる。
  「参照すべき資料」は所在を指すのが役目なので、参照で足りる可能性がある

機械検査（`check-handover.mjs`）で「archive への参照だけの節」を検出できるなら、そちらに寄せる案もある。
